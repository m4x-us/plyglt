@BRAND.md
@CURRICULUM.md
@AGENTS.md

## Architecture

### 1. Layer Map

The codebase is organized into four layers. Dependencies flow strictly downward — no layer imports from a layer above it.

```
app/          — Next.js routes, pages, and layouts. Thin: no business logic.
components/   — React UI components. Import from hooks/ and lib/ only.
hooks/        — Custom React hooks. Compose lib/ modules and Zustand stores.
lib/          — Pure services and utilities. No React. No Zustand imports.
store/        — Zustand stores (srsStore, settingsStore, entitlementStore).
              — Peer of lib/, not above it. Stores import from lib/, not vice versa.
content/      — Static card data and type definitions. Imported by lib/packLoader.ts.
```

Notable modules:
- `lib/utils.ts` — pure utility functions; exports `localDateStr(d?)`, `sha256Hex(text)`, `packUrl(lang)`, and `formatRelativeTime(pastMs, nowMs?)` (Task #520 — coarse "just now"/"3m ago"/"2h ago"/"5d ago" formatting for `components/SyncSignIn.tsx`'s last-synced status line). Used by `hooks/useStudySession.ts`, `lib/queue.ts`, `lib/packLoader.ts`, `lib/specialtyPackLoader.ts`, and `components/SyncSignIn.tsx`.
- `lib/packTypes.ts` — shared type definitions for the pack subsystem: `Pack`, `PackMeta`, `Manifest`, `LoadPackResult`. Single source of truth — `lib/packLoader.ts`, `lib/specialtyPackLoader.ts`, and `lib/specialtyPackMerge.ts` import types from here. No React, no Zustand.
- `lib/checkout.ts` — checkout URL constants and pricing ($34.99/yr — annual plan only), plus the customer portal URL. Re-exported by `lib/entitlement.ts`. Used by `components/BuyModal.tsx` and `app/settings/page.tsx`.
- `lib/specialtyPackLoader.ts` — orchestrates specialty pack download: entitlement gates, in-flight dedup, and deciding where bytes come from (cache vs network) plus sha256 verification. Exports `loadSpecialtyPack()`, `getLoadedAddOns()`, `clearSpecialtyCache()`. Re-exports `getLoadedAddOns` via `lib/packLoader.ts`. Must remain pure (no React, no Zustand). The parse-verify-merge-persist "commit" step (merging verified bytes into the base pack's in-memory cache and persisting to storage) lives in `lib/specialtyPackMerge.ts` — extracted under the Rule 1 400-line cap (Task #447).
- `lib/specialtyPackMerge.ts` — the commit step of the specialty-pack load pipeline: parses add-on JSON, shape-validates, re-checks the caller's deactivation-generation guard (twice — before merging and bracketing the storage-persist writes, Task #409), merges units into the base pack, and persists to platform storage. Exports `mergeSpecialtyPackFromJson()`. Imported ONLY by `lib/specialtyPackLoader.ts`. Extracted from that file under Rule 1 (Task #447) — same pattern as `store/entitlementStore.ts` → `store/entitlementAddOns.ts` (Task #412): receives the caller's `GenerationGuard` instance as a parameter rather than owning or importing one, so it has no dependency on that file's other module-scope state.
- `lib/basePackLoader.ts` — the storage/network load path for base packs (cache validation, sha256 verification, download, offline stale-cache re-verification, per-language eviction-generation guard — Task #436). Extracted from `lib/packLoader.ts` under the Rule 1 400-line cap (Task #378). Imported by exactly two legal callers — `lib/packLoader.ts` (the real runtime caller) and `lib/packResolver.ts` (the `LoadPackOptions` type only, erased at compile time) — a mechanical test in `tests/packLoader.test.ts` enforces this boundary (Task #428).
- `lib/packResolver.ts` — pure, dependency-injected orchestration of what to load in which order: specialty codes resolve their base pack (static seed or network load-first with failure propagation) before the specialty request (Task #378). No React, no Zustand; unit-tested directly in `tests/packResolver.test.ts`; `hooks/useLangPack.ts` is the sole production caller.
- `lib/generationGuard.ts` — shared snapshot/bump/isStale invalidation primitive (`createGenerationGuard()`) guarding in-flight async work against writing back state that a concurrent eviction cleared. Used by `lib/basePackLoader.ts` (one guard instance per language, keyed in a `Map` — Task #436) and `lib/specialtyPackLoader.ts` (Task #409).
- `lib/featureFlags.ts` — feature flag reader (`getFeatureFlags()`); also exports `isProEnabled(flagValue, licenseType, validUntil)`, a single combinator for all Pro-gated features (corrected round-12 audit — this bullet previously described a stale 2-arg signature with no expiry check). Returns `false` if `flagValue` is falsy or `licenseType !== "subscription"`; otherwise checks `validUntil` against `SUBSCRIPTION_GRACE_PERIOD_MS` (a lapsed subscriber stays gated-in through the grace period, then loses access) — `validUntil: null` means no expiry (pre-versioning migrated users). All Pro-gated call sites must use this combinator instead of inline logic.
- `lib/srs.ts` — FSRS v4 scheduler; also owns `prerequisitesMet(card, progressMap)` and `selectQualifyingNewCard(allCardMap, cards, introductions)` (Batch 18), the single source of truth for `Card.prerequisites` gating. Used by `store/srsStore.ts`'s `getNewCards` (FSRS new-card queue) and `hooks/useStudySession.ts`'s introduce-on-mount effect (introduction-engine gating) — both routes must stay on this shared implementation so the two subsystems can never diverge on how prerequisites are enforced.
- `hooks/useStudySession.ts` — session management hook; 16-param contract (corrected round-11 audit — the module's own `UseStudySessionParams` type gained `getNearDueCards`, `peekResumableSession`, and `clearExpiredResumableSession` since this bullet was last counted at 14) managing queue, position, ratings, active session commit, and session-start introduction auto-selection. Do not add business logic here. The mount-fill effect (Task #533, extended by Batch 23's owner-ratified session floor, current mechanism as of Wave 3's Tasks #562/#565/#573) has two phases: the normal path introduces at most one new card per day (`canIntroduceNewCard` default cap, all session types); then, for `isInterrupt` sessions only, a fill pass raises the queue to `lib/queue.ts`'s `INTERRUPT_SESSION_FLOOR` (6) — first flexing additional new-card introductions (hard cap `INTERRUPT_SESSION_MAX_NEW` = 3 per session, the working-memory limit) via a `while` loop whose own condition re-evaluates `canFlexIntroduceToday(canIntroduceNewCard, today)` (`hooks/useInterruptConfig.ts`'s thin wrapper around `canIntroduceNewCard(today, INTERRUPT_FLEX_DAILY_MAX)`, extracted Wave 6/Task #618 — corrected round-11 audit, this bullet previously described a direct call that no longer exists) on every iteration (Task #562) — a per-introduction recheck against live store state, not a single check computed once before the loop starts, so it genuinely enforces `INTERRUPT_FLEX_DAILY_MAX` as a cross-session daily ceiling rather than letting each session's mount pass it once and then flex a full batch regardless of what earlier sessions that same day already introduced. That single condition covers two distinct stop reasons without distinguishing them (Task #566): the `strandedAcrossDays` pause, or the daily ceiling itself. Fill step 2 pulls near-due FSRS reviews slightly early (`getNearDueCards`, injected), over-fetching the entire near-due pool rather than a heuristic limit (Task #541). The former post-loop "never-empty" backstop was deleted in Wave 3 (Task #565) — it was structurally dead code (a pure function of inputs frozen since the loop's last attempt, so a repeat call could never succeed where the loop had already failed); a session that reaches the end of the fill pass with zero content is genuinely empty, per Task #561's "floor is a target, not a guarantee." Scoped to `isInterrupt` deliberately: unit/global sessions keep their natural size. `hooks/useStudyQueueSetup.ts` caps the interrupt queue at `INTERRUPT_SESSION_CAP` (8) — sessions run 6-8 cards, targeting BRAND.md's ratified 45-90s range; `lib/queue.ts`'s own comment on the constant discloses the real worst case as 64-120s (8 cards at 8-15s/card), up to 30s past the 90s target — a deliberate tradeoff (Batch 23, owner-ratified) favoring a longer worst-case session over truncating a heavy backlog day's content mid-fill (corrected round-11 audit — this bullet previously stated the 45-90s range as an unqualified fact rather than a target with a disclosed exception). Separately, round-11's audit also found and fixed a live bug in `handleRate` (`hooks/useStudySession.ts`): a wrong ("again") answer's requeue-insertion was unscoped to `isInterrupt` and had no cap check, so repeated wrong answers in an interrupt session grew the queue past `INTERRUPT_SESSION_CAP` with no ceiling — fixed by skipping the requeue once an interrupt session is already at the cap (the card is scored wrong and returns via normal scheduling instead of immediate in-session retry). A pending resume decision is checked before the empty-queue screen (round-7 fix, Task: batch 23 audit) — the mount-fill effect skips filling while a resumable session is pending (Task #629), so a caught-up day can have an empty queue and a pending resume at the same time, and the wrong screen ordering would silently strand that session (Rule 23).
- `components/BuyModal.tsx` — primary conversion surface; renders pricing and opens the checkout URL via `openExternalUrl`. Receives `onActivate` callback for key entry flow.
- `components/UpdateChecker.tsx` — invisible component mounted inside `EntitlementValidator.tsx`. Calls `checkForUpdates()` on mount in Tauri. Never auto-installs.
- `components/LanguageGrid.tsx` — language picker on `app/page.tsx`; implements Free / Unlock / In-development display states. Also renders an Add-ons section (specialty packs) when any registered specialty packs exist for the selected language. Receives `hasAddOn(code)` callback to determine purchase state.
- `lib/interruptGate.ts` — the shared, cross-device interrupt-firing gate (Batch 21, Task #528). Pure client module (no React/Zustand) exposing `readInterruptGateState(userId, timeoutMs = 750)` (queries the most recent `effective_until` for a user from the `interrupt_gate_events` Supabase table; returns an explicit `{status:"known"|"unknown", ...}` result, never decides fire-vs-suppress itself) and `recordInterruptGateEvent({userId, deviceId, eventType, occurredAt, minutesUntilEligible})` (writes a `fired`/`snoozed` row). Called by `components/InterruptHandler.tsx` (Task #529, the real firing-decision gate check) and `lib/tauriInterrupt.ts`'s `snoozeInterrupt` (Task #530). See `docs/INTERRUPT_ARCHITECTURE.md` §5–§6 for the full schema and the REST-over-Realtime cost reasoning.
- `hooks/useSnoozeAndExit.ts` — extracted from `app/study/page.tsx`'s "Snooze X min" button (Task #530) to keep that route under CLAUDE.md's 150-line cap. Resolves `userId`/`deviceId` via a one-time `.getState()` snapshot (not a reactive subscription — the value is only needed at click time), calls `snoozeInterrupt`, then `exitMandatoryMode`, then navigates.
- `hooks/useHydrationStuck.ts` — bounded, read-only observation of a loading condition that isn't guaranteed to resolve on its own (Task #644, generalized Batch 23 round-7). Takes a single `blocked: boolean` (the caller's own composite gate condition — never a narrower single sub-signal, see the hook's own round-7 comment) and returns `true` once it has stayed `true` continuously for `HYDRATION_STUCK_TIMEOUT_MS` (15000ms — round 8 briefly widened this to 45000ms on the mistaken belief that `app/study/page.tsx`'s network `packLoading` could legitimately exceed 15s downloading `it.json`; round 9 reverted it after confirming `it` is bundled statically (`hooks/useLangPack.ts`'s `STATIC_PACKS`), never fetched at runtime — sufficient justification alone, since no reachable-today path makes `packLoading` network-bound at all. Round 10 corrected a second, overclaiming statement round 9's own revert comment made — that every network pack fetch is bound to ~20s "regardless of connection speed" — which undercounted a real, sequential multi-fetch chain (manifest → base pack → specialty pack, each independently 20s-bounded) plus an unbounded storage-cache-read step ahead of it; see the hook's own comment for the full correction. Doesn't change today's value, since `it` never reaches any of that machinery). Never unblocks the caller's actual gate — `app/study/page.tsx` renders `components/StudyHydrationStuck.tsx` (a static retry screen whose button calls `window.location.reload()`) when this returns `true`, in place of the indefinite "Loading…" state.
- `lib/tauriInterrupt.ts` — the single gateway for interrupt-related Tauri IPC commands (`updateInterruptConfig`, `snoozeInterrupt`, `enterMandatoryMode`, `exitMandatoryMode`, `updateTrayBadge`, and — added Task #526 — `markInterruptFired()`, the JS-side confirmation that a real interrupt session was shown, now the only thing that advances the Rust `last_triggered_secs` clock; see `src-tauri/src/interrupt.rs`'s `mark_interrupt_fired` command). `snoozeInterrupt` also optionally writes a shared-gate `snoozed` event via `lib/interruptGate.ts` when passed a `{userId, deviceId}` context (Task #530).
- `src-tauri/src/push.rs` + `lib/tauriPush.ts` + `hooks/usePushRegistration.ts` + `hooks/usePushInterruptTap.ts` — the iOS APNs push subsystem (Task #522, built+Simulator-verified 2026-08-14). No Tauri plugin provides this: tauri-plugin-notification's iOS delegate deliberately ignores `UNPushNotificationTrigger` notifications and never registers for remote notifications (verified in its Swift source). `push.rs` adds token callbacks to tao's runtime-declared `AppDelegate` ObjC class via `class_addMethod`, wraps the notification-center delegate in a proxy (push taps → `push:notification-tapped` event + a cold-start pending-tap flag; local notifications forwarded untouched to the plugin's delegate), and exposes three commands (`register_for_push_notifications` — returns false off iOS, the platform gate JS relies on instead of user-agent sniffing; `get_push_token`; `take_pending_push_tap`). `lib/tauriPush.ts` is the JS gateway (event names are the wire contract — keep in sync with push.rs's `EVENT_*`). `usePushRegistration` (mounted in `components/InterruptHandler.tsx`) uploads the token via `lib/pushTokenClient.ts` when signed-in + Pro + interrupts enabled + permission granted — permission prompting stays owned by the settings toggle (`hooks/useNotificationPermission.ts`). `usePushInterruptTap` routes taps to `/study?mode=interrupt`, mirroring `hooks/useInterruptDeepLink.ts`'s cold/warm two-path shape. The `plyglt://` scheme is registered on iOS via `CFBundleURLTypes` (Info.plist + project.yml, kept in sync) and `aps-environment` lives in the entitlements file (also mirrored in project.yml). Simulator testing: `xcrun simctl push booted app.plyglt <payload.json>` with `"Simulator Target Bundle"` set. Two iOS build-system traps (both hit live 2026-08-14): (1) Xcode's Signing & Capabilities editor can rewrite `plyglt_iOS/Info.plist` and silently DROP hand-added keys — the first TestFlight upload shipped without `CFBundleURLTypes` this way; after any Xcode GUI session, diff Info.plist, and treat project.yml as the source of truth. (2) Never hand-run `xcodegen generate` in `src-tauri/gen/apple/` after builds have populated `Externals/` — XcodeGen sweeps the built `libapp.a` files into the project as resources, breaking every subsequent build with "Multiple commands produce libapp.a"; if the pbxproj is ever broken this way, restore it from git rather than regenerating.
- `hooks/useInterruptConfig.ts` — wraps `store/settingsStore.ts` + `store/srsStore.ts` (and, since Task #529, `store/authStore.ts`'s `userId` and `store/syncStore.ts`'s `deviceId`) so `components/InterruptHandler.tsx` stays within the components/ → hooks/ layer boundary. `computeDue(units)` sums FSRS-due cards (`getStats().due`) plus introduction-cadence-due cards and at most one qualifying new card (Task #523) — the fire-gate now matches what `lib/queue.ts`'s `buildQueue` actually puts in a session, closing a gap where an introduction-only day never fired an interrupt at all. Task #533: when that normal-cap sum is 0, `computeDue` falls back to counting any untouched, prerequisite-met card in the catalog (ignoring the daily cap), and — Batch 23 — any near-due card (`getNearDueCards`) when even that comes up empty, so the interrupt still fires in every scenario the session floor can actually serve. `hooks/useStudySession.ts`'s mount effect performs the matching fill once the session opens. See `store/srsStore.ts`'s `canIntroduceNewCard(today, maxPerDay?)` for the underlying cap mechanism. Server mirror: `send-interrupt-notifications` never skips a gated-eligible user for a zero due estimate (Batch 23 removed `skippedNoCards`); the payload floors its announced count at 6 — keep that constant in sync with `lib/queue.ts`'s `INTERRUPT_SESSION_FLOOR`.
- `next.config.ts` + `src-tauri/tauri.ios.conf.json` — the iOS live-dev setup (Task #522, RESOLVED 2026-08-14; supersedes the earlier assetPrefix approach, which is deleted). Tauri only routes a mobile webview through its internal `tauri://localhost` scheme-handler proxy when devUrl's host is literally `localhost` or an IP (tauri source: `manager/webview.rs` `is_local_network_url`) — that proxy origin was the root cause of every iOS dev-asset failure (WebKit mixed-content against plain HTTP; Next 16's cross-origin dev-resource block against HTTPS, which 403s absolute-URL asset fetches from a `tauri://` document because WKWebView sends no Referer from custom-scheme origins, so `allowedDevOrigins` can never match). The fix avoids the proxy entirely: `tauri.ios.conf.json` (Tauri's per-platform config merge) overrides `devUrl` to `https://plyglt.localhost:3050` — macOS resolves `*.localhost` to loopback natively, the non-`localhost` hostname makes Tauri navigate the webview directly to the dev server exactly like desktop, and `beforeDevCommand` runs `npm run dev:https` (Next's `--experimental-https` with the mkcert certs in gitignored `certs/`). Document origin = dev server origin, so assets and the HMR websocket are same-origin; hot reload verified live on the Simulator. New-machine setup (one-time): `brew install mkcert`, `mkcert -install` (needs password), `mkcert -key-file certs/localhost-key.pem -cert-file certs/localhost.pem localhost plyglt.localhost 127.0.0.1 ::1`, and `xcrun simctl keychain <sim-udid> add-root-cert "$(mkcert -CAROOT)/rootCA.pem"`. Caveat: editing only `tauri.ios.conf.json` does NOT trigger a Rust rebuild (the config is baked in at compile time via `generate_context!` and cargo doesn't track the platform file) — `touch src-tauri/tauri.conf.json` after changing it. Desktop `tauri dev` is untouched (plain-HTTP devUrl, direct navigation, same-origin).

**Interrupt engine, cross-device (Batch 21, `docs/INTERRUPT_ARCHITECTURE.md`, COMPLETE 2026-08-13):** every trigger path — desktop unlock/wake/idle (`src-tauri/src/os_events.rs`), desktop's scheduled poll (`src-tauri/src/interrupt.rs`), and mobile's push dispatch (`supabase/functions/send-interrupt-notifications`) — now respects one interval-based gate instead of firing independently, and the "last fired" clock only advances on a real fire with content, never on an empty check. Firing and snoozing on any of a user's devices are recorded to the shared, append-only `interrupt_gate_events` Supabase table (migration `20260813000000_interrupt_gate_events.sql`) so a Pro user's multiple devices don't independently re-interrupt them. Unified interval default: 90 minutes on both desktop and mobile. `store/settingsStore.ts`'s `dndStart`/`dndEnd` are now the single canonical quiet-hours setting for both platforms (default realigned to `"21:00"`/`"08:00"`, the true complement of mobile's `push_tokens` waking-hours default).

**Rule:** `lib/` must never import from `store/`, `hooks/`, `components/`, or `app/`. `store/` must never import from `hooks/`, `components/`, or `app/`. Violations are a stop-the-line event.

---

### 2. Tauri Graceful-Degradation Pattern

`lib/tauri.ts` is the single gateway to all Tauri APIs. Every export in that file degrades gracefully when running in a browser (Next.js web build or test environment):

- `isTauri` — boolean, `false` during SSR and in any non-Tauri window.
- `invoke(cmd, args)` — wraps `@tauri-apps/api/core`. Returns `null` in web; never throws.
- `listen(event, handler)` — wraps `@tauri-apps/api/event`. Returns a no-op unlisten function in web.
- `checkForUpdates()` — queries the Tauri updater plugin; no-ops in web. Never auto-installs.
- `enableAutostart()` / `disableAutostart()` — Tauri autostart plugin; no-op in web.

**Rule:** Never import `@tauri-apps/api` directly from any file outside `lib/tauri.ts`. All Tauri surface area flows through this module. This keeps the web build clean, keeps tests runnable without a Tauri context, and provides a single point to audit for platform-specific behaviour.

---

### 3. Platform Storage Abstraction

`lib/storage.ts` exports `createPlatformStorage(storeName)`, a factory that returns a Zustand-compatible `StateStorage` object backed by the correct persistence layer for the current runtime:

- **Tauri (desktop):** `@tauri-apps/plugin-store` — JSON file in the OS `appDataDir`. Survives browser cache clears. Scoped to the app, not the browser profile.
- **Web / browser:** `localStorage` — synchronous, origin-scoped.

All three Zustand stores (`srsStore`, `settingsStore`, `entitlementStore`) pass a `createPlatformStorage(...)` instance to Zustand's `persist` middleware. Store hydration is asynchronous; use `useIsHydrated()` to gate any UI that requires the full persisted state before rendering.

**Rule:** Never call `localStorage` directly from any file outside `lib/storage.ts`. The abstraction layer exists precisely so that desktop and web builds behave identically from the perspective of every caller.

---

### 4. Migration Convention

`store/migrations.ts` is the single source of truth for all Zustand store schema migrations. Each persisted store has:

- A `*_VERSION` integer constant (e.g. `SRS_VERSION`).
- A `*_MIGRATIONS` record mapping each version number to a migration function.
- An exported `migrate*Store(persisted, storedVersion)` function that walks the chain from the stored version to the current version, one step at a time.

**When changing the persisted shape of a store:**
1. Increment the relevant `*_VERSION` constant.
2. Add a new entry to the `*_MIGRATIONS` record for the new version number.
3. Add a test in `tests/migrations.test.ts` that exercises the new migration on a realistic stored blob.

**Never remove an entry from a migrations record.** The chain must remain intact to upgrade data from any historical version. Throwing on a missing migration step is intentional — silent fallbacks would corrupt user data.

---

### 5. Entitlement Model

Entitlement is **client-only and honour-system**. There is no server-side license verification. This is an intentional, owner-confirmed trade-off (decision 2026-06-24): the product prioritises offline-first operation, zero backend dependency for core functionality, and user privacy over DRM enforcement.

`lib/entitlement.ts` and `store/entitlementStore.ts` manage license state locally. `lib/licenseTypes.ts` defines the `LICENSE_TYPES` enumeration and the `LicenseType` type. Activation is handled by `hooks/useLicenseActivation.ts`. Pricing constants and checkout URLs live in `lib/checkout.ts` (re-exported by `lib/entitlement.ts` for backwards compatibility).

**Specialty pack add-ons:** `store/entitlementStore.ts` also tracks purchased specialty packs via `purchasedAddOns: string[]`. Two store actions manage this: `hasAddOn(code)` returns whether a given specialty pack code has been purchased; `purchaseAddOn(code)` appends the code idempotently. `lib/entitlement.ts` exports a parallel pure function `hasAddOn(state, code)` for use outside React. The entitlement store schema is at `ENTITLEMENT_VERSION = 3` (see `store/migrations.ts`). `purchaseAddOn`'s implementation (the Pro gate, receipt-format validation, and the Tauri IPC verify_addon_receipt call) lives in `store/entitlementAddOns.ts` — extracted under the Rule 1 400-line service cap (Task #412). `createPurchaseAddOn(set, get)` closes over `entitlementStore.ts`'s real Zustand `set`/`get`, typed against narrow interfaces rather than importing `EntitlementState`, so the two modules have no circular dependency. `entitlementStore.ts` re-exports `ERR_ADDON_*` and `PurchaseAddOnResult` so existing external imports are unaffected.

**Do not treat this as a bug or a missing feature.** Document it as designed when writing tests, comments, or new features that touch entitlement.

---

### 6. Pack Format

Language packs are served as static JSON files at `public/packs/{lang}.json`. The shape of each file is defined by the `Pack` interface in `lib/packTypes.ts`.

`lib/packLoader.ts` manages a three-level cache (in-memory → platform storage → network) and performs sha256 integrity verification on every load:

- On cache hit: re-verifies the sha256 hash against the manifest before serving. A hash mismatch evicts the cached copy and re-fetches.
- On network fetch: verifies sha256 before writing to storage. A mismatch is a hard error — no corrupted pack is ever cached or returned.

The manifest lives at `public/packs/manifest.json` and maps each language code to `{ version, sha256, size, name }`. `loadPack` validates the requested language code against `READY_PACK_CODES` before making any network request, preventing path traversal and storage key poisoning.

Only `it` (Italian) and `es` (Spanish) are registered in `lib/langRegistry.ts`.

**Specialty packs (add-ons):** `lib/langRegistry.ts` exports the `SpecialtyPack` interface, the `SPECIALTY_PACKS` array, and `isSpecialtyPackCode(s)` (the canonical ready-check — returns true iff the code is registered AND `ready:true`). The former `isReadySpecialtyPackCode` alias was deleted in Task #380 (completing #361) — every call site now uses the canonical name; do not reintroduce the alias. Specialty packs extend a base language pack with domain-specific vocabulary (e.g. `it-medical`). `lib/specialtyPackLoader.ts` handles the merge path: `loadedAddOns` tracks which add-ons are in memory; `getLoadedAddOns()` exposes this list (re-exported via `lib/packLoader.ts`); `loadSpecialtyPack(lang, memCache, manifest)` performs the fetch, sha256 check, and unit merge. `lib/packLoader.ts` delegates the specialty branch to `lib/specialtyPackLoader.ts`. Attempting to load a specialty pack before its base language pack returns `{ ok: false, error: "base_pack_not_loaded" }`. The base pack's cards are never duplicated — specialty packs are additive overlays.

**Content authoring, per language:** `scripts/exportPack.ts` compiles `content/index.ts` (Italian, the legacy unnamespaced `u{unit}-t{tier}-{seq}` ID format — frozen, never migrate) or `content/{langCode}/index.ts` (every language after Italian, namespaced `{lang}-{level}u{unit}-t{tier}-{seq}` IDs, e.g. `es-a1u01-t1-001`) into `public/packs/{langCode}.json`, then updates `public/packs/manifest.json`. Because the manifest holds every language's entry, concurrent `exportPack.ts` runs for different languages share a cross-process mutex (`scripts/manifestLock.ts`'s `withManifestLock`, an `mkdirSync`-based lock directory with staleness-based reclaim) guarding the manifest's read-modify-write section. `content/es/` currently holds only a small, explicitly-labeled scaffold unit proving the pipeline runs end-to-end — not real curriculum content (see that file's own header comment). `scripts/checkCardIds.ts` and `scripts/lintCardQuality.ts` (per-language baseline files, `scripts/cardQualityBaseline.{lang}.json`) both run in CI (`.github/workflows/ci.yml`) against every registered language's pack, not just as a manual local step.

**Source language (the learner's interface language, not the target language being studied):** `lib/language.ts` exports `SOURCE_LANGUAGES`, `DEFAULT_SOURCE_LANG_CODE` ("en"), and `isKnownSourceLangCode(code)`. Persisted in `store/settingsStore.ts`'s `sourceLang` field (`SETTINGS_VERSION` 3, `store/migrations.ts`), selected via `app/page.tsx`'s "I speak" picker. Deliberately independent of target-language storage (`lib/constants.ts`'s `LANG_PAIR_KEY`, `store/srsStore.ts`'s `srs-{pair}` keys) — changing the interface language must never fork a user's SRS review history for the language they're actually learning. `components/StudyCard.tsx` reads `sourceLang` to resolve a card's prompt/accepted text (`getPrompt`/`getAccepted`) for `produce`/`recognize` cards. Because a `recognize` card's accepted answer is written in the source language while every other card type's accepted answer is in the target language, article-stripping during grading must use the matching language's rule: `getSourceLanguageArticles(sourceLangCode)` for `recognize` cards, `lang.articles` (the target `LanguageConfig`) for everything else — using the wrong one silently marks a correct, article-omitted answer wrong. `getRecognizeLabel(sourceLangCode)` supplies the card-type instruction text ("Translate to Español") for the same reason `lang.uiStrings.cardLabels.recognize` alone cannot: that label is a property of the target language, not the source language a `recognize` card is actually graded against. `store/settingsStore.ts`'s persist config validates `sourceLang` in a custom `merge` function on every hydration (not just via `migrate`, which zustand skips whenever the stored version already matches current) — a corrupted or hand-edited value must never reach `getPrompt`/`getAccepted`'s dynamic property lookup unvalidated.

---

### 7. Introduction Engine

`lib/introduction.ts` is a pure-function module (no React, no Zustand) implementing the
22-phase intensive introduction cadence from BRAND.md.

10 exports — 4 constants: `GRADUATION_THRESHOLD`, `CONSECUTIVE_WRONG_RESET`, `MAX_PHASE_DAY`,
`MAX_APPEARANCES_BY_PHASE_DAY` (frozen); 6 functions: `getDayOfPhase`, `maxAppearancesToday`,
`shouldAppearToday`, `shouldGraduate`, `recordResult`, `getNextCardType`.

Integrates with `store/srsStore.ts` via four actions: `introduceCard`,
`recordIntroductionResult`, `getIntroductionDueCardIds`, `canIntroduceNewCard`.
These actions read/write `state.introductions: Record<string, IntroductionRecord>`.

**Rule:** `lib/introduction.ts` must remain pure — no React, no Zustand imports.
The store imports from lib/, not vice versa.

Session-start activation: on mount, `hooks/useStudySession.ts` calls `canIntroduceNewCard` and, if true, introduces the first qualifying card and appends it to the queue. This means every study session begins with a new card when the daily cap permits (live since Task #085, 2026-06-29).

**Reset and pause mechanism (Tasks #178, #228, #246, #254, #258):** `phaseStartDate` — not `dayOfPhase` — is the authoritative reset anchor. `dayOfPhase` is always recomputed by callers via `getDayOfPhase(record.phaseStartDate, today)`; a persisted `dayOfPhase` value is never trusted directly. On a triple-wrong streak, `recordResult` advances `phaseStartDate` to today (restarting Day 1 intensity) and sets `strandedAcrossDays: true` on the record. `canIntroduceNewCard` blocks all new-card introductions while ANY record has `strandedAcrossDays: true` — the flag is cleared only by a subsequent correct answer on that record (a wrong answer never clears it). When `getDayOfPhase` throws due to a corrupt `phaseStartDate`, `recordIntroductionResult`'s catch path fully repairs the record on a correct answer: `phaseStartDate` is reset to `today` (allowing the card to rejoin `getIntroductionDueCardIds` at Day-1 intensity) and `strandedAcrossDays` is cleared if set. A wrong answer does not repair either field. A day-22+ rescue path in `getIntroductionDueCardIds` (`store/srsStore.ts`) surfaces a non-graduated card once per day even past the 22-day table, so a non-graduated card can never permanently disappear from the due queue.

Key invariants:
- One new card per day maximum by default (`canIntroduceNewCard(today, maxPerDay = 1)` enforces the cap). Task #533: callers may pass a higher `maxPerDay` to flex past this default — used exclusively by the proactive-interrupt content-supply floor (`hooks/useInterruptConfig.ts`'s `computeDue`, `hooks/useStudySession.ts`'s `isInterrupt`-scoped mount-effect fallback) when the day's normal supply would otherwise leave an interrupt with nothing to show. Every other caller (manual unit/global sessions) still gets the plain default and is unaffected.
- Graduation requires 15 consecutive correct retrievals (not time-based)
- Wrong 3× in a row advances `phaseStartDate` to today (Day 1 intensity restarts) and sets `strandedAcrossDays: true`, pausing new-card introductions until a correct answer clears it
- `recordResult` is immutable — always returns a new object, never mutates input
- `getDayOfPhase` throws `[ERR-INTRO-DATE]` on a calendar-invalid `phaseStartDate` or `today` rather than silently propagating `NaN`; callers (`store/srsStore.ts`) catch this per-record so one corrupted record cannot abort computation for every other card; a correct answer in `recordIntroductionResult`'s catch path also repairs the corrupt `phaseStartDate` to `today`, allowing the card to rejoin the due queue (Task #258)

---

### 8. E2E Testing

Playwright smoke tests live in `tests/e2e/`. The config is at `playwright.config.ts` (repo root). E2E tests run on port **3099** to avoid colliding with the default dev server on 3000. Run with:

```bash
npm run test:e2e
```

Unit and integration tests (Vitest) remain in `tests/` alongside source files and run with `npm test`. E2E tests are a separate suite and are not counted in coverage thresholds. Both suites must pass before any batch is considered done.
