# Audit Checklist — plyglt
Generated: 2026-08-06 by /meet
Stack: Next.js 16 (React 19) + Tauri 2 (Rust) + Zustand 5 / Desktop (macOS/Windows/Linux) + Web

## Trust Boundary Checks
[ ] `lib/tauri.ts:invoke` — sole Tauri IPC gateway per CLAUDE.md §2 ("never import `@tauri-apps/api` directly outside `lib/tauri.ts`"); verify no file in `components/`, `hooks/`, `app/`, or `store/` imports `@tauri-apps/api/*` directly
[ ] `lib/storage.ts:createPlatformStorage` — sole hydration entry point for the three persisted Zustand stores (`srsStore`, `settingsStore`, `entitlementStore`); verify no file outside `lib/storage.ts` calls `localStorage` directly (CLAUDE.md §3 rule)
[ ] `lib/basePackLoader.ts` — network fetch entry for base-pack JSON + manifest; verify it is imported only by `lib/packLoader.ts` (real caller) and `lib/packResolver.ts` (type-only `LoadPackOptions` import), per the mechanical boundary test in `tests/packLoader.test.ts` (Task #428)
[ ] `scripts/manifestLock.ts:withManifestLock` — cross-process mutex guarding `public/packs/manifest.json`'s read-modify-write; verify every `scripts/exportPack.ts` invocation (both `it` and `es`) wraps its manifest write in `withManifestLock`, not just the language currently being exported

## Parse Boundary Checks
[ ] `lib/packTypes.ts:48` `LoadPackResult` + its shape-check comment — verify the shape-check function actually validates `card.prerequisites` is an array when present (not just truthy-with-`.length`), since `lib/srs.ts:207`'s `card.prerequisites.every(...)` has no `Array.isArray` guard of its own per that file's own header comment
[ ] `lib/importBackup.ts:parseBackup` — verify numeric-string `_version` handling (`tests/importBackup.test.ts`'s #481/#487 tests) still deep-equates the full parsed result against its numeric-version equivalent field-for-field, not just at the boundary-equal case
[ ] `lib/basePackLoader.ts` `readCacheMeta`/`readCacheData` — `JSON.parse` of platform-storage cache entries; verify a corrupted (non-JSON or wrong-shape) cached blob sets `cacheValid = false` and triggers re-fetch rather than throwing a `TypeError` that aborts the caller
[ ] `store/migrations.ts` — verify every `migrate*Store` function throws (not silently returns the unmigrated shape) when it hits a stored version number with no matching entry in its `*_MIGRATIONS` record, per the file's own "never remove a migration entry — throwing on a missing step is intentional" rule

## Invariant Verification
[ ] `lib/introduction.ts:getDayOfPhase` — throws `[ERR-INTRO-DATE]` on a calendar-invalid `phaseStartDate`/`today`; verify `store/srsStore.ts` catches this per-record (not corpus-wide) so one corrupted `IntroductionRecord` cannot abort due-card computation for every other card
[ ] `lib/generationGuard.ts:createGenerationGuard` — snapshot/isStale/bump primitive; verify every caller (`lib/basePackLoader.ts`'s per-language `evictionGuards` Map, `lib/specialtyPackLoader.ts`'s deactivation guard, `store/entitlementAddOns.ts`'s deactivation guard) calls `bump()` BEFORE clearing the state the in-flight work would otherwise resurrect, not after
[ ] `scripts/lintCardQuality.ts:492-497` (`--update-baseline`) — rewrites the baseline file unconditionally from `buildBaseline(pack)`'s current violation state, with no check that the new baseline is a subset of (or smaller than) the old one; verify nothing in the script itself enforces "baseline only shrinks" (CURRICULUM.md's own ratchet convention) — that invariant is entirely a human/process discipline, not a coded guard
[ ] `lib/srs.ts:prerequisitesMet` / `selectQualifyingNewCard` — the single source of truth CLAUDE.md names for prerequisite gating; verify `store/srsStore.ts`'s `getNewCards` and `hooks/useStudySession.ts`'s introduce-on-mount effect both call these exact functions rather than reimplementing the check locally

## Type Discriminant Exhaustiveness
[ ] `lib/packTypes.ts:48` `LoadPackResult` — 6-variant discriminated union (`invalid_lang`/`base_pack_not_loaded`/`download_failed`/`checksum_mismatch`/`parse_error`/success); verify every call site of `loadPack`/`loadSpecialtyPack` checks `result.ok` before touching `result.pack`
[ ] `store/entitlementAddOns.ts:60` `PurchaseAddOnResult` — verify `store/entitlementStore.ts`'s re-exported `ERR_ADDON_*` constants stay in sync with every literal error branch `PurchaseAddOnResult` can actually return (no branch added to one file without the other)
[ ] `content/types.ts:8` `CardType` — 5 literals (`recognize`/`produce`/`conjugate`/`fill_blank`/`passage_cloze`); verify `components/StudyCard.tsx` and `lib/introduction.ts:getNextCardType` both handle all 5 explicitly rather than falling through a default case that would silently mishandle a 6th type
[ ] `lib/licenseTypes.ts` `LicenseType` — verify `lib/featureFlags.ts:isProEnabled`'s `licenseType === "subscription"` comparison and `store/migrations.ts`'s entitlement migration agree on the full literal set (no historical stored value silently maps to neither branch)

## Silent Failure Audit
[ ] 45 non-test `catch` blocks found across `lib/`, `store/`, `hooks/`, `components/`, `app/` — spot-verify each follows the `[ERR-{CODE}-${Date.now()}]` ref-ID convention demonstrated at `app/study/page.tsx:75,125-126`, `lib/tauriInterrupt.ts:55,67,79,91`, `hooks/useLicenseActivation.ts:60,80,105,109`; flag any catch block in that set that swallows silently or omits the ref-ID
[ ] `app/settings/page.tsx:52` `handleLaunchAtLogin` catch — logs `[ERR-AUTOSTART-...]` and rolls back `setLaunchAtLogin(!v)`; verify the rollback fires on every rejection path (both `enableAutostart` and `disableAutostart`), not just whichever branch a given test exercises
[ ] `hooks/useLicenseActivation.ts:105` nested `evictErr` catch — verify a failure evicting stale entitlement state during a failed activation cannot leave the store reporting `isPackUnlocked === true` for a license that was never actually validated
[ ] `lib/packManifest.ts:99-100` `inFlightManifest` module-level promise dedup — verify the `release()` cleanup (`if (inFlightManifest === request) inFlightManifest = null`) runs on both the success and rejection path, so a failed manifest fetch cannot permanently wedge future `loadPack` calls behind a dead promise

## Test Assertion Quality
[ ] AGENTS.md's own banned-assertion grep gate (`.toBeDefined()`/`.toBeTruthy()`/`.not.toBeNull()`/`.toBeGreaterThan(0)` without `// existence-check:`) — confirmed clean repo-wide via direct grep at detection time; verify it stays clean and that any `// existence-check:` comment names a genuinely non-deterministic value (ID/timestamp), not a vague justification
[ ] `tests/importBackup.test.ts:251-265` "defaults dueDate to approximately now" — uses `before`/`after` `Date.now()` bounds plus an inline existence-check comment, the correct pattern for a non-deterministic timestamp; verify no other `Date.now()`-referencing test among the 14 files that use it (e.g. `tests/srsStore.test.ts`, `tests/srs.test.ts`) instead compares two independently-taken `Date.now()` values with `.toBe`/`.toEqual`, which would be flaky under real clock drift
[ ] `tests/importBackup.test.ts` #481/#487 tests (numeric-string `_version` acceptance) — deep-equate the full `parseBackup` result object against its numeric-version equivalent rather than checking only `.ok`; verify this same full-object-diff pattern is followed in the sibling boundary tests immediately below (0/negative/fractional `_version`), not a narrower field check

## Authorization and Data Isolation
Single local user, no server, honor-system entitlement (CLAUDE.md §5) is explicit, owner-confirmed by-design — not itself a finding. What's checkable is whether the client-side gate is applied consistently at every content-serving call site.
[ ] `store/entitlementStore.ts:isPackUnlocked` / `hasAddOn` — verify every call path reaching `lib/packLoader.ts`'s `loadPack`/`loadSpecialtyPack` for a non-free language is preceded by one of these two gates; cite `hooks/useLangPack.ts:159` where `hasAddOn` gates specialty-pack loading directly inside the hook, not only at the `components/LanguageGrid.tsx` UI layer
[ ] `lib/featureFlags.ts:isProEnabled` — verify this is the only combinator used for Pro-gated feature checks; grep for any direct `licenseType === "subscription"` comparison outside this function that could drift from its expiry-aware logic (the same `SUBSCRIPTION_GRACE_PERIOD_MS` window `isPackUnlocked` applies)
[ ] `lib/specialtyPackLoader.ts` — verify `loadSpecialtyPack` itself does not independently re-check entitlement (that responsibility belongs to `hooks/useLangPack.ts`) but DOES still reject a request whose base pack was never loaded (`base_pack_not_loaded`), confirming the layering CLAUDE.md §6 documents hasn't drifted

## Concurrency Safety
[ ] `lib/packLoader.ts:73` `inFlightBaseLoads` Map + `lib/specialtyPackLoader.ts:51` `inFlight` Map — verify both in-flight dedup maps are cleared via their `.then(cb, cb)`-style cleanup regardless of the eviction-generation guard's outcome, so a stale-but-still-pending entry cannot wedge a future load for the same language code
[ ] `lib/generationGuard.ts` used per-language via `lib/basePackLoader.ts:87`'s `evictionGuards` Map — verify a concurrent `evictPack()` for language A cannot bump language B's guard (correct per-language keying, no cross-language leakage)
[ ] `scripts/manifestLock.ts:withManifestLock` — `mkdirSync`-based mutex with a 30s stale-lock reclaim (`STALE_LOCK_MS`); verify that window stays comfortably above real export runtime as CI machines slow down or content grows, and that `reclaimIfStale`'s swallow-and-retry catch doesn't mask a genuine permissions error as "lock legitimately vanished"

## Idempotency Verification
[ ] `store/entitlementAddOns.ts:196-198` `purchaseAddOn` — `s.purchasedAddOns.includes(code) ? s.purchasedAddOns : [...s.purchasedAddOns, code]` idempotent-append; verify a test calls `purchaseAddOn` twice with the same code and asserts the array length is unchanged on the second call
[ ] The cross-thread 30s/5s interrupt-poll TOCTOU logged in `.autocode/agents/cto.md` (Batch 19, F3, severity 5, accepted as debt — `src-tauri/src/interrupt.rs`/`os_events.rs`) — verify it remains accepted debt at its documented severity, not silently promoted into a live double-fire bug by a subsequent unrelated change to the poll intervals
[ ] `lib/basePackLoader.ts`/`lib/packManifest.ts` retry paths — verify a retried pack download after a failed sha256 check does not leave a partial write in platform storage that a subsequent successful download merges with rather than fully overwrites

## Production Diagnosability
[ ] Spot-check the 45 catch blocks found across `lib/`, `store/`, `hooks/`, `components/`, `app/` for the `[ERR-{CODE}-${Date.now()}]` convention — cite compliant examples (`app/study/page.tsx:75` `[ERR-IPC-EXIT-...]`, `lib/tauriInterrupt.ts:55/67/79/91`) as the bar; flag any bare `console.error(err)` call with no ref-ID prefix
[ ] `scripts/manifestLock.ts:releaseLock` — its `[ERR-MANIFEST-LOCK-RELEASE-${Date.now()}]` log explicitly notes the failure does NOT mean the underlying manifest write failed; verify this cleanup-failure-vs-real-failure distinction survives any future refactor, since conflating them would send a false alarm into CI logs
[ ] `hooks/useLicenseActivation.ts`'s four catch blocks (lines 60, 80, 105, 109) — verify none logs the raw license key or receipt token (PII/secret risk) while still including enough context (e.g. key length/prefix) to distinguish a network failure from a malformed-input failure in the field

## Degradation Under Load
[ ] `app/learn/page.tsx:48-49` `byLevel = LEVELS.reduce(...)` calling `ALL_UNITS.filter(u => u.level === lvl)` on every render of the Home component, over all 126 units / 30,609 cards, with no `useMemo` — contrast with `app/study/page.tsx:35`'s equivalent `ALL_UNITS.flatMap((u) => u.cards)`, which IS wrapped in `useMemo`; verify whether the unmemoized version is a real hot path worth fixing or negligible at this dataset size
[ ] `app/study/page.tsx:103` `ALL_UNITS.flatMap((u) => u.cards)` — a second, separate flatten of the same ~30K-card set outside the `useMemo` boundary at line 35; verify it isn't re-run on every render in the global/interrupt session path
[ ] `lib/packCache.ts:99` in-memory `Map<string, Pack>` — grows unbounded across loaded languages (currently 2: `it`, `es`); verify this stays acceptable as more languages register, and that any test-only cache-clear path fully resets it between test files to avoid cross-test pollution rather than masking a genuine unbounded-growth concern

## STRUCTURAL LAYER END

## TEAM_SPECIFIC LAYER
[PRESERVED — populated automatically by /advance after each wave. Do not edit manually. /meet will preserve this section across regenerations.]
[ ] requirements auto-detected from Wave 4 (4x, max severity 9): a fix that looks wired and passes its own unit test can still be dead code in production if the test drives state directly (setState/injected fixtures) instead of the real end-to-end call path — verify every "requirements implemented" claim against a seam test, not a unit test — added: 2026-07-07
[ ] code-quality auto-detected from Wave 4 (9x, max severity 8): check for algorithms that claim to "vary" or "rotate" a value but structurally can only reach a subset of their declared range (e.g. filtering out only the single most-recent value from a fixed-order pool) — added: 2026-07-07
[ ] tests auto-detected from Wave 4 (7x, max severity 6): a test named after an "atomicity" or "single-operation" contract must assert the operation COUNT (e.g. via subscribe + snapshot-count), not just final-state values — final-state-only assertions pass identically under a non-atomic multi-call implementation — added: 2026-07-07
[ ] data-loss auto-detected from Wave 4 (2x, max severity 6): date-validation guards using only isNaN(new Date(str).getTime()) miss day-of-month rollover (e.g. "2026-02-30" silently normalizes to a valid date) — require a round-trip re-format-and-compare check, not isNaN alone — added: 2026-07-07
[ ] security auto-detected from Wave 4 (2x, max severity 5): Object.freeze() on a map/registry only protects the outer object — verify nested config objects are also frozen (or explicitly documented as an accepted trade-off) wherever a Readonly<> type implies full immutability — added: 2026-07-07
[ ] documentation-trust auto-detected from Wave 4 (3x, max severity 2): file-header "USED BY"/dependency comments and deleted TODO markers drift from reality as code changes — verify header claims against an actual grep of callers before trusting them — added: 2026-07-07
[ ] async auto-detected from Wave 14 (6x, max severity 7): no eviction-generation guard on a shared in-flight base-pack load — evictPack racing a concurrent fresh load can resurrect the evicted pack in memCache and storage; check every cache-eviction path has a generation/invalidation guard symmetric with its sibling loaders — added: 2026-07-17
[ ] error-handling auto-detected from Wave 14 (3x, max severity 5): a sha256/crypto verification call left unguarded outside try/catch rejects the shared in-flight promise for every concurrent requester instead of returning the typed error contract every sibling branch honors — added: 2026-07-17
[ ] edge-case auto-detected from Wave 14 (3x, max severity 6): a "ready" filter applied to one half of a base/specialty pair (e.g. specialty codes) but not the other — a persisted unready code on the unfiltered half strands the user with no self-heal path — added: 2026-07-17
