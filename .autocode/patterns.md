# AutoCode Patterns Log

## 2026-07-03 | Task: Batch 1 re-audit
- tests [Test name 'unrecognised all-languages variant → subscription licenseType, all packs' is a factual lie. Input 'All Languages Extended'.toLowerCase().includes('all languages') = true — the RECOGNISED branch executes, returning ALL_PACK_CODES. The enclosing describe block comment says 'free pack access only — the conservative fallback for any unknown variant', directly contradicting the assertion. No test covers a truly unrecognised variant that resembles 'all languages'.] -- severity 7 | tests/entitlement.test.ts:resolveVariantEntitlement:602 | NEW
- error-handling [Guard 'if (!res.instance)' is falsy only for null/undefined. An API response with instance: { id: '' } passes the guard and flows to ok:true with instanceId:''. Every subsequent validateLicense/deactivateLicense sends empty instanceId to the Lemon Squeezy API, producing errors surfaced as generic network failures. Fix: change guard to 'if (!res.instance?.id)'.] -- severity 7 | lib/entitlement.ts:activateLicense:139 | NEW
- tests [Input expires_at: '2027-01-01T00:00:00Z' makes validUntil deterministic (1798761600000). Assertions 'expect(result.validUntil).toEqual(expect.any(Number))' and 'toBeGreaterThan(Date.now())' are banned pseudocode forms per AGENTS.md — a regression returning Date.now()+1 passes both. Fix: 'expect(result.validUntil).toBe(new Date("2027-01-01T00:00:00Z").getTime())'.] -- severity 6 | tests/entitlement.test.ts:activateLicense — S001 ok:true path:344 | NEW
- tests [Same deterministic-value/pseudocode pattern as the activateLicense test, in the validateLicense ok:true block. Three assertions: expect.any(Number) (banned), .not.toBeNull() (banned, caught by AGENTS.md grep gate), and >Date.now().toBe(true) (proves sign only). Fix: assert the exact timestamp from parseExpiry on the fixed input string.] -- severity 6 | tests/entitlement.test.ts:validateLicense — ok:true with expiresAt path:452 | NEW
- data-loss [DATE_RE = /^\d{4}-\d{2}-\d{2}$/ validates syntactic form only; accepts calendar-invalid '2026-13-45'. A corrupt stored record with syntactically-structured but calendar-invalid dates passes the regex, is preserved as phaseStartDate, then causes new Date('2026-13-45').getTime() to return NaN in getDayOfPhase, silently hiding the card forever. Fix: add && !isNaN(new Date(value).getTime()) after each DATE_RE.test() call.] -- severity 5 | store/migrations.ts:SRS_MIGRATIONS[3]:55 | NEW
- data-loss [The migration for-loop casts introductions as Record<string, unknown>. If a stored value is null, accessing record.phaseStartDate throws TypeError. Zustand's persist middleware catches unhandled migration errors and falls back to empty default state, silently discarding all SRS card history. No null guard on record itself exists before property access. No test covers this input.] -- severity 5 | store/migrations.ts:SRS_MIGRATIONS[3]:58 | NEW
- tests [Migration spread '{ ...record, phaseStartDate }' outputs 11 fields; test asserts only 2 (phaseStartDate and introducedDate). The 8 unasserted fields include dayOfPhase, consecutiveCorrect, and graduated — whose silent loss would cause the worst behavioral regressions. Changing the spread to '{ cardId: record.cardId, phaseStartDate }' drops all non-key fields; this test still passes. Rule 16 violation: 11 output fields, 2 asserted.] -- severity 5 | tests/migrations.test.ts:v2 → v3: adds phaseStartDate:116 | NEW
- tests [Two failures: (1) migration emits console.error when both phaseStartDate and introducedDate are absent, but no vi.spyOn assertion verifies the log fires — the call is deletable without any test failing. (2) Three assertions on localDateStr() output (genuinely non-deterministic) have zero '// existence-check:' comments as required by AGENTS.md.] -- severity 5 | tests/migrations.test.ts:v2 → v3: record missing introducedDate gets phaseStartDate from today:179 | NEW
- security [purchaseAddOn(code) appends code to purchasedAddOns with no validation against isSpecialtyPackCode() or SPECIALTY_PACKS. lib/langRegistry.ts exports isSpecialtyPackCode(s) for exactly this purpose. Dormant while SPECIALTY_PACKS is frozen([]), but gap must be closed before specialty packs ship to prevent race conditions or typos storing non-existent codes that hasAddOn() would then confirm as purchased.] -- severity 5 | store/entitlementStore.ts:purchaseAddOn:137 | NEW
- tests [No test exercises activateLicense when the LS response contains instance: { id: '' }. The guard 'if (!res.instance)' does not catch this case. The test suite covers instance: null (correctly returning ok:false) but the empty-string bypass is entirely undetected. Fix: mock response with instance: { id: '' } and assert ok:false with error: ERR_ACTIVATE_NO_INSTANCE.] -- severity 5 | tests/entitlement.test.ts:activateLicense — null safety:313 | NEW
- tests [resolveVariantEntitlement returns 3 fields. The Annual test passes expiresAt: '2027-01-01T00:00:00.000Z', making validUntil deterministic (1798761600000), but does not assert it. The Monthly test correctly asserts validUntil. An Annual-specific regression in expiry parsing would pass this test. Rule 16: 3-field output, 2 fields asserted with a deterministic third.] -- severity 4 | tests/entitlement.test.ts:resolveVariantEntitlement — Annual:617 | NEW
- code-quality [LANG_CONFIG_MAP is created via Object.fromEntries() but not wrapped in Object.freeze(). The existing comment on the frozen arrays explicitly states 'A mutable export could be .push()'d to bypass guards — hence freeze.' The same logic applies to LANG_CONFIG_MAP: any importer could write LANG_CONFIG_MAP['it'] = maliciousConfig without a TypeError. Asymmetry with sibling frozen exports is unexplained.] -- severity 4 | lib/langRegistry.ts:LANG_CONFIG_MAP:48 | NEW
- code-quality [MAX_APPEARANCES_BY_PHASE_DAY is a plain mutable exported object. Any importer can write MAX_APPEARANCES_BY_PHASE_DAY[1] = 0 to suppress day-1 flooding or corrupt any phase cap without a TypeError. The introduction engine's central scheduling table is exposed as a mutable export. Fix: Object.freeze().] -- severity 4 | lib/introduction.ts:MAX_APPEARANCES_BY_PHASE_DAY:9 | NEW
- security [VARIANT_ALL_LANGUAGES = 'all languages'. Matching via n.includes(VARIANT_ALL_LANGUAGES) grants ALL_PACK_CODES to any variant name containing that substring, not just the specific Lemon Squeezy SKU. 'All Languages Extended', 'Extended All Languages Bundle', and 'Omnilinguistic All Languages Premium' would all receive ALL_PACK_CODES. F001 demonstrates the risk is already materialised in tests labelled 'unrecognised'.] -- severity 4 | lib/entitlement.ts:resolveVariantEntitlement:106 | NEW

## 2026-07-03 | Task: Batch 1 — Security + Correctness Foundation
- tests [Happy-path "accepts a well-formed current backup" asserts validCardCount, skippedCardCount, srs.streak, and srs.lastStudiedDate but nothing from result.entitlement. A parseBackup that corrupted entitlement.licenseType or entitlement.unlockedPacks passes the primary seam test. The ok:true result shape has 6 fields; only 2 are verified here. Rule 16 violation.] — severity 4 | tests/importBackup.test.ts:parseBackup:25 | NEW
- error-handling [!data._version at line 65 is truthy for any non-number truthy value (string "999", {}, true) as well as for numeric 0. When the truthiness check passes for a non-number value, line 68 typeof data._version === "number" is false and the newer-version comparison is skipped entirely. A backup from a future app version that uses a string _version field is silently processed by old parsing logic rather than returning the version-mismatch error to the user.] — severity 4 | lib/importBackup.ts:parseBackup:65 | NEW
- security [LANG_CONFIG_MAP is not wrapped in Object.freeze(). The comment at line 42 documents that ALL_PACK_CODES, READY_PACK_CODES, and FREE_PACK_CODES are frozen because they back the security allowlist. LANG_CONFIG_MAP is exported via Object.fromEntries() at line 48 without Object.freeze(). An in-process caller mutates LANG_CONFIG_MAP["it"] = bogusConfig silently, inconsistent with all three sibling array exports in the same file.] — severity 4 | lib/langRegistry.ts:LANG_CONFIG_MAP:48 | NEW
- tests [No test covers activateLicense when the LS response contains instance:{ id: "" }. The test suite covers instance:null (returns ERR_ACTIVATE_NO_INSTANCE) but not instance:{ id: "" } (which returns ok:true with instanceId:"", the silent bypass described in F011). The entire F011 code path is fully untested.] — severity 4 | tests/entitlement.test.ts:activateLicense:1 | NEW

## 2026-07-02 | Task: #178 — phaseStartDate + triple-wrong reset fix
- migrations store/migrations.ts:57 empty-string fallback in schema migration — field missing in stored record defaults to "" which propagates NaN through date arithmetic, silently disabling affected cards; always use a semantically valid sentinel (today's date, or throw) — never "" — severity 7 | AUDIT
- tests tests/introduction.test.ts:241-247 cross-day state carryover not tested — counters that reset on day boundaries (consecutiveWrongToday) need explicit tests spanning a day change; same-day tests cannot catch cross-day state carryover bugs — severity 8 | AUDIT
- tests tests/introduction.test.ts:241-247 shallow field assertions on complex records — 2 of 12 fields asserted on a record-returning function; always use toMatchObject with all relevant fields or enumerate and assert every field — severity 5 | AUDIT
- architecture content/types.ts:17 stale-by-design persisted field — persisting a derived field (dayOfPhase) alongside the authoritative source (phaseStartDate) creates a dual-truth record with no type enforcement; prefer not persisting the derived field — severity 5 | AUDIT
- tests tests/srsStore.test.ts:393 new schema fields not tracked in adjacent tests — when a field is added to a struct, every test that constructs or asserts on that struct must be audited; introduceCard test missed phaseStartDate — severity 4 | AUDIT

## 2026-07-02 | Task: Batch 5 — lib/introduction.ts (standalone audit)
- requirements lib/introduction.ts:recordResult:101 persisted field written by pure function immediately overridden by store caller that always recomputes from source field — pure function's intent never honored; fix requires separating source field from derived field — severity 9 | AUDIT
- requirements store/srsStore.ts:canIntroduceNewCard BRAND.md spec requirement absent from implementation with no TODO, no task reference, no test acknowledging the gap — silent scope omission — severity 7 | AUDIT
- data-loss lib/introduction.ts:getDayOfPhase:44 / shouldAppearToday card state machine has no recovery path for stuck state at introduction/FSRS boundary — cards beyond phase table ceiling with graduated=false become permanently invisible — severity 7 | AUDIT
- code-quality lib/introduction.ts:66,94,99 magic numeric thresholds duplicated across two functions without named constants — graduation threshold (15) and reset trigger (3) — severity 6 | AUDIT
- security lib/introduction.ts:9 exported mutable scheduling table without Object.freeze() — global scheduling table corruptible by same-process importers — severity 6 | AUDIT
- data-loss store/srsStore.ts:introduceCard:211 store guard uses wrong polarity on existing-record check — graduated card re-introduction silently overwrites all historical progress — severity 6 | AUDIT
- requirements lib/introduction.ts:getNextCardType:116 pure function fully implemented and correct but never wired to production callers — BRAND.md variety rule has zero runtime enforcement — severity 5 | AUDIT
- edge-case lib/introduction.ts:recordResult:98 temporal-semantics field (consecutiveWrongToday) lacks calendar-day reset logic present on sibling field (appearancesToday) in same function — daily invariant silently broken — severity 5 | AUDIT
- edge-case lib/introduction.ts:getDayOfPhase:42 date string parameters accepted without NaN guards — malformed input propagates through arithmetic silently, causing card disappearance — severity 5 | AUDIT
- tests tests/introduction.test.ts no seam tests tracing multi-function introduction workflow — unit isolation hides cross-function behavioral bugs that only manifest in composed call chain — severity 4 | AUDIT

## 2026-06-28 | Task: #053 — WorldClass cycle 2
- tests tests/entitlement.test.ts validateLicense license-key-absent path asserted ok:false only — ERR_LICENSE_NOT_ACTIVE path unpinned; pattern: every error-returning branch of a function covered by a Task-N constant extraction must have a .toBe(ERR_*) assertion, not just an ok:false check — severity 5 | WORLDCLASS

## 2026-06-26 | Task: #060 /audit — 7-agent parallel review
- type-safety lib/packLoader.ts dead discriminant "not_cached" — declared in LoadPackResult union with zero return sites; pattern: every union variant must have at least one reachable return site; after changing a guard's return value, audit the full type and remove orphaned members — severity 6 | AUDIT
- tests tests/useLangPack.test.ts Rule 13 seam test missing — task adds discriminant crossing module boundary (packLoader→useLangPack→page) but zero behavioral tests trace data through the seam; pattern: when a new error discriminant is introduced, the test file for every intermediate consumer must exercise the new code path end-to-end — severity 6 | AUDIT
- security lib/packLoader.ts integrity bypass — clearPackCache evicts SHA-failed data from storage but local variable still holds those bytes; offline fallback subsequently serves integrity-failed pack as ok:true; pattern: after evicting data for integrity failure, null the local capture variable to prevent stale-fallback from resurrecting it — severity 7 | AUDIT
- error-handling lib/packLoader.ts structural validation gap — four JSON.parse(cachedData) as Pack cache-serve paths skip Array.isArray(pack.units) guard present only on fresh downloads; pattern: structural validation must be applied on ALL parse paths, not just the primary download path — severity 5 | AUDIT
- type-safety hooks/useLangPack.ts discriminant erased at hook boundary — LoadPackResult error union widened to string|null in LangPackState; pattern: when a discriminated union crosses a module boundary, the narrowed type must follow; derive PackLoadError from the source union rather than widening to string — severity 4 | AUDIT
- error-handling app/study/page.tsx,components/InterruptHandler.tsx silent error ignored — two useLangPack consumers never destructure error; pack load failure renders empty state with no user feedback; pattern: every consumer of a hook with an error field must handle the non-null case explicitly — severity 5 | AUDIT

## 2026-06-26 | Task: #003 standalone re-audit — Lens 1
- security lib/langRegistry.ts ALL_PACK_CODES mutable string[] — sole security allowlist for loadPack/evictPack is exported without Object.freeze() or readonly; pattern: security boundaries derived from LANGUAGE_REGISTRY must be immutable — severity 7 | AUDIT
- type-safety lib/langRegistry.ts PackCode resolves to string — `as string[]` cast on the source array erases literal types; PackCode type provides zero narrowing; pattern: don't cast away tuple/const types when downstream types depend on them for safety — severity 6 | AUDIT
- error-handling lib/packLoader.ts 7 silent catch {} — module-scale Rule 8 violation; pattern: when adding a security fix to a file with pre-existing catch {} violations, the entire file's error handling must be hardened in the same cycle — severity 6 | AUDIT
- data-corruption lib/langRegistry.ts placeholder configs — stub entries sharing another language's LanguageConfig corrupt all consumers of LANG_CONFIG_MAP; pattern: placeholder registry entries must either omit from exports or use a safe NullConfig not a live language's config — severity 8 | AUDIT

## 2026-06-25 | Task: #001 — WorldClass cycle 9 (orchestrated, WC_CYCLE 2 internal cycles 6-7)
- code-quality store/entitlementStore.ts "entitlement-v1" string literal duplicated on lines 127 and 130 without a named constant; Zustand persist name key and platform storage key can diverge silently — severity 6 | worldclass: -4 pts (arch) / -5 pts (vibes) | WORLDCLASS
- tests tests/entitlement.test.ts two deactivateLicense ok:true tests with identical mock payloads, identical assertions, only description differs — dead weight, no additional coverage — severity 4 | worldclass: -3 pts (arch) / -4 pts (vibes) | WORLDCLASS

## 2026-06-25 | Task: #001 — WorldClass cycle 8 (orchestrated, WC_CYCLE 2 internal cycle 1 — pre-build reference)
- code-quality lib/langRegistry.ts Rule 2 header missing; file was modified (added PackCode type) without adding the required header block — severity 5 | worldclass: -3 pts | WORLDCLASS
- tests tests/entitlement.test.ts Rule 13 seam test missing: validateLicense→markValidated→isPackUnlocked chain untested; activation and deactivation seams exist but validation renewal seam does not — severity 4 | worldclass: -3 pts | WORLDCLASS
- tests tests/entitlement.test.ts validateLicense struct guard (valid:true with non-active status) path untested — severity 4 | worldclass: -2 pts | WORLDCLASS

## 2026-06-25 | Task: #001 — WorldClass cycle 7 (orchestrated, WC_CYCLE 1 internal cycle 2)
- code-quality lib/entitlement.ts pervasive single-line catch/if blocks make Error Reference System hard to audit; contradicts Slow=Deliberate — severity 5 | worldclass: -4 pts | WORLDCLASS
- code-quality store/entitlementStore.ts PackCode[] upgrade stopped at state field; setEntitlement parameter, ActivateResult, resolveVariantEntitlement return type all still string[] — severity 4 | worldclass: -3 pts | WORLDCLASS
- feature-flag components/EntitlementValidator.tsx no ENTITLEMENT_VALIDATION_ENABLED constant; Rule 4 violation — severity 4 | worldclass: -2 pts | WORLDCLASS

## 2026-06-25 | Task: #001 — WorldClass cycle 4 (fresh invocation, cycle 1)
- code-quality store/migrations.ts DEPENDS ON header names @/store/entitlementStore but import is from @/lib/licenseTypes — severity 3 (worldclass: -2 pts)
- error-handling lib/entitlement.ts API contract violation paths (!raw, !instance, !meta) return without ref-ID log — severity 4 (worldclass: -2 pts)
- security lib/entitlement.ts invoke cast to LsActivateBody/LsValidateBody without runtime shape validation — severity 5 (worldclass: -2 pts)
- error-handling app/settings/page.tsx outer try/catch in handleActivate/handleValidate/handleDeactivate is dead — lib functions never throw — severity 5 (worldclass: -4 pts)
- security lib/entitlement.ts res.license_key.key not validated as non-empty string before being persisted — severity 5 (worldclass: -3 pts)
- auth lib/entitlement.ts deactivateLicense infers success from non-throw alone; response body ignored — severity 5 (worldclass: -2 pts)
- async components/EntitlementValidator.tsx runEntitlementValidation on ok:false does not update lastValidated; every mount re-triggers LS call — severity 4 (worldclass: -2 pts)

## 2026-06-25 | Task: #001 — WorldClass cycle 1
- tests lib/entitlement.ts+tests/entitlement.test.ts Rule 13 seam test missing: activateLicense→setEntitlement→isPackUnlocked chain not tested end-to-end — severity 6 (worldclass: -4 pts)
- code-quality lib/entitlement.ts duplicate date-parsing IIFE in parseVariant:79 and validateLicense:123; same anonymous IIFE copy-pasted instead of extracted helper — severity 4 (worldclass: -3 pts)
- tests components/EntitlementValidator.test.tsx hand-rolled runValidatorEffect() mirrors component logic but diverges silently; not testing the real component — severity 5 (worldclass: -5 pts)
- tests tests/entitlement.test.ts activateLicense ok:true test asserts validUntil is any Number but not > Date.now(); weaker than S002 which checks future date — severity 4 (worldclass: -3 pts)
- code-quality lib/entitlement.ts validateLicense reuses LsActivateBody (activation response type) as validation response type; lying type — severity 4 (worldclass: -4 pts)
- tests tests/migrations.test.ts no test for migration chain throw guard; migrateEntitlementStore({},3) when ENTITLEMENT_VERSION=2 is untested — severity 4 (worldclass: -3 pts)

## 2026-06-24 | Task: #001
- security store/migrations.ts ENTITLEMENT_MIGRATIONS[1]:55 persisted legacy licenseType bypasses expiry check via migration passthrough; unknown licenseType not sanitized to LicenseType union — severity 7 | store/migrations.ts:ENTITLEMENT_MIGRATIONS[1]:55 | NEW
- tests tests/migrations.test.ts:101 migration test asserts wrong behavior: old licenseType value expected to survive migration unchanged; blocks fix for migration passthrough — severity 6 | tests/migrations.test.ts:migrateEntitlementStore:101 | NEW
- code-quality app/settings/page.tsx:306 stale display label renders for non-subscription licenseKey holders; dead else branch in ternary not removed with type union change — severity 5 | app/settings/page.tsx:SettingsPage:306 | NEW
- security lib/entitlement.ts:52 parseVariant unlocksAll uses substring match on "all" and "bundle"; future variant names containing these substrings will unintentionally unlock all packs — severity 5 | lib/entitlement.ts:parseVariant:52 | NEW
- error-handling app/settings/page.tsx:178 bare catch without binding; JSON.parse and parseBackup errors all produce identical user message with no logging or ref ID; violates Rule 8 — severity 5 | app/settings/page.tsx:handleImportFile:178 | NEW
- code-quality app/page.tsx:181 BuyModal copy says "one purchase" — contradicts subscription-only pricing model per BRAND.md — severity 4 | app/page.tsx:BuyModal:181 | NEW
- error-handling app/settings/page.tsx:46 validateLicense promise has no .catch(); network errors produce unhandled rejection; violates Rule 8 — severity 4 | app/settings/page.tsx:useEffect:46 | NEW
- error-handling app/settings/page.tsx FileReader.onerror never assigned; file read failures produce no UI feedback and no error logged; violates Rule 8 — severity 4 | app/settings/page.tsx:handleImportFile:151 | NEW

## 2026-06-25 | Task: #001 — WorldClass cycle 3 (FINAL — MAX_CYCLES)
- code-quality app/settings/page.tsx 530 lines — 3.5× the ≤150-line route limit (Rule 1); pre-existing, tracked for Batch 3 refactor — severity 7 (worldclass: -12 pts)
- error-handling lib/entitlement.ts:activateLicense invoke call missing try/catch; deactivateLicense and validateLicense both have try/catch but activateLicense was missed; FIXED post-scoring — severity 7 (worldclass: -10 pts)
- code-quality app/settings/page.tsx useEffect duplicated runEntitlementValidation logic instead of calling it; omitted error logging on ok:false path; FIXED post-scoring — severity 5 (worldclass: -5 pts)
- code-quality lib/entitlement.ts parseVariant magic strings "monthly"/"annual"/"all languages" inline; should be named constants near CHECKOUT_URLS — severity 4 (worldclass: -4 pts)
- tests app/settings/page.tsx no co-located .test.tsx; 530-line component with hooks and conditional branches — severity 4 (worldclass: -4 pts)
- tests vitest.config.ts functions coverage 76.15% below AGENTS.md 80%+ floor; dragged by React hooks untestable in node environment — severity 4 (worldclass: -3 pts)

## 2026-06-24 | Task: #001 (Cycle 2)
- tests tests/migrations.test.ts storedVersion=1 v1-to-v2 migration path not tested; real-world upgrade path for existing users has zero coverage — severity 4 | tests/migrations.test.ts:migrateEntitlementStore:101 | NEW
- error-handling app/settings/page.tsx reader.onerror discards ProgressEvent; DOMException on target.error never read or logged — severity 4 | app/settings/page.tsx:handleImportFile:154 | NEW
- error-handling app/settings/page.tsx three console.error calls lack MODULE_CODE-TIMESTAMP ref IDs required by Rule 8 — severity 4 | app/settings/page.tsx:useEffect:49 | NEW
- code-quality store/migrations.ts inline VALID Set is third parallel LicenseType definition; untyped and frozen by migration immutability rule — severity 3 | store/migrations.ts:ENTITLEMENT_MIGRATIONS:67 | NEW
- code-quality store/migrations.ts migration v2 coerces any unknown licenseType to subscription; includes corrupted values; asymmetric with importBackup.ts — severity 3 | store/migrations.ts:ENTITLEMENT_MIGRATIONS:70 | NEW
- code-quality store/entitlementStore.ts persist key entitlement-v1 does not reflect ENTITLEMENT_VERSION=2; no comment explaining deliberate mismatch — severity 2 | store/entitlementStore.ts:persist:80 | NEW
- code-quality store/migrations.ts comment says one-time purchase — perpetuates deleted pricing concept in source — severity 2 | store/migrations.ts:ENTITLEMENT_MIGRATIONS:62 | NEW
- edge-case lib/entitlement.ts parseVariant includes annual matches semiannual/biannual; includes monthly matches bimonthly; no tests for these edge cases — severity 2 | lib/entitlement.ts:parseVariant:51 | NEW

## 2026-06-25 | Task: Task #001 — WorldClass cycle 6 (orchestrated, WC_CYCLE 1)
- tests lib/entitlement.ts:resolveVariantEntitlement console.warn on unknown variant untested; spy needed to assert observability event fires — severity 4 | worldclass: -2 pts | WORLDCLASS
- tests components/EntitlementValidator.tsx:runEntitlementValidation console.warn on ok:false untested; spy needed — severity 3 | worldclass: -1 pt | WORLDCLASS

## 2026-06-25 | Task: Task #001: Delete all lifetime entitlement code + harden entitlement module — WorldClass cycle 5 (orchestrated)
- code-quality app/settings/page.tsx 515 lines — 3.4× Route ≤150 limit (Rule 1), pre-existing tracked Task #026 — severity 6 | worldclass: -8 pts | WORLDCLASS
- tests lib/entitlement.ts:deactivateLicense null-invoke response path has no test — severity 4 | worldclass: -3 pts | WORLDCLASS
- tests deactivateLicense→clearEntitlement store seam untested — severity 4 | worldclass: -2 pts | WORLDCLASS
- tests components/EntitlementValidator.tsx:runEntitlementValidation co-located test not in scoring scope — severity 6 | worldclass: -4 pts | WORLDCLASS
- code-quality lib/entitlement.ts:parseVariant licenseType hardcoded "subscription" but described as "parses" — severity 3 | worldclass: -2 pts | WORLDCLASS
- code-quality store/entitlementStore.ts:VALIDATION_TTL_MS comment only documents grace-period use, not needsValidation use — severity 3 | worldclass: -2 pts | WORLDCLASS

## 2026-06-28 | Task: #053 /audit standalone — 5-agent review
- tests tests/entitlement.test.ts ERR_LICENSE_NOT_ACTIVE imported but unasserted — constant exported and imported in test file but all three pinning call sites tested with toContain("not active") substring; pattern: every constant extracted as a Poka-Yoke measure must have at least one toBe(CONSTANT) assertion in the test suite — severity 6 | AUDIT
- tests tests/entitlement.test.ts four pre-existing substring assertions covering same code paths as new pinning tests, left in place — old stringContaining tests not upgraded or removed after stronger toBe tests added; pattern: when adding an exact-value assertion for a code path, either upgrade or remove any pre-existing substring assertions for the same path — severity 5 | AUDIT
- done-condition tasks.md grep pattern unsatisfiable — done condition grep matched the constant definition lines themselves, not just inline call-site usage; pattern: grep-based done conditions for extracted constants must exclude the definition line (e.g. use `return.*"string"` not just `"string"`) — severity 3 | AUDIT

## 2026-07-04 | Task: Batch 14 Wave 1 (#163)
- correctness [updateInterruptConfig extended from 3 to 7 required params in lib/tauriInterrupt.ts. tests/tauri.test.ts (not in the builder's declared file scope) still called the old 3-arg signature at 3 call sites — a real tsc break that shipped past the builder's own "npm test" pass because vitest's default run does not type-check the whole program. Fix: added the 4 new args to all 3 call sites. Poka-yoke: any breaking signature change to an exported function must grep all call sites project-wide before closing, not just files in the task's declared File: scope.] -- severity 5 | tests/tauri.test.ts:81,92,98 | NEW

## 2026-07-04 | Task: #164
- functional-defect wake_enabled is written by update_interrupt_config but never read anywhere else in the crate — toggling "Remind on wake" has zero runtime effect — severity 9 | src-tauri/src/os_events.rs:start_os_listeners (wake-detection branch):172 | NEW
- functional-defect unlock_enabled is written but never read — toggling "Remind on unlock" has zero runtime effect — severity 9 | src-tauri/src/os_events.rs:start_os_listeners (unlock-detection branch):181 | NEW
- functional-defect idle_enabled is written but never read — toggling "Remind when idle" has zero runtime effect — severity 9 | src-tauri/src/os_events.rs:start_os_listeners (idle-detection branch):191 | NEW
- functional-defect IDLE_THRESHOLD_SECS hardcoded to 900.0 instead of configurable st.idle_threshold_secs — changing the idle-threshold UI input has zero effect — severity 8 | src-tauri/src/os_events.rs:module const IDLE_THRESHOLD_SECS / start_os_listeners:31 | NEW
- process self-authored unresolved TODO proves the team knew the wake/unlock/idle wiring was incomplete when Task #163 was marked COMPLETE — severity 7 | src-tauri/src/os_events.rs:start_os_listeners (TODO comment):29 | NEW
- test-quality zero Rust #[test] blocks exist anywhere in src-tauri/src/*.rs — the layer containing the critical defect has no test harness at all — severity 7 | src-tauri/src/:n/a — entire crate:0 | NEW
- documentation-trust store/migrations.ts comment claims a functioning OS-trigger opt-out that does not exist at runtime — severity 9 | store/migrations.ts:comment above SETTINGS_MIGRATIONS entry:158 | NEW
- documentation-trust Wake/Unlock/Idle toggle descriptions claim independent control that runtime code never honors — severity 9 | app/settings/page.tsx:OS Triggers section JSX:104 | NEW
- documentation-trust updateInterruptConfig JSDoc says "the Rust background thread" (singular), obscuring that two threads exist and neither consumes the new fields — severity 8 | lib/tauriInterrupt.ts:JSDoc above updateInterruptConfig:21 | NEW
- documentation-trust InterruptHandler.tsx comment "Keep the Rust thread in sync" is false for the 4 new fields — severity 7 | components/InterruptHandler.tsx:config-sync effect comment:30 | NEW
- documentation interrupt.rs file header not updated to list the 4 new InterruptState fields — severity 4 | src-tauri/src/interrupt.rs:file header:1 | NEW
- documentation os_events.rs file header documents current behavior as complete rather than disclosing the unread/hardcoded fields — severity 4 | src-tauri/src/os_events.rs:file header:4 | NEW
- functional-defect OS Triggers UI section has no platform gate — renders non-functionally on Windows/Linux — severity 8 | app/settings/page.tsx:OS Triggers section:102 | NEW
- test-quality InterruptHandler.test.tsx not updated for the 3-to-7-arg updateInterruptConfig signature change — severity 7 | components/InterruptHandler.test.tsx:config-sync test block:188 | NEW
- test-quality tests/settingsStore.test.ts has zero coverage for the 4 new OS-trigger setters — severity 6 | tests/settingsStore.test.ts:n/a — missing coverage:0 | NEW
- test-quality tests/tauri.test.ts uses identical boolean values for wakeEnabled/unlockEnabled/idleEnabled, masking swap bugs — severity 5 | tests/tauri.test.ts:update_interrupt_config test cases:81 | NEW
- test-quality three "no-op at current version" migration tests would pass even if the version-guard were deleted — severity 4 | tests/migrations.test.ts:'is a no-op when already at current version' tests:40 | NEW
- input-validation idle-threshold number input has no clamp/validation logic — severity 5 | app/settings/page.tsx:idle-threshold number input onChange:110 | NEW
- reliability out-of-range idleThresholdMinutes can fail Rust u32 deserialization and silently drop the entire bundled IPC call — severity 6 | onChange handler → updateInterruptConfig → update_interrupt_config:110 | NEW
- input-validation setIdleThresholdMinutes has no range validation unlike sibling bounded setters — severity 4 | store/settingsStore.ts:setIdleThresholdMinutes:38 | NEW
- input-validation settings migration validates idleThresholdMinutes type only, not range — severity 6 | store/migrations.ts:SETTINGS_MIGRATIONS[2]:167 | NEW
- code-quality "15 minutes" idle default hardcoded independently in four places with no shared constant — severity 6 | idle-default constants:31 | NEW
- architecture 7-positional-parameter interrupt-config contract duplicated identically across 5 files with no shared schema — severity 6 | update_interrupt_config parameter contract | NEW
- reliability config-sync effect has no debounce, allowing rapid toggles to race and silently revert — severity 5 | components/InterruptHandler.tsx:config-sync effect:31 | NEW
- reliability update_interrupt_config silently no-ops on a poisoned mutex with no error surfaced — severity 5 | src-tauri/src/interrupt.rs:update_interrupt_config:111 | NEW
- reliability exitMandatoryMode has no try/catch and its call sites handle failure inconsistently (pre-existing) — severity 4 | exitMandatoryMode and call sites:60 | NEW
- architecture InterruptHandler.tsx imports directly from store/, violating the components/ layer rule (pre-existing) — severity 4 | components/InterruptHandler.tsx:module imports:1 | NEW
- tests 6 new OS-trigger toggle tests create an appearance of coverage for a feature whose backing implementation is inert — severity 5 | app/settings/page.test.tsx:OS-trigger toggle tests (6 new tests):0 | NEW
- process-systemic Seven independent auditors across seven methodologies converged on the identical root defect with zero disagreement on its existence — a systemic process failure (unit-level plumbing verified end-to-end, final consumption step one file away never traced by a single test), not an isolated coding mistake — severity 9 | CROSS-CUTTING | SYNTHESIS

## 2026-07-06 | Task: Batch 19 Wave 2
- documentation-trust Wave 2's own fix to src-tauri/src/interrupt.rs's header introduced a NEW false claim ("accepts InterruptConfig struct") while the function still takes 7 positional params — caught by the orchestrating CTO's post-wave verification, not by the builder. Poka-yoke: header/comment updates describing a design-in-progress must be re-read against the actual function signature before closing, not just against the intent of the task. — severity 5 | src-tauri/src/interrupt.rs:8 | NEW

## 2026-07-07 | Task: Batch 18 Wave 1
- code-quality store/srsStore.ts defined its own local `CONSECUTIVE_WRONG_RESET = 3` with a comment admitting it was a "Mirror of the triple-wrong threshold in lib/introduction.ts" instead of importing the real exported constant — a parallel-constant Stop-the-Line violation (AGENTS.md: "Any parallel list/array that should be derived from a single source of truth"). Arose because Task #180 (srsStore.ts) and Task #179 (introduction.ts, which newly exported the constant) ran in parallel streams with no shared context — Task #180's builder had no way to know the constant would become importable. Caught by the orchestrating CTO's post-wave verification, not by either builder. Poka-yoke: when two parallel streams both need the same threshold/constant and one stream is the one creating the canonical export, the orchestrator's wave plan should flag the dependency explicitly rather than relying on each builder to independently discover it. — severity 4 | store/srsStore.ts:25 | NEW

## 2026-07-07 | Task: #181
- tests tests/introduction_behavior.test.ts:239-243 — "avoids lastSeenType when multiple alternatives exist" getNextCardType test still uses toContain/not.toBe despite the case being fully deterministic (pool[0] always resolves to "recognize"); Task #181's F15 scoped the tightening to only the null-lastSeenType case, leaving an equally-deterministic sibling test three lines away untightened — severity 3 | tests/introduction_behavior.test.ts:239 | NEW
- scope-drift Task #181's own text anticipated a 2-way file split once tests/introduction.test.ts exceeded 250 lines; the non-seam content alone (shape/constants/scheduling-math + shouldAppearToday/shouldGraduate/recordResult/getNextCardType) did not fit under 250 lines in one file, requiring a 3rd file. Necessity-driven, not scope creep — but tasks that anticipate a file split should say "into N files" rather than hardcoding "two," since line-count math depends on what's added during the task itself — severity 2 | tests/introduction_behavior.test.ts (new file) | NEW
- tooling Task #181's own Done-When grep command (`grep -c "phaseDay\|phase_day\|phase day"`) omits -E, so literal alternation syntax may not match depending on the grep implementation/mode — a verifier running the command exactly as written could falsely report failure even though the done-when's own "(or equivalent parameterized coverage)" clause is satisfied. Poka-yoke: Done-When grep commands using alternation should always specify -E explicitly rather than relying on default grep behavior — severity 1 | .autocode/tasks.md:Task #181 Done when | NEW

## 2026-07-07 | Task: #183 — 8-agent audit (A, B, S, N, K, W, V, R)
- tests tests/introduction_behavior.test.ts:239-243 — my own "avoids lastSeenType when multiple alternatives exist" fix used a fixture (`available=["recognize","produce","fill_blank"]`, lastSeenType="fill_blank") where available[0] already equals the correct answer before filtering, so deleting the `.filter()` call in lib/introduction.ts wouldn't fail the test — a genuine Rule 18/B7 violation introduced while fixing a DIFFERENT Rule 18 violation. Caught independently by Agent K (contract verifier). Poka-yoke: when writing a determinism-proving fixture, explicitly verify the "avoided" value is NOT already the unfiltered answer — the whole point of the test is lost if it is. Fixed by choosing lastSeenType === available[0] so filtering changes the outcome — severity 6 | tests/introduction_behavior.test.ts:239 | NEW
- code-quality lib/answerCheck.ts:12 — ITALIAN_ARTICLES regex has two byte-identical duplicate alternation branches (l'|l', un'|un', both ASCII straight apostrophe U+0027), strongly suggesting curly-apostrophe (U+2019) support was intended but never implemented. Empirically confirmed by Agent W: "l'amico" (curly quote, as iOS/macOS autocorrect produces by default) fails matching where "l'amico" (straight quote) succeeds — a real grading bug for users on Apple devices. Task #183's own hardening (asserting the exact regex source as the correct expected value) is right for a regression test but surfaced rather than fixed the underlying bug — spun out as Task #226 — severity 8 | lib/answerCheck.ts:12 | NEW
- tooling AGENTS.md's Test Assertion Quality Gate documents .toBeGreaterThan(0) as a 4th banned pattern (proves sign, not value) but the mechanical grep gate this task made permanent only checks 3 of the 4 documented patterns; 7 unjustified instances existed at audit time, including 3 inside tests/entitlement.test.ts — a file this very task was already editing. Converged independently by Agents B, N, and V. Poka-yoke: when a task's job is "make the enforcement gate match the documented policy," diff the gate's pattern list against the policy prose line-by-line before declaring the gate complete, not just against the task's own literal bullet list — severity 4 | AGENTS.md:34-38 | NEW
- process 6 inline finding-ID comments added during this task's own remediation (F007, F008, F010, F011, F017, F019) reused bare IDs that were already assigned to unrelated findings from the earlier Batch 19 (#164) audit — same ID scheme, different audit run, no namespace disambiguation. Caught by Red Agent R (unprimed). Fixed by prefixing every inline finding-ID comment with the originating task number ("Task #183 F007") — severity 4 | tests/migrations.test.ts, tests/entitlement.test.ts | NEW
- code-quality 5 `// existence-check:` comments were added to lines that were not actually one of the three banned patterns the tag exists to exempt (already-exact `.toBe()`/`.not.toBe()` calls in migrations.test.ts and packLoader.test.ts) or were attached to a deleted assertion rather than a retained one (session.test.ts) — misapplying a gate-specific convention as generic non-determinism narration. Caught independently by Agents A, K, V, and W (4-way convergence). Fixed by replacing with plain "Note:" comments where the tag didn't apply — severity 2 | tests/migrations.test.ts:243-252, tests/packLoader.test.ts:122, tests/session.test.ts:69 | NEW

## 2026-07-07 | Task: Batch 18 — batch-level audit (8 agents: A, B, S, N, K, W, V, Red R)
- requirements store/srsStore.ts:272 canIntroduceNewCard's cross-day wrong-streak pause (Task #180's own F10 acceptance criterion, BRAND.md "wrong across multiple days → pause new introductions") is dead code — consecutiveWrongToday can never be persisted >= CONSECUTIVE_WRONG_RESET because recordResult (lib/introduction.ts:120-127) resets it to 0 in the exact same write that would push it to the threshold. The only test "proving" this behavior (tests/srsStore.test.ts F10) injects the unreachable state directly via setState, bypassing the only real write path. Converged independently by 7 of 8 auditors (A, B, N, W, K, Red R, V) plus the orchestrating CTO's own trace and empirical grep. Marked COMPLETE despite being non-functional — severity 9 | store/srsStore.ts:272 | NEW
- requirements store/srsStore.ts:246-247, lib/introduction.ts:135-146 the "variety rule" (Task #180, BRAND.md "each encounter uses a different retrieval angle") is inert: recordIntroductionResult is never called with the actually-displayed card type, and nothing anywhere in the codebase reads IntroductionRecord.lastSeenType to select what's shown (StudyCard.tsx renders strictly from the content pack's fixed card.type). getNextCardType's output is written back into lastSeenType purely to satisfy its own unit test — a fully closed loop with zero effect on the user experience. Converged independently by Agents A, B, W plus the orchestrating CTO's own full-repo grep. Poka-yoke: a "wire X into Y" task must include a seam test that traces the value from write to an actual consumer, not just a store-level round-trip test — severity 9 | store/srsStore.ts:246 | NEW
- code-quality lib/introduction.ts:140-146 getNextCardType is structurally broken independent of the wiring gap above: given the fixed-order ALL_CARD_TYPES array, filtering out only the single lastSeenType always leaves the same earlier-indexed alternative, so the function can only ever oscillate between 2 of the 5 CardTypes (recognize/produce) — conjugate, fill_blank, and passage_cloze are permanently unreachable regardless of how many times it's called. Confirmed empirically (10 sequential calls, only 2 distinct outputs) by the orchestrating CTO and independently by Agent W. Directly defeats BRAND.md's stated premise for the feature ("varied retrieval across encounters produces durable memory") — severity 8 | lib/introduction.ts:140 | NEW
- requirements lib/introduction.ts:getDayOfPhase:51-62 date validation only checks string shape (DATE_RE) not calendar validity; a shape-valid, calendar-invalid string ("2026-13-45") passes the guard and the function silently returns NaN instead of throwing, directly contradicting its own docstring ("Throws on malformed input... NaN propagation would cause silent card disappearance"). Converged independently by Agents S, W, K, Red R, V plus the orchestrating CTO's own node verification. This is the exact failure mode Task #179 was supposed to eliminate, reintroduced one validation layer down — severity 7 | lib/introduction.ts:51 | NEW
- data-loss store/migrations.ts:71-90 the v3 migration's isNaN guard (added by Task #184 specifically to reject calendar-invalid dates) does not catch day-of-month rollover — "2026-02-30" passes both DATE_RE and isNaN (JS Date silently normalizes it to March 2, a valid timestamp), so the calendar-invalid string is preserved as-is into the migrated record instead of falling back to today. The guard's own comment overclaims "rejects calendar-invalid strings" as a general class when it only covers month-overflow. Converged independently by Agents K, Red R, V plus the orchestrating CTO's own node verification — severity 6 | store/migrations.ts:77 | NEW
- data-loss store/migrations.ts:67-91 the null-record recovery path (added by Task #184 to prevent a full-store-reset TypeError) produces an IntroductionRecord with only phaseStartDate populated — 10 of 11 required fields are missing. The next recordResult call computes totalEncounters/consecutiveCorrect as undefined+1 = NaN, permanently corrupting those counters and preventing the card from ever graduating (NaN >= threshold is always false). A "recovery" path that itself introduces silent, permanent data corruption on the one record it recovers. Found by Agent A, confirmed by the orchestrating CTO's own code trace — severity 6 | store/migrations.ts:91 | NEW
- error-handling store/srsStore.ts:getIntroductionDueCardIds:250-264 getDayOfPhase's throw (added by Task #179) is uncaught inside a .filter() over ALL introduction records with no per-record try/catch anywhere in the call chain, and the app has zero ErrorBoundary/componentDidCatch components. One corrupted record now aborts due-card computation for every card, not just the offending one — a larger blast radius than the silent single-card disappearance it replaced. Found by Agents S and K independently — severity 5 | store/srsStore.ts:254 | NEW
- security lib/langRegistry.ts:48-50 Object.freeze(LANG_CONFIG_MAP) (Task #186) is shallow; the nested LanguageConfig objects (ITALIAN/SPANISH from lib/language.ts) and their uiStrings/cardLabels sub-objects remain fully mutable at runtime despite the Readonly<> type annotation implying full tamper-proofing. Same class as the already-known F07 shallow-freeze gap. Converged independently by Agents S, A, B, Red R (4-way) — severity 5 | lib/langRegistry.ts:48 | NEW
- security lib/entitlement.ts:activateLicense:139 the `!res.instance?.id` guard (Task #185) checks truthiness only, not type; a non-string id (e.g. a number) from an untyped IPC response passes the guard and is persisted into a string-typed instanceId field, violating the function's own return type contract. Found by Red Agent R — severity 5 | lib/entitlement.ts:139 | NEW
- tests tests/commitSession.test.ts:36-47 "all three slices are consistent — no partial application" claims to prove commitSession's atomic single-set()-call contract but only checks final-state values; would pass identically if commitSession made three sequential set() calls instead of one. Rule 18/B7 violation on the exact contract the test's own name and the store's own doc comment claim to guarantee. Converged independently by Agents K and V — severity 6 | tests/commitSession.test.ts:36 | NEW
- tests tests/useLangPack.test.ts:84-103 the RAW_DISCRIMINANTS/EXPECTED_MESSAGES enumeration (Task #227) omits base_pack_not_loaded — 1 of 5 LoadPackResult error discriminants is never tested, a Rule 16 enumeration gap in a fixture explicitly built to enumerate all discriminants. Found by Agent K — severity 6 | tests/useLangPack.test.ts:93 | NEW
- tests tests/packLoader.test.ts stale-cache-fallback branches (lib/packLoader.ts:210-235) — no test exercises syntactically-valid-but-semantically-malformed cached JSON (e.g. non-array units) reaching the offline fallback path, which skips the shape validation the happy-path download branch performs. Found by Agent K — severity 5 | tests/packLoader.test.ts:1 | NEW
- code-quality lib/introduction.ts:9, store/migrations.ts:60 DATE_RE regex independently defined in two files — the exact duplicate-constant failure class this team already hit once this batch (CONSECUTIVE_WRONG_RESET, logged 2026-07-07 Batch 18 Wave 1) and fixed. AGENTS.md explicitly bans "any parallel list/array that should be derived from a single source of truth." Converged independently by Agents B and Red R — severity 4 | lib/introduction.ts:9 | NEW
- tests tests/study_loop.test.ts:86-109 getStats's masteryPct field (drives MASTERY_GATE, a BRAND.md-critical unlock threshold) is never asserted; 4 of 5 return fields checked. Found by Agent K — severity 4 | tests/study_loop.test.ts:86 | NEW
- tests tests/importBackup.test.ts:106-141 normalizeCardProgress fallback coverage forces only 2 of 7 CardProgress fallback branches (stability, lapses); difficulty/retrievability/dueDate/reps fallback paths untested. Found by Agent K — severity 4 | tests/importBackup.test.ts:106 | NEW
- code-quality lib/introduction.ts:42,61, store/srsStore.ts:257 magic number 22 (phase-day graduation boundary) repeated in 3 places with no shared named constant. Found by Agent B — severity 4 | lib/introduction.ts:42 | NEW
- code-quality lib/introduction.ts:88-91,116 shouldGraduate() is exported but never called from production code; recordResult re-implements the identical check inline instead of calling it — duplicated logic across two expressions of the same rule. Found by Agent B — severity 4 | lib/introduction.ts:88 | NEW
- code-quality AGENTS.md Verification Gate grep (line ~39) bans 4 assertion patterns but the adjacent Stop-the-Line Violations bullet list only mentions 3 (omits .toBeGreaterThan(0)) — a parallel-list violation introduced by this very batch, directly contradicting the rule stated one line above it in the same document. Converged independently by Agents W and K — severity 4 | AGENTS.md:84 | NEW
- tests tests/introduction_behavior.test.ts:178-183 "does not mutate the original record (immutability)" re-checks only 1 of 11 IntroductionRecord fields on the original object; a rewrite that mutates other fields in place would still pass. Found by Agent K — severity 3 | tests/introduction_behavior.test.ts:178 | NEW
- code-quality lib/introduction.ts:79-85, store/srsStore.ts:258 the "effective same-day appearance count" ternary is duplicated 3 times instead of extracted to a shared helper. Found by Agent B — severity 3 | lib/introduction.ts:79 | NEW
- code-quality lib/entitlement.ts:deactivateLicense:204 catch block has no bound identifier (`catch {`), a literal instance of the AGENTS.md-banned "catch{} that silently discards an error" pattern even though the block does log a redacted ref ID. Found by Agent K — severity 3 | lib/entitlement.ts:204 | NEW
- code-quality tests/introduction_behavior.test.ts is 256 lines; its own header comment states the Task #181 split was done "to stay under the 250-line file limit," overshooting its own stated target by 6 lines. Converged independently by Agents A and B — severity 2 | tests/introduction_behavior.test.ts:1 | NEW
- documentation-trust lib/introduction.ts comment "Re-evaluate this interpretation at store-wiring stage (#049)" was deleted (Task #179) without the re-evaluation being performed; BRAND.md's "Wrong once → Day 2 intensity" is still not implemented as an actual frequency bump (the cap remains purely calendar-driven), and the open question was closed via a settled-sounding rationalization comment rather than resolved or explicitly deferred. Converged independently by Agents B and W — severity 2 | lib/introduction.ts:129 | NEW
- documentation-trust lib/introduction.ts:4-5 file header "USED BY" comment claims introduceCard as a consumer of this module; introduceCard (store/srsStore.ts:219-236) constructs its IntroductionRecord literal directly and imports nothing from lib/introduction.ts. Found by Agent V — severity 2 | lib/introduction.ts:4 | NEW
- code-quality tests/useLangPack.test.ts:93-103 EXPECTED_MESSAGES is a parallel hardcoded copy of hooks/useLangPack.ts's production copy strings; any future wording tweak requires a synchronized edit in both files with no compiler enforcement. Found by Red Agent R — severity 3 | tests/useLangPack.test.ts:93 | NEW
- code-quality tests/entitlement.test.ts hardcoded epoch 1798761600000 (for the "2027-01-01T00:00:00Z" fixture) copied 4 times with no shared named constant. Found by Red Agent R — severity 1 | tests/entitlement.test.ts:367 | NEW

- process-systemic Multiple severity 6-9 findings (the cross-day pause, the variety rule wiring gap, the getNextCardType algorithm bug, and to a lesser extent the two date-validation gaps) share one systemic root cause: task-completion in this batch was verified via unit tests that inject state directly (setState) or call pure functions in isolation, never through the actual production call path from user action to persisted effect. Every one of these features looked wired and looked tested, but none were exercised end-to-end before being marked COMPLETE. This recurs the exact Rule 13 (Test the Seams) gap the audit-checklist.md already tracks at the assertion level, but here it recurred at the feature level — severity 8 | CROSS-CUTTING | SYNTHESIS

## 2026-07-08 | Task: Batch 18 remediation re-audit (8 agents: A, B, S, N, K, W, V, Red R)
- requirements store/srsStore.ts:266-276 canIntroduceNewCard's new strandedAcrossDays cross-day pause (Task #228) is defeated by ANY same-day review of the stranded card, not just a stabilizing correct answer — the guard checks `lastSeenDate !== today`, so a WRONG answer on the blocked day also updates lastSeenDate and silently lifts the pause for the rest of that day even though the card never stabilized. Confirmed empirically by the orchestrating CTO (repro script) and independently by Agents W and B. BRAND.md's "pause until this one stabilizes" is only honored for calendar days the card isn't reviewed at all — severity 6 | store/srsStore.ts:274 | NEW
- error-handling store/srsStore.ts:239 recordIntroductionResult calls getDayOfPhase with no try/catch — the sibling call site (getIntroductionDueCardIds) was hardened by Task #234 and tested, this one (the highest-traffic path, hit on every card rating from app/study/page.tsx's onRate handler) was left exposed. Converged independently by Agents S, K, A, W (4 of 8) — severity 6 | store/srsStore.ts:239 | REPEATED (partial fix)
- data-loss lib/packLoader.ts:187,193 the Array.isArray(pack.units) shape guard added by Task #239 covers only 3 of 5 JSON.parse(...) as Pack sites in loadPack; the two "cache hit" paths (sha256-verified hit and the fully-unverified no-manifest offline path) remain unguarded, violating the module's own documented invariant that a corrupted pack is never cached or returned. This is a pre-existing catalogued pattern (patterns.md 2026-06-26) that the remediation only partially closed. Converged independently by Agents K, A, W, B (4 of 8) — severity 5 | lib/packLoader.ts:187 | REPEATED (partial fix)
- tests tests/srsStore.test.ts:647 `expect(n + 1).toBe(n + 1)` is a vacuous tautology (vitest's toBe uses Object.is, and Object.is(NaN,NaN) is true) that passes for any value of n including NaN; the comment's stated rationale ("NaN propagation check") is false. Harmless since the preceding line does the real check, but fails the Deletion Test on its own. Converged independently by Agents N, A, K, V, B (5 of 8) — severity 4 | tests/srsStore.test.ts:647 | NEW
- code-quality lib/utils.ts:isCalendarValidDate's round-trip re-format doesn't zero-pad the year component (uses d.getUTCFullYear() directly, unlike month/day which use padStart), so a genuinely calendar-valid date with year <1000 (e.g. "0999-06-15") is incorrectly rejected as calendar-invalid. Confirmed empirically. Low practical impact — the app never handles pre-1000 dates — but a real logic bug relative to the function's own stated contract. Found by Red Agent R — severity 3 | lib/utils.ts:18 | NEW
- tests tests/langRegistry.test.ts deep-freeze mutation tests cover only 2 of the 3 levels deepFreezeConfig actually freezes (config, uiStrings); the third level (cardLabels), explicitly named in the production fix's own comment, has no dedicated mutation-throws test. Converged independently by Agents K and A — severity 3 | tests/langRegistry.test.ts:130 | NEW
- tests tests/exportBackup.test.ts:44-46 and tests/importBackup.test.ts:65-67 comments claim "all 7 CardProgress fields asserted" but CardProgress has 8 fields and the tests actually assert all 8 — a miscounted comment, not a coverage gap (repeats the same off-by-one error in two places). Found by Agent V — severity 3 | tests/exportBackup.test.ts:44 | NEW
- tests lib/utils.ts:isCalendarValidDate is a newly load-bearing exported function (consumed by both lib/introduction.ts and store/migrations.ts) with no direct unit test — only exercised indirectly via its two callers' tests; no leap-year edge case (e.g. Feb 29 in a non-leap year) is tested anywhere. Found by Agent K — severity 3 | lib/utils.ts:9 | NEW
- documentation-trust lib/introduction.ts:3 file header "DEPENDS ON" comment lists DATE_RE as an import, but the file only imports isCalendarValidDate — DATE_RE is not referenced anywhere in the file. Converged independently by Agents A and W — severity 2 | lib/introduction.ts:3 | NEW
- process-integrity .autocode/tasks.md Task #229 and #230 are marked "Status: COMPLETE" with their acceptance-criteria checkboxes still showing unchecked `[ ]` in the file — the work was independently verified done via other evidence (code + tests), but the task list's own completion markers don't reflect it. Found by Agent W — severity 2 | .autocode/tasks.md | NEW
- documentation-trust BRAND.md still asserts the variety rule ("each encounter uses a different retrieval angle... varied format each time") as an active, delivered feature; Task #229 correctly removed the fake code-level wiring, but the product-level spec was never updated to disclose that this is currently undeliverable given the one-type-per-card content model. Found by Agent W — severity 2 | BRAND.md | NEW

- process-systemic Two of the three real functional defects found this cycle (the recordIntroductionResult uncaught-throw gap and the packLoader partial shape-guard gap) are the SAME root-cause class recurring: a remediation task fixed only the specific call site or code path literally named in the prior finding's text, not every structurally-identical instance of the same pattern in the same function/module. Both gaps were reachable by grepping the touched file for other occurrences of the exact same code shape (another getDayOfPhase call, another JSON.parse(...) as Pack site) — neither required new investigation to find. Lesson: when closing a finding that names "a call site" or "a code path," grep the whole file/module for sibling instances of the identical pattern before considering the fix complete — severity 6 | CROSS-CUTTING | SYNTHESIS

## 2026-07-08 | Task: Batch 18 remediation re-audit cycle 3 (8 agents: A, B, S, N, K, W, V, Red R)
- code-quality lib/specialtyPackLoader.ts:90 has its own inline `!Array.isArray(addOnPack.units)` check instead of importing the shared `validatePackShape()` helper Task #248 just created — a 4th/5th instance of "fixed the named site, missed the structural sibling" (this file wasn't touched by any of the 3 remediation cycles; `validatePackShape` isn't even exported, so reuse wasn't possible without a further edit). Currently inert since SPECIALTY_PACKS is empty, but a live landmine for the day specialty packs ship. Converged independently by Agents A, B, W (3 of 8) — severity 5 | lib/specialtyPackLoader.ts:90 | NEW
- data-loss lib/packLoader.ts:loadPack the two "cache hit" shape-validation-failure branches (sha256-verified hit, no-manifest hit) call clearPackCache() before returning parse_error, but the two "offline-fallback" branches (fetch !res.ok, fetch throws) return parse_error on shape failure WITHOUT calling clearPackCache — a corrupted cache entry hit through either offline-fallback path is never evicted, causing every subsequent offline load attempt to hit the same corrupted cache indefinitely (a permanent self-inflicted outage for that language pack while offline). Found by Red Agent R, confirmed via direct code read — severity 5 | lib/packLoader.ts:229,248 | NEW
- code-quality lib/packLoader.ts:validatePackShape's name and doc comment ("structural shape... a valid Pack") overclaim what it checks — only `Array.isArray(pack.units)`, none of the other 8 required Pack fields, and no validation of individual unit/card element shapes. Matches an already-open, unchecked item in audit-checklist.md ("verify individual Unit and Card fields are validated"). Converged independently by Agents K, V, B, W, Red R (5 of 8) — severity 4 | lib/packLoader.ts:108 | REPEATED (pre-existing catalogued gap, name now more misleading given the function's broader-sounding name)
- tests tests/introduction_behavior.test.ts's "third consecutive wrong advances phaseStartDate" test (the unit test for recordResult, the exact function Task #228 modified to add strandedAcrossDays) asserts 8 fields but never asserts `result.strandedAcrossDays` — deleting the `strandedAcrossDays: true` line from recordResult would not fail this test; only the store-level seam test would catch it. Found by Agent W — severity 4 | tests/introduction_behavior.test.ts | NEW
- requirements store/srsStore.ts:canIntroduceNewCard Task #246's guard simplification (dropping `lastSeenDate !== today`) changes behavior for the same calendar day a triple-wrong reset fires — previously that day was implicitly NOT blocked, now it's blocked immediately. No test (old or new) exercises `canIntroduceNewCard` on that same day in either direction; the new behavior is very likely correct (matches BRAND.md's literal "pause until stabilizes" more closely) but shipped with zero test coverage of the change itself. Converged independently by Agents B, W, Red R (3 of 8) — severity 4 | store/srsStore.ts:281 | NEW
- documentation-trust tests/srsStore.test.ts's F10 seam test comments ("card is stranded AND lastSeenDate ... !== today ... → block") describe the two-clause guard Task #246 removed this cycle; the assertions are still correct but the comments now document logic that no longer exists. Converged independently by Agents V, W — severity 3 | tests/srsStore.test.ts:465 | NEW
- code-quality store/srsStore.ts's recordIntroductionResult and getIntroductionDueCardIds catch-block log messages both hardcode "corrupt phaseStartDate", but getDayOfPhase throws for either phaseStartDate OR today being calendar-invalid — a copy-pasted overclaim (recordIntroductionResult's version copies the pre-existing overclaim already present in getIntroductionDueCardIds since Task #234). Converged independently by Agents V, Red R — severity 3 | store/srsStore.ts:239 | REPEATED

- process-systemic Three consecutive remediation cycles on this same introduction-engine/pack-loader code have each left exactly one new instance of the identical "fixed the literal call site named in the finding, missed a structurally identical sibling" defect (Task #180 → 2 dead features; Task #234/#239 → 2 unfixed sibling call sites; Task #248 → specialtyPackLoader.ts's untouched 6th site). Real-world severity has dropped sharply each cycle (severity 9 → severity 6 → severity 5), and this cycle's two real defects both live in currently-dormant code paths (specialty packs unshipped, offline+corrupted-cache is a narrow double-failure). Worth a decision point: continue iterating to zero, or accept the remainder as documented debt with owner sign-off — severity 5 | CROSS-CUTTING | SYNTHESIS

## 2026-07-08 | Task #251 — Direct spot check WARN
- tests tests/packLoader.test.ts's "network-throws offline-fallback" eviction test has a redundant/misleading forceRedownload:true follow-up assertion that passes identically whether or not clearPackCache ran; the real proof is the two toBeNull() checks already present — severity 3 | tests/packLoader.test.ts | NEW
- tests lib/packLoader.ts's two JSON.parse-throws catch blocks (within the offline-fallback branches) now call clearPackCache but have no dedicated test for that specific sub-case, only the sibling shape-check sub-case is tested — severity 3 | lib/packLoader.ts | NEW
- code-quality lib/packLoader.ts:loadPack now has 4 near-identical "parse → hasValidUnitsArray → evict" blocks across its cache-hit and offline-fallback branches; candidate for extraction into a shared helper if a 5th site is ever added — severity 1 | lib/packLoader.ts | NEW

## 2026-07-08 | Task: Batch 18 remediation re-audit cycle 4 (8 agents: A, B, S, N, K, W, V, Red R)
- data-loss lib/packLoader.ts:clearPackCache's own 3-statement body (pre-existing, untouched by this batch) has no atomicity protection — if the second removeItem() throws, memCache.delete() never runs, leaving a partially-evicted state. Task #251 added 4 new call sites depending on this function's correctness. Blast radius confirmed narrow: the sole caller (hooks/useLangPack.ts) has a terminal .catch() that logs and shows a friendly error, so this doesn't crash the app — it just means eviction silently doesn't complete in an already-rare double-failure scenario. Converged independently by Agents K, S, Red R (3 of 8) — severity 4 | lib/packLoader.ts:97 | NEW
- code-quality lib/packLoader.ts's evictPack never calls clearSpecialtyCache() or prunes loadedAddOns when evicting a base pack that has a merged specialty add-on — the exact "fixed the named site, missed a caller" class this batch spent 3 cycles closing, just in a caller (evictPack) nobody re-checked. Currently dormant since SPECIALTY_PACKS is empty. Found by Agent W — severity 4 | lib/packLoader.ts:evictPack | NEW
- requirements store/srsStore.ts:recordIntroductionResult a record that is both strandedAcrossDays:true and has a calendar-invalid phaseStartDate can never self-heal — the corrupt-date catch path returns before calling recordResult, which is the only code that clears strandedAcrossDays. Permanent block with no test coverage either way; narrow double-fault. Found by Agent W — severity 4 | store/srsStore.ts:recordIntroductionResult | NEW
- documentation-trust CLAUDE.md's Introduction Engine section (§7) still states "Wrong 3x in a row resets dayOfPhase to 1" and "Six exports" for lib/introduction.ts — the actual mechanism built across 4 remediation cycles (phaseStartDate advance + strandedAcrossDays, 10 exports) was never synced back to the project's own canonical architecture doc. Found by Agent W — severity 5 | CLAUDE.md | NEW
- documentation-trust store/migrations.ts's v2-v3 migration comment describes getDayOfPhase's pre-hardening NaN-propagation failure mode as if still current; lib/introduction.ts's getDayOfPhase (changed in this same diff range) no longer returns NaN on invalid input, it throws — the comment was never updated to reflect the fix this same batch made. Found by Red Agent R — severity 4 | store/migrations.ts:50 | NEW
- code-quality lib/packLoader.ts:191 dead-code assignment `cachedData = null` with a copy-pasted "A003-style" comment claiming load-bearing significance — the function returns unconditionally on the next line, so nothing ever reads this value again in this branch. Introduced by this cycle's own edit, not inherited debt. Found by Agent B — severity 4 | lib/packLoader.ts:191 | NEW
- tests tests/packLoader.test.ts's Task #248 shape-validation tests assert only the data-key is null after eviction, not the meta-key — a hypothetical clearPackCache regression dropping only meta-key removal would pass these two tests undetected (though it would be caught by the separate Task #251 tests, since clearPackCache is shared). Found by Agent B — severity 2 | tests/packLoader.test.ts | NEW
- documentation-trust .autocode/audit-checklist.md:54 still lists tests/packLoader.test.ts:118-119's not.toBeNull() assertion as an open gap; it was rewritten to exact-value assertions during Task #183/#248 and never checked off. Found by Agent B — severity 2 | audit-checklist.md | NEW
- tests tests/packLoader.test.ts's new "rejects a malformed add-on pack via the shared hasValidUnitsArray guard" test (Task #250) claims in its own comment to prove delegation to the shared function, but reverting specialtyPackLoader.ts to its OLD functionally-identical inline check would still pass this test — it proves rejection behavior, not delegation. Found by Agent V — severity 3 | tests/packLoader.test.ts | NEW

- process-systemic This cycle broke the 3-cycle recurring "fixed the named site, missed the sibling" pattern for the SPECIFIC findings named in cycle 3 (confirmed by 4 independent agents doing a fresh sibling-instance sweep, all reporting none found) — but the same underlying team habit (fix what was named, don't re-verify the surrounding function's OTHER callers/invariants) produced two new, structurally identical instances this cycle anyway (evictPack's specialty-cache gap; clearPackCache's own internal non-atomicity). Severity has now plateaued around 4-5 for 2 consecutive cycles rather than continuing to drop, and the debt.md register has crossed 30+ entries — a strong signal this batch has reached the point of diminishing returns on further /audit cycles and should either get one more narrowly-scoped cleanup pass or be accepted as substantively complete with the remainder logged as debt — severity 5 | CROSS-CUTTING | SYNTHESIS

## 2026-07-08 | Task: Batch 18 remediation re-audit cycle 5 (8 agents: A, B, S, N, K, W, V, Red R — verifying Tasks #252-257)
- requirements store/srsStore.ts:recordIntroductionResult's Task #254 fix clears strandedAcrossDays on a correct answer even when phaseStartDate is corrupt, but never repairs phaseStartDate itself — the record keeps throwing in getDayOfPhase forever, so getIntroductionDueCardIds's catch block filters it out of the due set every single day (the filter runs before the day-22+ rescue check is ever reached). CLAUDE.md's just-rewritten §7 claims a non-graduated card "can never permanently disappear from both queues" — false for this exact state. Converged independently by 6 of 8 agents (A, B, Red R, W, K, V) — severity 5 | store/srsStore.ts:recordIntroductionResult,getIntroductionDueCardIds | NEW
- tests tests/srsStore.test.ts's "#254: correct answer clears strandedAcrossDays... (self-heal path)" test name overclaims — it never asserts phaseStartDate is repaired or that the card reappears in getIntroductionDueCardIds, only that strandedAcrossDays clears and canIntroduceNewCard unblocks. A subtly-wrong implementation that permanently drops the card would still pass. Found by Agent W — severity 4 | tests/srsStore.test.ts | NEW
- data-loss lib/packLoader.ts:loadPack's forceRedownload path can silently overwrite a merged specialty pack in memCache with a freshly-downloaded unmerged base pack, without pruning loadedAddOns — same defect class Task #253 just fixed in evictPack, different call site. Dormant: no specialty packs ship yet, no production caller passes forceRedownload. Found by Agent A — severity 5 | lib/packLoader.ts:loadPack | NEW
- requirements lib/packLoader.ts:evictPack cannot evict a specialty-pack code directly — isValidPackCode (via ALL_PACK_CODES) excludes specialty codes entirely; isSpecialtyPackCode exists for exactly this check and is never called anywhere. The inverse of Task #253's bug, left unaddressed. Dormant. Found by Agent W — severity 4 | lib/packLoader.ts:evictPack | NEW
- code-quality lib/packLoader.ts:clearPackCache's new error logs use lang-keyed ref IDs (ERR-CACHE-CLEAR-META-{lang}) instead of the file's established Date.now()-based uniqueness pattern used by every other error log in the same file — repeated failures for the same lang are indistinguishable in logs, a direct Rule 8 inconsistency. Found by Agent A — severity 3 | lib/packLoader.ts:clearPackCache | NEW
- code-quality clearSpecialtyPacksForLang and clearPackCache are both independently-callable exported functions whose correctness currently depends entirely on their sole caller (evictPack) always pairing them together; no internal guard prevents a future caller from invoking one without the other and causing a duplicate-merge or false-success. Found by Agent V — severity 3 | lib/specialtyPackLoader.ts,lib/packLoader.ts | NEW
- documentation-trust CLAUDE.md's rewritten §7 attributes the strandedAcrossDays reset/pause mechanism to "Tasks #178, #228, #246" but Task #254 (this same cycle) added a second, independent clearing code path in store/srsStore.ts that bypasses recordResult entirely — the attribution list is incomplete. Found by Agent V — severity 2 | CLAUDE.md | NEW
- documentation-trust lib/specialtyPackLoader.ts's file header "Called by: lib/packLoader.ts (specialty branch of loadPack)" wasn't updated when Task #253 added a second call site (evictPack) — undercounts actual callers. Found by Agent V — severity 2 | lib/specialtyPackLoader.ts | NEW
- tests tests/srsStore.test.ts's "#254: wrong answer does NOT clear strandedAcrossDays..." test never actually enters Task #254's new code branch (gated on correct=true; this test uses correct=false) — passes identically whether #254's fix exists or not, despite its name implying it verifies the fix. Found by Agent V — severity 2 | tests/srsStore.test.ts | NEW
- process An untracked stale worktree directory (.claude/worktrees/agent-a1f394e6195614543/) contains a pre-fix copy of lib/packLoader.ts and tests/packLoader.test.ts missing the #252/#253 fixes entirely — harmless to the build but could mislead a future grep-based audit into thinking the old code is still live. Found by Agent B — severity 2 | .claude/worktrees/ | NEW

- process-systemic For the SPECIFIC 6 findings from cycle 4, no further sibling-miss recurrence was found (all 6 fixes verified root-cause-correct by multiple independent agents) — but the underlying habit produced two MORE new instances of the same defect class this cycle: the forceRedownload/loadedAddOns overwrite gap in loadPack (structurally identical to #253's original bug, different call site) and the inverse evictPack-can't-evict-specialty-code gap. This is now the 5th consecutive cycle in which fixing named findings has surfaced at least one fresh instance of the identical underlying pattern elsewhere in the same ~3-file area. Both new instances are dormant (no specialty packs ship yet) — severity 5 | CROSS-CUTTING | SYNTHESIS

## 2026-07-08 | Task: Batch 18 remediation re-audit cycle 6 (3 agents: A, K, Red R — verifying Tasks #258-259)
- requirements store/srsStore.ts:recordIntroductionResult's Task #258 repair path resets phaseStartDate and clears strandedAcrossDays on a correct answer, but never updates consecutiveCorrect, totalEncounters, appearancesToday, or lastSeenDate — the correct answer that triggers the repair is silently dropped from the card's encounter history and does not count toward the 15-consecutive-correct graduation threshold. This is a hand-rolled partial reimplementation of recordResult rather than a delegation to it. Converged independently by Agents Red R and K — severity 5 | store/srsStore.ts:recordIntroductionResult | NEW
- documentation-trust CLAUDE.md's own newly-added claim that the corrupt-date catch path "fully repairs the record" is itself inaccurate given the above — the same documentation-overclaim pattern recurring in the very fix meant to close a prior documentation-overclaim finding. Found by Red Agent R — severity 4 | CLAUDE.md | NEW
- data-loss lib/packLoader.ts:loadPack's Task #259 fix added clearSpecialtyPacksForLang before the 3 memCache.set(lang, pack) overwrite sites, but missed 4 structurally identical sibling clearPackCache+return sites in the exact same two offline-fallback blocks, a few lines above the sites that were fixed. Those 4 sites wipe memCache entirely (via clearPackCache) without pruning loadedAddOns — a worse outcome than the original bug (data is gone, not just silently overwritten). This is the 7th instance of the "fixed here, missed elsewhere" pattern in this batch, found in the exact same function the fix for the 6th instance had just touched. Dormant (requires a specialty pack merged + an offline fetch failure + shape/parse-invalid stored bytes). Found by Agent A — severity 5 | lib/packLoader.ts:loadPack | NEW
- tests tests/packLoader.test.ts's new #259 test only exercises 1 of the 3 fixed call sites (the fresh-download success path) — the two offline-fallback success paths have zero regression protection for this specific fix. Converged independently by Agents A and K — severity 3 | tests/packLoader.test.ts | NEW
- tests tests/srsStore.test.ts's wrong-answer test for #258 never asserts phaseStartDate is left untouched — a mutant that hoists the phaseStartDate repair outside the `if (correct)` gate while correctly gating strandedAcrossDays would pass every existing test. Found by Agent A — severity 3 | tests/srsStore.test.ts | NEW

- process-systemic This is now the 6th consecutive audit cycle on this batch, and the 2nd cycle in a row where fixing named findings from the prior cycle immediately surfaced at least one fresh instance of the identical "fixed here, missed elsewhere" pattern — this time in the very same lines the fix itself was editing. Severity has held at 5 for 4 consecutive cycles (5→5→5→5) rather than continuing to drop. Both new correctness findings this cycle require a specialty pack to exist, which remains true across all 6 cycles: SPECIALTY_PACKS is still empty in production. This is now unambiguously past the point of diminishing returns for further /audit cycles on Batch 18 — the remaining gaps are entirely in code paths for a feature that hasn't shipped, and each fix attempt has a demonstrated track record of surfacing one more instance of the same latent pattern rather than converging to zero — severity 5 | CROSS-CUTTING | SYNTHESIS

## 2026-07-08 | Task: Batch 18 remediation re-audit cycle 7 (8 agents: A, B, S, N, K, W, V, Red R — verifying Task #260's structural refactor)
- tests A test assertion proving "a subsequent load succeeds after a shape-invalid/parse-invalid cache eviction" was silently deleted during Task #260's consolidation, with no replacement and no comment explaining why — this specific guarantee (recovery after eviction, not just eviction itself) now has zero coverage anywhere in the file. Converged independently by 3 of 8 agents (B, K, V) — severity 4 | tests/packLoader.test.ts | NEW
- code-quality lib/specialtyPackLoader.ts:loadSpecialtyPack's own memCache.set call (the merge write) is a 6th mutator of the same memCache object Task #260 consolidated in lib/packLoader.ts, correctly left OUT of the consolidation (it's an additive merge, not a replace-and-prune operation) but nowhere documented as "deliberately excluded" vs. "not yet found" — the exact ambiguity that caused 6 prior cycles' misses. Converged independently by 3 of 8 agents (A, W, Red R) — severity 4 | lib/specialtyPackLoader.ts:loadSpecialtyPack | NEW
- requirements lib/packLoader.ts:evictAndReject/validateAndCache silently evict on a hasValidUnitsArray shape-validation failure with zero console.error call, asymmetric with the sibling JSON.parse-throw path which does log (CACHE_PARSE_FAIL/PACK_PARSE_FAIL) — an operator debugging "why did this cache silently get wiped" finds a log line for corrupt JSON but nothing for wrong-shape data. Same gap exists in lib/specialtyPackLoader.ts's own shape check. Found by Agent B — severity 4 | lib/packLoader.ts:evictAndReject | NEW
- code-quality The "structural" single-funnel guarantee (all memCache writes route through cacheAndReturn, all evictions through clearPackCache) is enforced by file organization and naming conventions only, not by the type system or a lint rule — a future hand-added `memCache.set` call anywhere in loadPack would still silently bypass it. Converged by Agents A and W — severity 3 | lib/packLoader.ts | NEW
- error-handling A true concurrency race exists between loadPack(lang, {forceRedownload:true}) and loadSpecialtyPack running simultaneously on the same base lang: both read/mutate memCache with an await (network fetch) in between, no lock or version-check, so whichever memCache.set resolves last silently wins — could desync memCache from loadedAddOns. Requires specialty packs to be live AND true concurrent invocation AND forceRedownload — triple-dormant. Found by Red Agent R — severity 4 | lib/packLoader.ts:loadPack + lib/specialtyPackLoader.ts:loadSpecialtyPack | NEW
- documentation-trust clearPackCache's doc comment overclaims: it says "each of the 5 sites hand-rolled this logic independently" prior to the refactor, but only 3 of 5 origins previously called clearSpecialtyPacksForLang at all — the 2 cache-hit branches never had it. Routing them through cacheAndReturn is new behavior for those 2 sites, not a preservation of prior hand-rolled logic. Found by Agent V — severity 2 | lib/packLoader.ts:clearPackCache | NEW
- code-quality lib/packLoader.ts:loadPack (line 226) and lib/specialtyPackLoader.ts:loadSpecialtyPack (line 67) hand-roll the identical SPECIALTY_PACKS ready-check predicate independently in two files rather than deriving from one shared function — if the predicates ever diverge, loadSpecialtyPack's non-null assertion on .find(...)! throws instead of returning a typed error. Found by Agent W — severity 3 | lib/specialtyPackLoader.ts:loadSpecialtyPack | NEW
- process-integrity Confirmed by direct inspection: loadPack("it", ...) is never actually called in production today. hooks/useLangPack.ts:64 bypasses it entirely via STATIC_PACKS for Italian (the only ready base pack), and Spanish ("es") is registered but not ready, so it short-circuits to invalid_lang before ever touching memCache. The entire packLoader.ts machinery this whole 7-cycle remediation arc has hardened has zero production exercise today — only test coverage. Confirmed independently, surfaced by Agent W, verified directly — severity N/A (context, not a defect) | hooks/useLangPack.ts | INFORMATIONAL

- process-systemic 7 consecutive audit cycles on this batch. Cycle 7's max severity (4-5) is flat-to-marginally-lower than cycles 3-6 (flat at 5), but more importantly: every single finding across all 7 cycles on this subsystem requires either a specialty pack to exist (SPECIALTY_PACKS remains empty) or true concurrent invocation (never exercised in tests or production) — and this cycle confirmed directly that the base-pack loadPack path itself has ZERO production exercise today (Italian bypasses it via static bundling, Spanish isn't ready yet). This batch has now spent 7 audit cycles hardening a code path with no live traffic. Recommend accepting the current state and closing further iteration on this specific file until either specialty packs or a second JSON-based base language actually ships — severity 4 | CROSS-CUTTING | SYNTHESIS

## 2026-07-08 | Task: batch 18 — WorldClass cycle 1
- edge-case hooks/useStudySession.ts:78-81 introduce-on-mount effect filters candidate cards only by "no existing FSRS progress or introduction record" — it never applies the same prerequisitesMet() gate that store/srsStore.ts's getNewCards correctly enforces. prerequisites: appears 978 times across content/cards/*.ts (live curriculum data), so a low-tier card ordered ahead of its own prerequisite can have its 22-day intensive introduction clock started before the curriculum says it should be offered — severity 7 | worldclass: -10 pts | WORLDCLASS
- security lib/entitlement.ts:activateLicense,validateLicense log the raw caught IPC error unredacted, while deactivateLicense (Task #074) was hardened to truncate the license key specifically because Tauri IPC errors can echo request params — the same "fixed one site, missed the sibling" pattern this codebase's history is full of, just outside the previously-scrutinized pack-loader area — severity 6 | worldclass: -8 pts | WORLDCLASS
- edge-case lib/answerCheck.ts:ITALIAN_ARTICLES/SPANISH_ARTICLES regexes have no word-boundary check after the matched article token — verified by execution: stripArticle("isola", ITALIAN_ARTICLES) strips to "sola", so checkAnswer("sola", ["isola"], {diacriticTolerant:true}) grades as "correct" against an unrelated word. Any word starting with i/il/lo/la/le/un/una/uno/gli is affected — this is the core grading engine hit on every card review across an 8,000-word curriculum, and no prior audit cycle caught it since none scoped answerCheck.ts — severity 7 | worldclass: -18 pts | WORLDCLASS
- feature-flag lib/introduction.ts:getNextCardType BRAND.md's headline "variety rule" (different retrieval angle each encounter) is honestly disclosed via comment as dead code (Task #229) but genuinely unimplemented — the content model has one Card per word+type, so the same card is shown verbatim up to 10x/day for weeks during the intensive introduction phase — severity 5 | worldclass: -8 pts | WORLDCLASS

## 2026-07-08 | Task: batch 18 — WorldClass cycle 5 (fresh-eyes pass, no prior context, on the already-remediated code)
- edge-case lib/answerCheck.ts:checkAnswer's Levenshtein-typo length gate used a.length (the article-INCLUSIVE normalized accepted answer) instead of the article-stripped length — verified by execution: checkAnswer("ne", ["il re"], {diacriticTolerant:true}) graded "close" pre-fix, because "il re" (5 chars) passed the >4 gate even though the true word "re" is only 2 chars — defeating the gate's own purpose of denying typo tolerance to short words. Pre-existing, not introduced by this batch's regex fix; found by an unprimed fresh-eyes re-read of the exact file this cycle was already scoring — severity 6 | worldclass: -8 pts (fresh-eyes pass) | WORLDCLASS

## 2026-07-09 | Task: batch 12 — 7-agent audit (A, B, S, N, K, W, V, Red R) — first-ever audit of this batch
- auth lib/specialtyPackLoader.ts:loadSpecialtyPack:67 entitlement is enforced nowhere in the data layer for specialty packs — loadPack/loadSpecialtyPack never read purchasedAddOns or call hasAddOn; the only gate anywhere is the onClick wiring decision in components/LanguageGrid.tsx:109. Converged independently by all 7 scored auditors (7/7) — the strongest convergence seen in this project's audit history. Given entitlement is honour-system/client-only by design, this UI wiring decision is the only trust boundary and is not a declared one (Rule 17a) — severity 8 | lib/specialtyPackLoader.ts:loadSpecialtyPack:67 | NEW
- edge-case lib/constants.ts:getTargetLangCode:19 setTargetLangCode("it-medical") stores "en-it-medical"; getTargetLangCode's .split("-")[1] returns "it", silently discarding "-medical" — the entire specialty-pack selection flow is unreachable from the real UI, deterministically, for any hyphenated code. Converged independently by 5 of 7 auditors. Explicitly NOT a mitigating control for the entitlement gap above — an unrelated bug that happens to currently mask it; fixing this bug alone exposes the entitlement gap through the primary UI with zero further code change — severity 8 | lib/constants.ts:getTargetLangCode:19 | NEW
- security store/entitlementStore.ts:clearEntitlement:111 resets purchasedAddOns to [] but never calls clearSpecialtyCache()/clearSpecialtyPacksForLang() — a license deactivation mid-session leaves already-merged specialty content fully accessible in memCache for the rest of the session; loadedAddOns never resyncs with the store — severity 7 | store/entitlementStore.ts:clearEntitlement:111 | NEW
- async lib/specialtyPackLoader.ts:loadSpecialtyPack:67 two race conditions with no locking anywhere in the module: same-code concurrent loads both pass the loadedAddOns.includes check before either pushes (duplicate merge); cross-code concurrent loads sharing a base language each read the base pack independently after their own await, and whichever memCache.merge() resolves last silently discards the other's merge while getLoadedAddOns() reports both as loaded — severity 7 | lib/specialtyPackLoader.ts:loadSpecialtyPack:67 | NEW
- security lib/specialtyPackLoader.ts:loadSpecialtyPack:45 sha256 verification is skipped entirely with no fail-closed branch when manifest?.packs?.[lang] is absent for the requested specialty code — arbitrary content merges into the base pack cache with zero integrity check — severity 8 | lib/specialtyPackLoader.ts:loadSpecialtyPack:45 | NEW
- code-quality lib/langRegistry.ts:isSpecialtyPackCode:88 zero production callers; lib/packLoader.ts:269 reimplements the same check inline instead of calling it. Converged by 4 of 7 auditors. Rule 6 (duplication) and Rule 20b (zero callers outside tests) violation — severity 5 | lib/langRegistry.ts:isSpecialtyPackCode:88 | NEW
- code-quality lib/entitlement.ts:hasAddOn:208 zero production callers despite its own doc comment stating it exists "for use outside React" — i.e. purpose-built specifically to close the entitlement-gap finding above and never wired in. store/entitlementStore.ts:133 duplicates the same logic instead of delegating, breaking the in-file pattern the file's own Rule-15 comment documents (isPackUnlocked and needsValidation both correctly delegate to pure-function twins; hasAddOn is the one action that doesn't). Converged by 5 of 7 auditors — severity 6 | lib/entitlement.ts:hasAddOn:208 | NEW
- requirements lib/packLoader.ts:evictPack:415 guards on isValidPackCode (base-only PackCode type) and cannot evict a specialty code; doc comment "any registered code can be evicted... e.g. after purchase reversal" is false for specialty packs. Converged by 6 of 7 auditors — severity 5 | lib/packLoader.ts:evictPack:415 | NEW
- data-loss lib/specialtyPackLoader.ts:loadSpecialtyPack:67 specialty packs are never given their own persisted storage key at all — merges directly into the base pack's in-memory entry only, so there is no separate cache entry to evict or re-verify even in principle, beyond the evictPack type-guard bug above — severity 6 | lib/specialtyPackLoader.ts:loadSpecialtyPack:67 | NEW
- data-loss lib/packLoader.ts:evictPack:415 never calls clearSpecialtyPacksForLang directly when evicting a base pack while a specialty add-on for that language is loaded (only reached internally via clearPackCache in the normal path) — orphans the add-on's code in loadedAddOns, which continues reporting it as active after the data it depends on has been wiped — severity 6 | lib/packLoader.ts:evictPack:415 | NEW
- error-handling lib/packLoader.ts:evictPack:415 silently returns "evicted nothing" for a specialty code with no error, log entry, or distinguishing return value — Rule 8: Log Everything violation — severity 5 | lib/packLoader.ts:evictPack:415 | NEW
- error-handling lib/specialtyPackLoader.ts:loadSpecialtyPack:67 unchecked non-null assertion on .find()! is safe only because the sole caller pre-checks; would throw a raw TypeError instead of a typed LoadPackResult error if ever invoked without that pre-check, unlike every other path in the function. Converged by 3 of 7 auditors — severity 4 | lib/specialtyPackLoader.ts:loadSpecialtyPack:67 | NEW
- data-loss store/migrations.ts:ENTITLEMENT_MIGRATIONS[2] (v2→v3):153 validates Array.isArray(purchasedAddOns) but not element type/shape. Converged by 3 of 7 auditors — severity 5 | store/migrations.ts:153 | NEW
- data-loss store/migrations.ts:ENTITLEMENT_MIGRATIONS[1] (v1 unlockedPacks guard):133 the same shallow Array.isArray-only validation gap recurs a third time in this same file — a repeating pattern, not a first occurrence — severity 4 | store/migrations.ts:133 | NEW
- feature-flag components/LanguageGrid.tsx:LanguageGrid:109 no feature flag gates the specialty-pack UI section or loadPack's specialty branch — severity 4 | components/LanguageGrid.tsx:109 | NEW
- tests tests/langRegistry.test.ts:1 tests mock getSpecialtyPacks/isSpecialtyPackCode/SPECIALTY_PACKS rather than exercising the real filter logic against a populated registry. Converged by 2 of 7 auditors — severity 4 | tests/langRegistry.test.ts:1 | NEW
- edge-case components/LanguageGrid.tsx:LanguageGrid:109 assumes, undocumented, that a user cannot own a specialty add-on without owning its base language; true only because Italian is always free/unlocked, not structurally enforced — severity 4 | components/LanguageGrid.tsx:109 | NEW
- error-handling lib/language.ts:getLanguageConfig:111 LANGUAGE_MAP[code] ?? ITALIAN silently falls back to Italian's display config for any unrecognized code — a second independent silent-fallback break beyond the constants.ts bug above, currently masked by it. Converged by 2 of 7 auditors — severity 6 | lib/language.ts:getLanguageConfig:111 | NEW
- requirements lib/langRegistry.ts:isSpecialtyPackCode vs isValidPackCode:88 the two functions do not agree on what they validate — isSpecialtyPackCode does not check .ready while packLoader.ts's inline reimplementation does. Converged by 2 of 7 auditors — severity 5 | lib/langRegistry.ts:88 | NEW
- tests tests/langRegistry.test.ts:isSpecialtyPackCode assertions:1 all 4 assertions run with mockSpecialtyPacks reset to length 0 — Array.prototype.some over an empty array returns false regardless of the predicate, so the true/positive branch is never exercised anywhere in the suite. Rule 18/B7 violation — pseudocode test — severity 6 | tests/langRegistry.test.ts:1 | NEW
- tests hooks/useLangPack.ts:loadPack call site:68 no test anywhere proves loadPack/loadSpecialtyPack itself refuses an unpurchased specialty pack; confirmed unimplemented, not merely untested. Converged by 3 of 7 auditors — severity 6 | hooks/useLangPack.ts:68 | NEW
- tests components/LanguageGrid.test.tsx:specialty-pack test suite:1 tests inject hasAddOn as a directly-controlled mock prop, never driving the real entitlementStore or loadPack chain — would not catch deletion of the UI lock or the absence of data-layer enforcement. Converged by 2 of 7 auditors — severity 5 | components/LanguageGrid.test.tsx:1 | NEW
- tests tests/entitlement.test.ts:purchasedAddOns describe block:1 only tests bookkeeping in isolation, no seam test analogous to the file's own existing "seam: activateLicense→setEntitlement→isPackUnlocked" pattern — severity 4 | tests/entitlement.test.ts:1 | NEW
- security store/entitlementStore.ts:purchaseAddOn:140 unconditional array-append with no payment/license/receipt check, reachable by any code path since it's a plain exported store action — severity 6 | store/entitlementStore.ts:purchaseAddOn:140 | NEW
- requirements store/entitlementStore.ts:purchaseAddOn:140 name and comment imply a verified purchase-recording function; implementation has no Promise return, no payment token, no verification, and zero production callers anywhere, even as a stub. Converged by 2 of 7 auditors — severity 5 | store/entitlementStore.ts:purchaseAddOn:140 | NEW
- edge-case store/entitlementStore.ts:purchaseAddOn:140 never validates its code argument against isSpecialtyPackCode; unregistered/malformed strings can be injected and persist forever, with no removal path anywhere — severity 5 | store/entitlementStore.ts:purchaseAddOn:140 | NEW
- async store/entitlementStore.ts:purchaseAddOn (Zustand persist):140 persist middleware writes localStorage from in-memory state at call time, not merged against the on-disk value — two tabs racing on purchaseAddOn for different codes causes the second write to silently drop the first tab's purchase — severity 6 | store/entitlementStore.ts:purchaseAddOn:140 | NEW
- data-loss lib/entitlement.ts:backup/restore serialization path:1 no purchasedAddOns field at all in backup/restore — fails closed (a legitimate purchaser loses add-on entitlement on restore) rather than open — severity 4 | lib/entitlement.ts:1 | NEW
- edge-case lib/packTypes.ts:hasValidUnitsArray:1 validates only Array.isArray(pack.units); does not cross-check unitCount/cardCount against units.length nor validate individual unit/card element shapes; specialtyPackLoader.ts spreads these fields into the merged pack without further checking — severity 5 | lib/packTypes.ts:1 | NEW
- requirements lib/constants.ts:getTargetLangCode:19 return type is declared string, implying round-trip fidelity with setTargetLangCode; for hyphenated codes it silently returns a truncated substring with no type-level or runtime failure signal — a contract-lie framing distinct from the functional bug above — severity 6 | lib/constants.ts:getTargetLangCode:19 | NEW

- auth Entitlement enforcement exists nowhere in the data layer for specialty packs: not in loadPack, not in loadSpecialtyPack, not in evictPack's reversal path, not in purchaseAddOn's write path. The purpose-built pure function meant to close this gap (lib/entitlement.ts hasAddOn) was built and never wired in. Every layer of the paid feature is architecturally unenforced, and this is not a dormant nicety — it is the entire trust boundary for a feature explicitly sold as a paid add-on (BRAND.md) — severity 8 | CROSS-CUTTING | SYNTHESIS
- code-quality Documentation and function/type names repeatedly claim behavior the code does not have: evictPack claims universal eviction and purchase-reversal support it cannot deliver, purchaseAddOn's comment claims a packLoader connection that does not exist, specialtyPackLoader's header claims purity it violates via I/O and module-state mutation, and getTargetLangCode's string return type implies round-trip fidelity it silently breaks — severity 5 | CROSS-CUTTING | SYNTHESIS
- tests The test suite systematically stops at the exact boundary where the real bugs live: mocking isSpecialtyPackCode/getSpecialtyPacks instead of exercising the real registry, injecting hasAddOn as a controlled prop instead of driving the real entitlementStore, and never exercising isSpecialtyPackCode's true/positive branch at all. No test in the suite would catch deletion of the UI-only entitlement gate or a reversed equality check — severity 6 | CROSS-CUTTING | SYNTHESIS
- async Every stateful specialty-pack write path lacks concurrency control: loadSpecialtyPack races on concurrent loads (same-code duplicate merge, cross-code silent discard), and purchaseAddOn's Zustand persist races across browser tabs with last-write-wins semantics. Neither path has a lock, CAS, or merge-on-read strategy — severity 6 | CROSS-CUTTING | SYNTHESIS

- process-systemic First-ever audit of this batch (pre-dates the audit SOP, never formally reviewed before). 7-agent independent review converged 7/7 on a single finding (entitlement enforced only at the UI layer, never the data layer) — the strongest convergence recorded in this project's audit history — plus 5/7 convergence on a second, compounding bug (localStorage lang-pair encoding truncates every hyphenated specialty code) that currently masks the first finding entirely. An unprimed naive reader (Agent N, excluded from scoring) additionally found two documented stop-the-line violations neither of the 7 scored auditors caught: lib/constants.ts calls window.localStorage directly instead of routing through lib/storage.ts, and components/LanguageGrid.tsx imports from content/index.ts directly, violating the components/-imports-from-hooks/lib-only rule. Both should be promoted to tasks regardless of scoring exclusion — severity 8 | CROSS-CUTTING | SYNTHESIS
- edge-case lib/answerCheck.ts:checkAnswer calls stripArticle on the raw untrimmed typed/answer strings; stripArticle's regex is anchored at ^ with no leading \s*, so accidental leading whitespace (e.g. typed " la casa") never matches the article alternation at all — verified by execution: checkAnswer(" la casa", ["casa"]) graded "wrong" pre-fix despite being a legitimate answer. Pre-existing, not introduced by this batch's regex fix; found by the same fresh-eyes pass — severity 5 | worldclass: -5 pts (fresh-eyes pass) | WORLDCLASS

## 2026-07-14 | Task: batch 12 -- second re-audit (8 agents: A, B, S, N, K, W, V, Red R) -- ran 2026-07-13, logged 2026-07-14 during Wave 13 consolidation reconciliation
- architecture lib/packCache.ts:18 + lib/specialtyPackLoader.ts:14-21 Genuine circular ES-module dependency between two lib/ files (packCache imports clearSpecialtyPacksForLang from specialtyPackLoader; specialtyPackLoader imports 5 symbols from packCache). Violates Rule 3 (Layers Down Only) and Rule 6 (Extract Ready) -- neither module can be extracted independently. Neither file's header acknowledges the cycle. at lib/packCache.ts:18 + lib/specialtyPackLoader.ts:14-21:module-level imports. — severity 6 | lib/packCache.ts:18 + lib/specialtyPackLoader.ts:14-21 | NEW
- documentation-trust lib/packCache.ts Header claims '@internal Used by lib/packLoader.ts. Not part of the module's external public API' -- false as of this batch; lib/specialtyPackLoader.ts also imports readCacheMeta/writeCacheMeta/readCacheData/writeCacheData/clearPackCache directly. at lib/packCache.ts:module header:14. — severity 5 | lib/packCache.ts | NEW
- documentation-trust CLAUDE.md CLAUDE.md states lib/langRegistry.ts exports getSpecialtyPacks(lang); this export does not exist in the current file (deleted this batch by Task #301). at CLAUDE.md:Section 6:0. — severity 5 | CLAUDE.md | NEW
- documentation-trust lib/langRegistry.ts USED BY header omits hooks/useLangPack.ts, which imports isValidPackCode, SPECIALTY_PACKS, isReadySpecialtyPackCode directly. at lib/langRegistry.ts:module header:10. — severity 5 | lib/langRegistry.ts | NEW
- code-quality lib/langRegistry.ts isSpecialtyPackCode and isReadySpecialtyPackCode are byte-identical implementations under two different names with different call sites -- duplicated logic that will silently diverge. Highest-convergence finding in the batch -- 5 independent auditors (A, K, N, Red R, V) flagged this identical issue. at lib/langRegistry.ts:isSpecialtyPackCode/isReadySpecialtyPackCode:88. — severity 6 | lib/langRegistry.ts | NEW
- code-quality components/LanguageGrid.test.tsx Mock still defines getSpecialtyPacks: () => [] though the real module no longer exports it -- stale mock left behind after removal. at components/LanguageGrid.test.tsx:vi.mock("@/lib/langRegistry"):49. — severity 4 | components/LanguageGrid.test.tsx | NEW
- requirements components/LanguageGrid.tsx:24,135 + app/page.tsx:79 Task #308 widened onUpgradeClick to (code?: string) => void and LanguageGrid calls onUpgradeClick(sp.code), but the only production caller (app/page.tsx:79) discards the argument entirely via a zero-arg closure -- Rule 20 violation (type-signature-only fix, no real caller wired). Five independent auditors (A, K, N, Red R, V) converged on this. at components/LanguageGrid.tsx:24,135 + app/page.tsx:79:onUpgradeClick. — severity 7 | components/LanguageGrid.tsx:24,135 + app/page.tsx:79 | NEW
- tests tests/langRegistry.test.ts Never mocks a SPECIALTY_PACKS entry with ready:false, so isSpecialtyPackCode's && sp.ready clause is never exercised against a real conditional -- deleting it breaks no test (Rule 18 violation). at tests/langRegistry.test.ts:SpecialtyPack registry describe block:70. — severity 5 | tests/langRegistry.test.ts | NEW
- tests store/entitlementStore.ts Task #322's empty-receiptToken rejection has zero test coverage anywhere in tests/entitlement.test.ts -- deleting the guard breaks nothing. at store/entitlementStore.ts:purchaseAddOn:228. — severity 5 | store/entitlementStore.ts | NEW
- code-quality lib/packLoader.ts:250-266 + :270-275 seedMemCache takes an unvalidated lang:string and writes directly to the memCache singleton with no isValidPackCode/READY_PACK_CODES check, silently invalidating getInstalledPacks' documented invariant that memCache is only ever populated via validated writes. at lib/packLoader.ts:250-266 + :270-275:seedMemCache / getInstalledPacks. — severity 5 | lib/packLoader.ts:250-266 + :270-275 | NEW
- documentation-trust store/entitlementStore.ts clearEntitlement's final-sweep comment claims clearSpecialtyCache() handles the case of a specialty pack whose registry entry was removed between merge and deactivation -- false; that orphaned base language is also excluded from affectedBaseLangs (its SPECIALTY_PACKS.find() returns undefined), so evictPack never runs for it either. The memCache-eviction guarantee that is the entire point of Task #326 does not extend to this case, though the comment implies it does. at store/entitlementStore.ts:clearEntitlement:188. — severity 5 | store/entitlementStore.ts | NEW
- code-quality hooks/useLangPack.ts console.error and setTargetLangCode (a localStorage write) execute directly in the hook's render body rather than inside a useEffect -- impure render function; under double-invoked renders (StrictMode) this can fire more than once, contradicting the adjacent comment's claim of 'at most once per corrupt value.' at hooks/useLangPack.ts:useLangPack:58. — severity 4 | hooks/useLangPack.ts | NEW
- architecture lib/constants.ts:16-34 + hooks/useExportImport.ts:25,67 Repeated, still-unfixed violation of CLAUDE.md Section 3 (localStorage must route through lib/storage.ts) -- first flagged in the original 2026-07-09 Batch 12 audit. A second, previously-undetected instance found this cycle in hooks/useExportImport.ts, indicating the violation is systemic rather than contained to one file. at lib/constants.ts:16-34 + hooks/useExportImport.ts:25,67:getTargetLangCode/setTargetLangCode. — severity 6 | lib/constants.ts:16-34 + hooks/useExportImport.ts:25,67 | NEW
- error-handling lib/packLoader.ts A fully garbage/unregistered code (neither a valid base pack code nor a registered specialty pack) silently no-ops with zero console output -- no warn, no error -- violating Rule 8 (Log Everything). at lib/packLoader.ts:evictPack:294. — severity 4 | lib/packLoader.ts | NEW
- security hooks/useExportImport.ts:78-81 + store/entitlementStore.ts:82-100,146 Restoring a backup calls setEntitlement({...result.entitlement, licenseKey, instanceId}), which includes purchasedAddOns; setEntitlement blindly spreads it into state with no purchaseAddOn/verify_addon_receipt call. Bounded by the honour-system entitlement model (CLAUDE.md Section 5) and purchaseAddOn's currently-dormant stub status (Task #295), but a real consistency gap worth closing before specialty content ships. at hooks/useExportImport.ts:78-81 + store/entitlementStore.ts:82-100,146:setEntitlement (backup restore path). — severity 5 | hooks/useExportImport.ts:78-81 + store/entitlementStore.ts:82-100,146 | NEW
- code-quality store/entitlementStore.ts setEntitlement's declared parameter type omits purchasedAddOns entirely even though the only call site (hooks/useExportImport.ts) passes it via object spread -- the declared contract is narrower than actual runtime behavior. at store/entitlementStore.ts:setEntitlement (type signature):82. — severity 4 | store/entitlementStore.ts | NEW
- security store/migrations.ts:159-163 vs lib/importBackup.ts:119-124 The v2->v3 migration filters purchasedAddOns to string-typed elements only with no isSpecialtyPackCode registration/ready check, while lib/importBackup.ts validates both type AND registration for the identical field -- a real enforcement asymmetry between two code paths writing the same security-sensitive field. at store/migrations.ts:159-163 vs lib/importBackup.ts:119-124:ENTITLEMENT_MIGRATIONS v2->v3 / parseBackup. — severity 5 | store/migrations.ts:159-163 vs lib/importBackup.ts:119-124 | NEW
- code-quality lib/packCache.ts write() prunes in-memory add-on tracking via clearSpecialtyPacksForLang but never removes that code's own persisted storage keys, unlike clearPackCache which pairs the identical prune with storage-key removal -- a 5th instance of the exact bug class clearPackCache's own doc comment says 4 prior tasks (#250, #251, #253, #259) already forgot. at lib/packCache.ts:PackMemCacheImpl.write:51. — severity 6 | lib/packCache.ts | NEW
- async store/entitlementStore.ts Drops any storage event arriving while a rehydrate is in flight and never re-triggers rehydrate() once it settles -- a second cross-tab write mid-rehydrate is never picked up until an unrelated future set() call happens to observe fresh state. at store/entitlementStore.ts:_handleCrossTabStorageEvent:293. — severity 5 | store/entitlementStore.ts | NEW
- edge-case lib/packTypes.ts:58-81 + lib/specialtyPackLoader.ts:117-122 hasValidUnitsArray never checks that pack.unitCount/cardCount are numbers; _mergeFromJson computes unitCount/cardCount sums directly from these unvalidated fields -- a non-numeric value passes shape validation and silently string-concatenates instead of summing. at lib/packTypes.ts:58-81 + lib/specialtyPackLoader.ts:117-122:hasValidUnitsArray / _mergeFromJson. — severity 4 | lib/packTypes.ts:58-81 + lib/specialtyPackLoader.ts:117-122 | NEW
- security lib/packLoader.ts The base-pack branch of loadPack has no entitlement check at all -- isPackUnlocked is enforced only at the UI layer (LanguageGrid.tsx, app/page.tsx), unlike the specialty-pack branch which independently re-checks purchasedAddOns inside specialtyPackLoader.ts. Real asymmetric defense-in-depth between the two pack types. at lib/packLoader.ts:loadPack:92. — severity 5 | lib/packLoader.ts | NEW
- error-handling store/entitlementStore.ts:clearEntitlement:182-193 + hooks/useLicenseActivation.ts:84-85 evictPack(...).catch() swallows failure and clearEntitlement's returned Promise always resolves; handleDeactivate awaits it then unconditionally reports successful deactivation even if the underlying memCache eviction failed. at store/entitlementStore.ts:clearEntitlement:182-193 + hooks/useLicenseActivation.ts:84-85:clearEntitlement / handleDeactivate. — severity 5 | store/entitlementStore.ts:clearEntitlement:182-193 + hooks/useLicenseActivation.ts:84-85 | NEW
- tests AGENTS.md Verification Gate + components/LanguageGrid.test.tsx The banned-weak-assertion grep gate is hard-scoped to tests/ --include=*.test.* only, but Rule 14 mandates co-located component tests -- components/LanguageGrid.test.tsx contains 12+ banned-pattern assertions with no existence-check comments that are structurally invisible to the automated gate. at AGENTS.md Verification Gate + components/LanguageGrid.test.tsx:weak-assertion grep gate. — severity 6 | AGENTS.md Verification Gate + components/LanguageGrid.test.tsx | NEW
- data-loss lib/importBackup.ts unlockedPacks/purchasedAddOns entries that fail validation are silently dropped via .filter() with no log, no user-facing warning, and no counter analogous to validCardCount/skippedCardCount computed for cards two blocks above -- violates the stop-the-line rule against silently corrupting persisted user data. at lib/importBackup.ts:parseBackup:113. — severity 6 | lib/importBackup.ts | NEW
- tests components/LanguageGrid.test.tsx:129,143,159,203,232 Every onUpgradeClick assertion checks only toHaveBeenCalled(), never toHaveBeenCalledWith(...) -- including the specialty-tile test specifically covering Task #308's onUpgradeClick(sp.code) call. Proves the type change was never validated to actually matter (test-side half of F007). at components/LanguageGrid.test.tsx:129,143,159,203,232:onUpgradeClick assertions. — severity 6 | components/LanguageGrid.test.tsx:129,143,159,203,232 | NEW
- requirements components/LanguageGrid.tsx:29-42 + store/entitlementStore.ts:110 BRAND.md states specialty packs are sold as add-ons within the Pro tier, but the Add-ons section's visibility gate (isPackUnlocked(sp.baseLang) || hasAddOn(sp.code)) has no licenseType check at all -- once SPECIALTY_PACKS gains a ready entry, every it-* add-on becomes visible/purchasable to a user who has never held Pro. at components/LanguageGrid.tsx:29-42 + store/entitlementStore.ts:110:specialtyPacks visibility gate / isPackUnlocked. — severity 5 | components/LanguageGrid.tsx:29-42 + store/entitlementStore.ts:110 | NEW
- requirements store/entitlementStore.ts purchaseAddOn has no check that licenseType === "subscription" before persisting a purchase, unlike app/stats/page.tsx (the other Pro-gated call site) which correctly routes through isProEnabled(flag, licenseType) as CLAUDE.md/AGENTS.md mandate for all Pro-gated features. at store/entitlementStore.ts:purchaseAddOn:221. — severity 5 | store/entitlementStore.ts | NEW
- async lib/packCache.ts:clearPackCache:137-173 vs cacheAndReturn:189-192 clearPackCache awaits Promise.allSettled for storage removal BEFORE memCache.delete(lang); a concurrent loadPack(lang) can complete its own memCache.write in that window and then have its freshly-loaded entry wiped -- no in-flight lock for base-pack loads analogous to specialtyPackLoader's inFlight Map. at lib/packCache.ts:clearPackCache:137-173 vs cacheAndReturn:189-192:clearPackCache. — severity 5 | lib/packCache.ts:clearPackCache:137-173 vs cacheAndReturn:189-192 | NEW
- code-quality lib/language.ts The hyphen-split fallback is a third, weakest independent implementation of "is this a specialty code" logic (alongside langRegistry.ts's two functions) -- accepts any hyphenated string with a matching registered base-language prefix regardless of whether the suffix is a real SPECIALTY_PACKS entry, and logs nothing on that path, contrary to the adjacent comment's claim of preventing silent masking. at lib/language.ts:getLanguageConfig:122. — severity 5 | lib/language.ts | NEW
- error-handling hooks/useLangPack.ts:73-84 + store/entitlementStore.ts:171-193 useLangPack's useState initializer calls seedMemCache("it", ...) exactly once at mount with no re-seed mechanism; clearEntitlement's evictPack can wipe memCache["it"] out from under a still-mounted component. Any specialty-pack load attempted afterward in the same session permanently fails with base_pack_not_loaded until a full page reload, with nothing signaling why. at hooks/useLangPack.ts:73-84 + store/entitlementStore.ts:171-193:useLangPack useState initializer / clearEntitlement. — severity 6 | hooks/useLangPack.ts:73-84 + store/entitlementStore.ts:171-193 | NEW
- error-handling store/entitlementStore.ts useEntitlementStore.persist.rehydrate() is not wrapped in try/catch; a synchronous throw leaves _rehydrateInFlight stuck true forever, permanently and silently disabling cross-tab sync for that tab. at store/entitlementStore.ts:_handleCrossTabStorageEvent:298. — severity 5 | store/entitlementStore.ts | NEW
- code-quality store/entitlementStore.ts clearEntitlement is a public store action reachable from anywhere; two concurrent invocations redundantly compute and evict the same base langs, relying on an undocumented idempotency assumption. at store/entitlementStore.ts:clearEntitlement:155. — severity 4 | store/entitlementStore.ts | NEW
- documentation-trust lib/specialtyPackLoader.ts Doc comment claims p1===p2 holds via loadPack's return for concurrent same-code loads -- false; loadPack is declared async and always wraps its return in a fresh Promise, so this is only true for direct loadSpecialtyPack calls. A contradicting comment in tests/packLoader.test.ts added in the same batch disagrees with this exact claim. at lib/specialtyPackLoader.ts:loadSpecialtyPack doc comment:269. — severity 5 | lib/specialtyPackLoader.ts | NEW
- documentation-trust store/entitlementStore.ts USED BY header is affirmatively false -- names app/learn/page.tsx and app/study/page.tsx as importers though neither imports this module directly, while omitting six real direct importers (app/page.tsx, app/stats/page.tsx, hooks/useLangPack.ts, hooks/useExportImport.ts, hooks/useLicenseActivation.ts, components/EntitlementValidator.tsx). at store/entitlementStore.ts:module header:10. — severity 5 | store/entitlementStore.ts | NEW
- tests tests/entitlement.test.ts:1049,1060,1065,1324-1341 Every hasAddOn test checks only behavioral output (true/false), identical whether the store delegates to libHasAddOn or reverts to inline duplicated logic -- the Task #300 delegation itself is completely unproven by any test (Rule 18 violation). at tests/entitlement.test.ts:1049,1060,1065,1324-1341:hasAddOn tests. — severity 6 | tests/entitlement.test.ts:1049,1060,1065,1324-1341 | NEW

- process-systemic Second independent re-audit of this batch after all 37 first-round findings (#295-#327) reached COMPLETE status. Highest-convergence finding (severity 7, 5/8 auditors): Task #308's onUpgradeClick(code?) type-signature widening was never wired to a real caller -- app/page.tsx discards the argument via a zero-arg closure (Rule 20 violation). Second-highest convergence (severity 6, 5/8 auditors): isSpecialtyPackCode and isReadySpecialtyPackCode are byte-identical duplicate implementations. Wave 13 (4 parallel streams) closed 45 of 46 assigned findings 2026-07-14; Task #357 (purchaseAddOn Pro-gate) was correctly deferred as architecturally blocked by an off-limits test file, not fabricated as complete despite one stream's completion report incorrectly implying otherwise -- caught via independent source verification before being recorded as done — severity 7 | CROSS-CUTTING | SYNTHESIS

## 2026-07-15 | Task: batch 12 -- third re-audit (7 scored agents: A, B, S, K, W, V, Red R; Agent N unscored naive lane) -- ran against Wave 13's committed state (commit 7a26598)
- requirements lib/packLoader.ts:loadPack:126 Rule 20b (orphan-caller) violation. loadPack's non-free base-pack entitlement gate checks (options?.unlockedLangs ?? []).includes(lang), but the only production caller, hooks/useLangPack.ts:111, never passes unlockedLangs. Dormant only because the sole ready base pack ("it") is free; the moment a second base pack ships ready:true, every legitimately-subscribed user hits invalid_lang forever. Independently found by 6 of 7 scored auditors (A, B, S, K, W, Red R) plus confirmed by Agent N -- the single strongest convergence signal in this audit run. at lib/packLoader.ts:loadPack:126. — severity 8 | lib/packLoader.ts:loadPack:126 | NEW
- requirements hooks/useLangPack.ts:useLangPack:73 The useState initializer only seeds memCache via STATIC_PACKS[targetLang], which has exactly one key ("it"). Selecting a specialty pack tile routes through LanguageGrid.tsx:137 -> app/page.tsx:33-37 -> a full-reload navigation with the specialty code as targetLang; the base pack is never seeded, so loadSpecialtyPack's memCache.has(baseLang) precondition permanently fails with base_pack_not_loaded. Dormant only because SPECIALTY_PACKS' one entry has ready:false; the UI path that triggers it already exists and is live. at hooks/useLangPack.ts:useLangPack:73. — severity 7 | hooks/useLangPack.ts:useLangPack:73 | NEW
- security lib/packLoader.ts:fetchManifest:74 Rule 8 violation. The !res.ok branch has zero logging (unlike the catch block below it), and the parsed manifest has no structural shape validation anywhere in the codebase. A CDN error envelope returned as HTTP 200 would be silently treated as "no manifest available," disabling sha256 verification on every fresh pack download with zero operator-visible log signal. at lib/packLoader.ts:fetchManifest:74. — severity 6 | lib/packLoader.ts:fetchManifest:74 | NEW
- data-loss store/migrations.ts:ENTITLEMENT_MIGRATIONS[1]:137 The v0->v1 unlockedPacks migration filters elements to typeof==="string" only -- no pack-code registration check, unlike its v3 purchasedAddOns sibling (Task #344, explicitly hardened) and lib/importBackup.ts's equivalent filter. Same "one call site hardened, sibling left exposed" pattern (Rule 19b) this team has previously logged as recurring. at store/migrations.ts:ENTITLEMENT_MIGRATIONS[1]:137. — severity 5 | store/migrations.ts:ENTITLEMENT_MIGRATIONS[1]:137 | NEW
- data-loss store/migrations.ts:ENTITLEMENT_MIGRATIONS[3]:164 The v2->v3 purchasedAddOns filter validates against the CURRENT, mutable isSpecialtyPackCode/.ready flag rather than a historical purchase-time record. If a pack ships ready:true, is purchased, then reverts to ready:false, a later migration run silently drops that user's paid purchase record with no warning logged anywhere. at store/migrations.ts:ENTITLEMENT_MIGRATIONS[3]:164. — severity 5 | store/migrations.ts:ENTITLEMENT_MIGRATIONS[3]:164 | NEW
- code-quality lib/specialtyPackLoader.ts:clearSpecialtyCache:47 Rule 10 violation. clearSpecialtyCache's name implies it clears specialty-pack cache state, but it only resets loadedAddOns bookkeeping and the inFlight map -- it never touches memCache. Three separate call sites carry compensating disclaimer comments warning readers not to assume the name's full scope; a name requiring three compensating comments elsewhere is itself the defect. at lib/specialtyPackLoader.ts:clearSpecialtyCache:47. — severity 4 | lib/specialtyPackLoader.ts:clearSpecialtyCache:47 | NEW
- error-handling lib/packCache.ts:readCacheMeta:161 Rule 8 violation, repeat/unfixed across a rewrite. readCacheMeta and readCacheData's catch blocks still omit the lang argument from their ref IDs, even though this file was substantially rewritten and extracted from packLoader.ts in this batch. A cache-read failure for Spanish vs Italian remains indistinguishable in production logs. at lib/packCache.ts:readCacheMeta:161. — severity 4 | lib/packCache.ts:readCacheMeta:161 | NEW
- code-quality store/entitlementStore.ts:purchaseAddOn:278 The Task #357 deferral comment states a store-level Pro gate would break tests/entitlement.test.ts "which calls purchaseAddOn with licenseType:\"free\"" -- verified false against the current file: every purchaseAddOn call site now runs under a beforeEach setting licenseType:"subscription" (a Wave 13 change). The deferral's own stated blocking reason no longer matches the file it cites, risking a future engineer accepting a stale rationale instead of re-evaluating whether the gate can now be implemented. at store/entitlementStore.ts:purchaseAddOn:278. — severity 5 | store/entitlementStore.ts:purchaseAddOn:278 | NEW
- code-quality app/page.tsx:LanguagePicker:29 Direct window.localStorage.getItem(LANG_PAIR_KEY) call instead of getLangPair(), violating lib/constants.ts's own documented invariant that it is "the SOLE AUTHORIZED CALLER of window.localStorage for LANG_PAIR_KEY." A live, non-dormant rule violation caught by the unprimed naive-reader lane, missed by all 7 scored auditors. at app/page.tsx:LanguagePicker:29. — severity 4 | app/page.tsx:LanguagePicker:29 | NEW
- error-handling lib/importBackup.ts:parseBackup:66 data.entitlement is checked only by truthiness while data.srs on the same line gets a strict shape check (typeof, non-null, non-array). A backup with entitlement:"corrupted" or entitlement:5 passes this guard and silently defaults every entitlement field instead of the backup being rejected the way equally-malformed srs input would be. at lib/importBackup.ts:parseBackup:66. — severity 5 | lib/importBackup.ts:parseBackup:66 | NEW
- data-loss hooks/useExportImport.ts:handleImport:82 When a backup's entitlement fields are null/falsy, this branch silently skips setEntitlement entirely -- the current session's entitlement state is left completely untouched with no reset, merge, or flag -- while the success message still reports "Restored N card(s) of progress" with no indication entitlement restoration did nothing. at hooks/useExportImport.ts:handleImport:82. — severity 5 | hooks/useExportImport.ts:handleImport:82 | NEW
- edge-case lib/packTypes.ts:hasValidUnitsArray:61 Validates unit id/name/cards and card id/type/prompt/accepted/tags/tier but not unit.level/theme/emoji/prerequisiteUnits, all non-optional on Unit. components/LevelSection.tsx:55 and app/study/page.tsx:43 unconditionally dereference unit.prerequisiteUnits.every(...) with no guard -- a pack passing sha256 and this validator but missing prerequisiteUnits would crash the UI on first render rather than fail at load. The offline authoring-time validator (scripts/validatePack.ts) already checks this field; the runtime guard is strictly weaker than its own mirror. at lib/packTypes.ts:hasValidUnitsArray:61. — severity 6 | lib/packTypes.ts:hasValidUnitsArray:61 | NEW
- tests tests/seam_importRestore.test.ts:1 Rule 13/18 gap. This file's own header states its purpose is covering the backup-restore path end-to-end, but grep for licenseKey/instanceId/purchasedAddOns/setEntitlement across the file returns nothing -- the entitlement-restore branch has zero seam coverage despite being squarely within this test file's stated scope. at tests/seam_importRestore.test.ts:1. — severity 4 | tests/seam_importRestore.test.ts:1 | NEW
- async store/entitlementStore.ts:clearEntitlement + lib/specialtyPackLoader.ts:loadSpecialtyPack A specialty-pack load in flight during deactivation takes purchasedAddOns as a one-time snapshot. If clearEntitlement's eviction completes and useLangPack's Task #362 re-seed effect re-populates memCache["it"] before the stale in-flight merge runs, the merge completes using pre-deactivation entitlement after purchasedAddOns has already been reset to []. Dormant only because the one registered specialty code has ready:false. at store/entitlementStore.ts:clearEntitlement:198. — severity 5 | store/entitlementStore.ts:clearEntitlement:198 | NEW
- async lib/packCache.ts:PackMemCacheImpl.write:118 write() fires its specialty-storage-key cleanup fire-and-forget (not awaited). A base-pack replace racing a concurrent loadSpecialtyPack merge that is persisting its own storage keys can have its just-written keys silently deleted by the trailing cleanup, while loadedAddOns/memCache still report the add-on merged in memory -- an unlocked, untested race. at lib/packCache.ts:PackMemCacheImpl.write:118. — severity 5 | lib/packCache.ts:PackMemCacheImpl.write:118 | NEW

- requirements Capability implemented at the library/callee layer without the one real production caller being updated to invoke it correctly -- the same shape of gap recurring twice in one batch. loadPack grew an unlockedLangs parameter but its sole caller never passes it; the specialty-pack architecture was built out and LanguageGrid's Add-ons UI already routes to it, but useLangPack's seeding logic was never extended to cover a specialty code as targetLang. Both gaps are invisible to their own test suites because the tests exercise the callee in isolation rather than mounting the real caller -- exactly the disqualified-proof-of-wiring pattern Rule 20 names. Both dormant today only because of a coincidental fact about current content, not because the wiring is correct — severity 8 | CROSS-CUTTING | SYNTHESIS
- code-quality Documentation is not kept in sync with code changes as a systemic habit, not an isolated incident. The false claim "SPECIALTY_PACKS is currently empty" persists in lib/packLoader.ts's own header plus two test files plus a security checklist. Three separate module headers carry stale DEPENDS ON/USED BY claims verified false by grep. A deferral comment's stated blocking rationale (Task #357) no longer matches the test file it cites as of this batch's own changes. No process step re-validates header/comment claims when the code underneath them shifts — severity 4 | CROSS-CUTTING | SYNTHESIS
- data-loss The "one call site hardened, sibling left exposed" pattern (Rule 19b) recurs within this batch's own migration and validation code. store/migrations.ts's v1 unlockedPacks migration lacks the registration check its v3 sibling was explicitly given three entries later. lib/importBackup.ts validates srs's shape strictly on the same line where entitlement gets only a truthiness check. In each case a validation was hardened on one path during remediation without a grep sweep of structurally identical sibling paths in the same file — severity 5 | CROSS-CUTTING | SYNTHESIS
- error-handling Multiple independent functions return an apparent-success or silent no-op state when the correct behavior is to surface a failure or partial-failure signal: fetchManifest's !res.ok branch returns null with no logging; useExportImport's entitlement-restore branch silently skips while still reporting import success; evictPack resolves its Promise without evicting anything for a specialty/garbage code. Each is a case where a caller who doesn't inspect logs cannot tell "succeeded" from "silently did nothing" — severity 5 | CROSS-CUTTING | SYNTHESIS


## 2026-07-16 | Task: #378 (stream W14A, cycle 1)
- [async] No eviction-generation guard on shared in-flight base load; evictPack racing loadBasePackFresh resurrects evicted pack in memCache and storage; Rule 19b symmetry violated vs specialtyPackLoader #394 — severity 7 | lib/packLoader.ts:loadBasePackFresh:156 | NEW
- [data-loss] Data-then-meta write ordering is the unsafe ordering #309 reversed in specialtyPackLoader; orphaned meta-less bytes served by offline fallback with zero sha256 re-verification — severity 6 | lib/packLoader.ts:loadBasePackFresh:267 | NEW
- [error-handling] Fresh-download sha256Hex unguarded; contradicts never-rejects comment; cache-hit sibling IS wrapped — severity 5 | lib/packLoader.ts:loadBasePackFresh:240 | NEW
- [async] Shared promise rejects whole dedup cohort on single throw (fault-isolation regression) — severity 5 | lib/packLoader.ts:loadPack:146 | NEW
- [tests] No Rule 13 seam test for the hook→loader handoff #378 exists to fix — severity 5 | hooks/useLangPack.test.ts:vi.mock:18 | NEW
- [code-quality] Base invalid_lang for specialty target shows Add-on not purchased.; pinned instead of fixed — severity 5 | hooks/useLangPack.ts:useLangPack:164 | NEW
- [edge-case] forceRedownload bypasses dedup and does not register; TOCTOU class remains for forced-vs-normal pairing — severity 5 | lib/packLoader.ts:loadPack:142 | NEW
- [code-quality] Non-canonical ready-unfiltered specialty lookups (isKnownCode, effect find); unready code with network base triggers useless base download — severity 5 | hooks/useLangPack.ts:useLangPack:128 | NEW
- [security] seedMemCache has no FREE_PACK_CODES invariant; future non-free static base + add-on purchase grants full base pack — severity 5 | lib/packLoader.ts:seedMemCache:297 | NEW
- [async] No useIsHydrated gate; pre-hydration unlockedPacks default causes transient invalid_lang for future non-free packs — severity 5 | hooks/useLangPack.ts:useLangPack:77 | NEW
- [tests] seedMemCache happy-path test asserts only console.error absence; deleting memCache.write stays green — severity 5 | tests/packLoader.test.ts:seedMemCache:779 | NEW
- [tests] LANG_PAIR pin test passes with entire #378 logic deleted — severity 5 | hooks/useLangPack.test.ts:pin:516 | NEW
- [tests] #296 seedMemCache payload assertions pass for any non-empty array — severity 5 | hooks/useLangPack.test.ts:296-test:211 | NEW
- [error-handling] Unguarded sha256Hex in specialty loader (sibling of F002; off-limits file) — severity 5 | lib/specialtyPackLoader.ts:198 | NEW
- [code-quality] Duplicated options type literal created by extraction — severity 4 | lib/packLoader.ts:loadBasePackFresh:167 | NEW
- [requirements] Network-base branch unreachable under real registry state — severity 4 | hooks/useLangPack.ts:useLangPack:140 | NEW
- [code-quality] Stale header: SPECIALTY_PACKS "currently empty" is false — severity 4 | lib/packLoader.ts:header:28 | NEW
- [tests] Eviction-generation re-seed tested for static-base branch only — severity 4 | hooks/useLangPack.test.ts:557 | NEW
- [async] Specialty precondition blind to in-flight base loads (dormant; hook sequences) — severity 4 | lib/packLoader.ts:loadPack:115 | NEW
- [code-quality] Three mutually-unaware concurrency mechanisms across two loaders — severity 4 | lib/packLoader.ts:module:156 | NEW
- [code-quality] STATIC_PACKS is a hook-local parallel registry vs langRegistry — severity 4 | hooks/useLangPack.ts:STATIC_PACKS:39 | NEW
- [tests] Invalid existence-check tag on deterministic units.length assertion — severity 4 | hooks/useLangPack.test.ts:149 | NEW
- [edge-case] forceRedownload silently dropped for specialty codes — severity 4 | lib/packLoader.ts:loadPack:116 | NEW
- [error-handling] tray-listener registration has no catch (off-limits file) — severity 4 | app/learn/page.tsx:41 | NEW
- [async] Hardening applied to specialtyPackLoader with base-pack peer left open across three protections (Rule 19b) — severity 7 | CROSS-CUTTING | SYNTHESIS
- [code-quality] Comments and identifiers claiming contracts the code does not enforce (7 instances incl. new #378 comments) — severity 5 | CROSS-CUTTING | SYNTHESIS
- [tests] Non-falsifiable tests pinning the exact behaviors #378 claims to deliver — severity 6 | CROSS-CUTTING | SYNTHESIS

## 2026-07-17 | Task: #378 (stream W14A, cycle 2 — PASS)
- [async] useIsHydrated subscribe race + zustand persist never finishes hydration on storage failure — now load-bearing for pack loads; hook carries 3s grace fallback, root fix routed to lib/storage.ts owner — severity 5 | lib/storage.ts:useIsHydrated:101 | ROUTED

## 2026-07-17 | Task: #378 — WorldClass cycle 1 (83/100)
- [edge-case] isKnownCode base-pack half lacks ready filter — persisted unready base code (en-es) strands user with no self-heal; same class F011 fixed for specialty codes — severity 6 | worldclass: -6 pts | WORLDCLASS
- [code-quality] Generation-guard primitive duplicated between basePackLoader and specialtyPackLoader, self-acknowledged in comments but never extracted — severity 5 | worldclass: -5 pts | WORLDCLASS
- [code-quality] 30 lines of specialty base-resolution business logic inline in useLangPack effect instead of a pure lib function — severity 4 | worldclass: -4 pts | WORLDCLASS
- [tests] Compound hydration recovery path (grace expiry then late hydration self-heal) untested — severity 4 | worldclass: -4 pts combined | WORLDCLASS
- [performance] N concurrent useLangPack mounts multiply uncached fetchManifest calls and grace timers — severity 4 | worldclass: -3 pts | WORLDCLASS
- [security] basePackLoader only-packLoader-may-import contract enforced by comment alone — severity 4 | worldclass: -2 pts | WORLDCLASS

## 2026-07-27 | Task: batch 12 -- fourth re-audit (8 agents: A, B, S, N, K, W, V, Red R) -- ran against Wave 15's committed state (commit 9b12348)
- [code-quality] Registered-specialty-pack-code predicate hand-rolled independently in 5 files with no shared function; already caused Task #384's registration-vs-readiness bug -- severity 5 | lib/importBackup.ts:isRegisteredSpecialtyCode:138 | REPEATED
- [error-handling] getLangPair() doesn't repair malformed values the way its sibling getTargetLangCode() does; the sibling's own repair is never persisted either, permanently blocking backup restore for a corrupted stored value -- severity 5 | lib/constants.ts:getLangPair:46 | NEW
- [concurrency] specialtyPackLoader's hand-rolled generation counter is asymmetrically hardened vs basePackLoader's shared generationGuard primitive -- severity 6 | lib/specialtyPackLoader.ts:_mergeFromJson:114 | ESCALATE
- [security] Specialty pack offline/no-manifest fallback paths never re-verify sha256 against the recorded cache hash, asymmetric with basePackLoader's staleBytesMatchRecordedHash fix -- severity 6 | lib/specialtyPackLoader.ts:_doLoad:224 | REPEATED
- [code-quality] Purchased-but-since-unready specialty pack routes into the unowned UI branch, showing a buy CTA to someone who already paid -- severity 6 | components/LanguageGrid.tsx:141 | NEW
- [security] Loader-level base-pack entitlement gate is expiry-blind; isPackUnlocked's expiry logic never reached by the real content routes -- severity 5 | lib/packLoader.ts:loadPack:189 | NEW
- [error-handling] evictPack can never reject (Promise.allSettled internally); clearEntitlement's defensive .catch and its re-throw block are dead code contradicting the function's own doc comment -- severity 6 | lib/packCache.ts:clearPackCache:235 | NEW
- [security] isProEnabled never checks subscription expiry unlike its sibling isPackUnlocked; 3 live production call sites rely on it today -- severity 6 | lib/featureFlags.ts:isProEnabled:26 | NEW
- [code-quality] store/srsStore.ts bypasses lib/constants.ts's sole-authorized-caller rule for localStorage, the same violation class fixed elsewhere under Task #340/#389 -- severity 5 | store/srsStore.ts:26 | NEW
- [security] Restored licenseKey/instanceId validated only by typeof string with no length/charset check, bypassable via a crafted backup file -- severity 5 | lib/importBackup.ts:148 | NEW
- [code-quality] parseFlag defaults to true unless explicitly disabled, inverting the safe-off default for an unfinished feature flag -- severity 5 | lib/featureFlags.ts:parseFlag:18 | NEW
- [security] Hand-crafted unsigned backup import grants a full week of paid access with zero license-server contact, worse than the normal activation flow though not worse than direct localStorage editing -- severity 6 | hooks/useExportImport.ts:readFile:81 | NEW
- [data-integrity] SRS migration validates only phaseStartDate; the other 9 IntroductionRecord fields pass through with zero type checking -- severity 6 | store/migrations.ts:88 | NEW
- [error-handling] lib/constants.ts has zero try/catch anywhere; a localStorage throw crashes the page with no ErrorBoundary -- severity 6 | lib/constants.ts:15 | NEW
- [data-integrity] useIsHydrated's failsafe timeout can silently overwrite live user state if real hydration finishes after the failsafe already gave up -- severity 6 | lib/storage.ts:useIsHydrated:114 | NEW
- [code-quality] hasValidUnitsArray never cross-checks unitCount/cardCount against actual array lengths despite _mergeFromJson using them arithmetically -- severity 5 | lib/packTypes.ts:hasValidUnitsArray:75 | NEW
- [documentation] lib/basePackLoader.ts's "USED BY: packLoader.ts ONLY" header is false (packResolver.ts also imports it); the test written to enforce this boundary asserts exactly two importers, contradicting its own name -- severity 4 | lib/basePackLoader.ts:15 | NEW
- [code-quality] isSpecialtyPackCode's name promises registration but checks registration+ready, forcing 4 other files to hand-roll a workaround -- severity 4 | lib/langRegistry.ts:isSpecialtyPackCode:103 | NEW
- [test-quality] 15 tests across 9 files fail the Deletion Test -- clustered specifically around entitlement/boundary-adjacent modules -- severity 4 (representative) | tests/packLoader.test.ts:441 | SYNTHESIS
- [test-quality] "has all card type labels defined and non-trivial" asserts only non-empty/non-trivial, not the exact expected strings -- a transposed label would still pass -- severity 3 | tests/language.test.ts:198 | NEW
- [code-quality] Sub-4-severity items (documentation drift, minor test hygiene, small duplication): logged to debt.md rather than promoted, see 2026-07-27 entries there.

- [process] Fix-the-instance-miss-the-sibling recurred a 4th+ time in this exact batch across 4 unrelated findings (registration predicate, generation guard, sha256 offline path, srsStore localStorage call) -- severity 6 | CROSS-CUTTING | SYNTHESIS
- [security] Two independent expiry-aware entitlement functions (isPackUnlocked, isProEnabled) both have real call paths bypassing their own expiry logic, via different mechanisms -- severity 6 | CROSS-CUTTING | SYNTHESIS
- [code-quality] N-hand-rolled-copies-of-one-check recurs across 4+ unrelated categories (specialty-code registration, hydration timers, license/receipt regexes, magic-number length checks) -- severity 5 | CROSS-CUTTING | SYNTHESIS
- [documentation] Every file touched by the #378 extraction now carries at least one stale header/CLAUDE.md claim; no CI gate compares header claims to real import graphs -- severity 5 | CROSS-CUTTING | SYNTHESIS
- [error-handling] Multiple independent error paths erase the distinction between a real failure and a benign default, contrary to Rule 8 -- severity 5 | CROSS-CUTTING | SYNTHESIS

## 2026-07-28 | Task: batch 12 -- fifth re-audit (9 agents: A, B, S, N[retried], K, W, V, Red R) -- ran against Wave 18's committed state (commit 263578b)
- [correctness] hooks/useLangPack.ts's unpurchased-specialty redirect isn't gated on entitlement-store hydration, so a real owner's pack selection can be permanently overwritten on a slow cold start -- severity 6, independently found by 2 auditors | hooks/useLangPack.ts:useLangPack:110 | NEW
- [validator-completeness] hasValidUnitsArray never validates card.prerequisites' shape; reachable from the LIVE FSRS queue and introduction engine, not gated behind unshipped specialty packs -- severity 7 | lib/packTypes.ts:hasValidUnitsArray:79 | NEW
- [test-coverage] app/stats/page.tsx's entire populated-dashboard render path (the paid Stats page's core value) has zero happy-path test coverage -- severity 7, stop-the-line per AGENTS.md | app/stats/page.tsx:StatsPage:86 | NEW
- [resilience] No pack/manifest fetch() call has a timeout or AbortController; a single hung connection permanently poisons the in-flight cache for that language/manifest with no recovery short of an app restart -- severity 6 | lib/basePackLoader.ts:loadBasePackFromStorageOrNetwork:183 | NEW
- [security] createPurchaseAddOn has no post-await deactivation-guard re-check, unlike the sibling specialty-pack-load path that already carries this exact guard -- severity 5, dormant | store/entitlementAddOns.ts:createPurchaseAddOn:120 | NEW
- [correctness] getLangPair's doc comment claims parity with getTargetLangCode's repair behavior but misses the empty-tail case, risking a silently corrupted persisted storage key -- severity 6 | lib/constants.ts:getLangPair:82 | NEW
- [rule-violation] lib/specialtyPackLoader.ts is now 430 lines, over the same 400-line cap just fixed in a sibling file this same wave family -- severity 5 | lib/specialtyPackLoader.ts:1 | NEW
- [correctness] parseFlag silently enables a safe-off-by-default flag when its env var is set to an empty string rather than left unset -- severity 5 | lib/featureFlags.ts:parseFlag:26 | NEW
- [test-quality] evictPack's fullyClean discriminant not asserted; a hollow #435 hydration test never advances timers far enough to invoke the code it claims to test -- severity 4 | tests/storage.test.ts:1 | NEW
- [documentation] security.md's own tracked S1/S3 findings are stale -- both already resolved, one citing a file:line that moved under a later refactor -- severity 4 | .autocode/agents/security.md:47 | REPEATED
- [test-quality] EntitlementValidator.test.tsx has a test that doesn't prove its own name, plus 2 unjustified banned assertions that evade the project's grep gate since it only scans tests/ -- severity 6 | components/EntitlementValidator.test.tsx:128 | NEW
- [test-quality] useLicenseActivation.test.ts asserts lastValidated via expect.any(Number) rather than a value near Date.now(), failing the Deletion Test -- severity 4 | hooks/useLicenseActivation.test.ts:47 | NEW

- [process] Fix-the-instance-miss-the-sibling recurred a 5th+ documented time within this single cycle: the hydration-gate omission, the file-size cap recurrence, and the missing post-await guard are three independent instances of the identical root cause -- severity 6 | CROSS-CUTTING | SYNTHESIS
- [documentation] Documentation staleness has escalated from source comments to the project's own audit tooling and memory (security.md, CLAUDE.md, module headers) -- a stale entry here misleads the next audit cycle or engineer, not just a local reader -- severity 5 | CROSS-CUTTING | SYNTHESIS
- [process] The Verification Gate's banned-assertion grep only scans tests/, structurally unable to catch violations in component-colocated tests despite Rule 14 mandating exactly that location -- already flagged as theoretical in Batch 18, now confirmed live -- severity 5 | CROSS-CUTTING | SYNTHESIS

## 2026-07-28 | Task: batch 12 -- sixth re-audit (8 agents: A, B, S, N, K[retried], W[retried], V, Red R) -- ran against Wave 19's committed state (commit 8878499)
- [process] Task #450's own widened Verification Gate grep returns 29 hits when run exactly as written today; the batch was carried to zero open tasks with no updated cycle-6 verdict reflecting a currently-red mandatory gate -- severity 9 (critical) | AGENTS.md:39 | NEW
- [documentation] security.md's S1/S3 "Resolved Findings" citations are stale on arrival -- Task #447's same-wave file split (specialtyPackLoader.ts -> specialtyPackMerge.ts) moved the exact code they cite -- severity 6, 5-way independent convergence (A, B, S, V, K) | .autocode/agents/security.md:60 | REPEATED
- [documentation] lib/generationGuard.ts's own header falsely claims specialtyPackLoader.ts's adoption is still a pending carry-forward; it completed and was confirmed this same cycle -- same root cause as the security.md finding above, found independently -- severity 5 | lib/generationGuard.ts:12 | NEW
- [correctness] hooks/useLangPack.ts's hydrationGraceExpired timeout-fallback branch can still permanently persist an unconfirmed redirect when hydration is genuinely stuck, not just slow -- Task #442's fix narrowed the window without closing it -- severity 6, 2-way independent convergence (S, N) | hooks/useLangPack.ts:140 | NEW
- [process] scripts/validatePack.ts still not synced with lib/packTypes.ts's hasValidUnitsArray -- flagged in cycle 5 for one divergence (card.prerequisites), still open one cycle later, plus a second divergence found this cycle (unitCount/cardCount cross-check) -- severity 6 | scripts/validatePack.ts:33 | REPEATED
- [code-quality] getLangPair's #446 fix is real but copy-pasted rather than shared with getTargetLangCode -- reproduces the exact parallel-derivation defect class that caused the bug it fixed -- severity 5 | lib/constants.ts:89 | NEW
- [code-quality] FETCH_TIMEOUT_MS declared independently in 3 files by this wave's own #445 fix -- a fresh instance of the already-tracked parallel-constant anti-pattern, introduced not inherited -- severity 4 | lib/basePackLoader.ts:194 | NEW
- [correctness] parseFlag's #448 fix only closes the undefined/empty-string case; any other non-conforming truthy env value still resolves to enabled=true regardless of defaultEnabled -- severity 5 | lib/featureFlags.ts:31 | NEW
- [rule-violation] store/entitlementStore.ts (403 lines) and lib/packLoader.ts (402 lines) have crept back over the 400-line Rule 1 cap -- severity 4, 2-way independent convergence (A, B) | store/entitlementStore.ts:1 | NEW
- [async] Narrow untested race between evictPack's Promise.all() eviction and bumpAddOnDeactivationGuard() -- a concurrent purchase can transiently flicker into purchasedAddOns before being overwritten -- severity 3, dormant | store/entitlementAddOns.ts:158 | NEW
- [test-coverage] lib/specialtyPackMerge.ts, this cycle's highest-risk new extraction, has no dedicated test file of its own -- severity 4 | lib/specialtyPackMerge.ts:1 | NEW

- [process] Fix-the-instance-miss-the-sibling recurred again at its smallest radius yet: within a single file's single diff hunk (entitlementStore.ts's header edit added 6 entries but missed the one import added in that same hunk) -- severity 5 | CROSS-CUTTING | SYNTHESIS
- [documentation] Precise file:line documentation is structurally fragile against same-wave refactors -- two independent files (security.md, generationGuard.ts) went stale in the same wave because a sibling task moved code out from under them -- severity 5 | CROSS-CUTTING | SYNTHESIS
- [process] Fixes are being validated against the reported repro case, not the general defect class -- three separate reviewers each found a nearby untouched variant of a bug its own fix just "closed" (hydration timeout path, parseFlag's non-empty-garbage case, evictPack's guard-timing window) -- severity 5 | CROSS-CUTTING | SYNTHESIS
- [process] New code is reproducing already-tracked anti-patterns immediately -- the FETCH_TIMEOUT_MS triplication was introduced by this wave's own fix, not inherited debt -- severity 4 | CROSS-CUTTING | SYNTHESIS

## 2026-07-28 | Task: batch 12 -- seventh re-audit (8 agents: A, B, S, N, K, W, V, Red R) -- ran against Wave 20's committed state (commit 8c102e1)
- [data-loss] parseBackup's newer-app-version compatibility gate is bypassed by a truthy non-number _version field (e.g. string "999"), confirmed via direct execution -- severity 7, LIVE | lib/importBackup.ts:94 | NEW
- [error-handling] validatePack's card-ID-uniqueness loop throws an uncaught TypeError on a malformed pack (unit.cards: null) instead of returning a normal error array, crashing the real CI validator process -- severity 6, LIVE, confirmed via direct execution | scripts/validatePack.ts:181 | NEW
- [tests] store/entitlementCrossTabSync.ts (Wave 20's own Task #463 extraction) has no dedicated test file for its dedup/requeue/throw-recovery concurrency logic -- severity 5, LIVE, 5-way independent convergence (A, B, K, W, Red R) -- the strongest convergence signal recorded in this batch's audit history | store/entitlementCrossTabSync.ts:1 | NEW
- [documentation] generationGuard.ts's corrected "no carry-forward" header now contradicts two sibling docs (basePackLoader.ts, a test file) touched the SAME wave -- a Rule 23 direct hit, the fix reproducing its own defect class one file away -- severity 4, 2-way convergence (A, B) | lib/generationGuard.ts:16 | NEW
- [tests] featureFlags.ts's TRUTHY_FLAG_VALUES second entry ("1") is never exercised by any test -- a Rule 16 violation inside the exact wave that introduced the enumeration -- severity 4 | tests/featureFlags.test.ts:1 | NEW
- [tests] fetchWithTimeout.test.ts's "backstop does not fire" test is itself pseudocode -- would pass whether or not the code it claims to prove exists -- a Rule 18 violation in a brand-new file authored to close a prior test-quality gap -- severity 4, 2-way convergence (K, V) | tests/fetchWithTimeout.test.ts:76 | NEW
- [process] vitest.config.ts excludes scripts/ from coverage entirely, hiding this batch's own flagship CI-validator logic from the Verification Gate -- structurally the same shape as cycle 6's own root cause, inverted -- severity 4 | vitest.config.ts:26 | NEW

- [process] Static/reading-based review (7 of 8 reviewers) missed both new LIVE bugs entirely; only the execution-based reviewer (running crafted inputs against the real code) caught them -- worth weighting execution-based verification higher, not just adding more reading-based reviewers -- severity 5 | CROSS-CUTTING | SYNTHESIS
- [process] This is the first cycle where the SPECIFIC critical failure from the prior cycle (a silently-red gate) is genuinely, verifiably fixed -- real forward progress -- but 4 of 7 promoted findings this cycle are Rule 23 violations: fixes that close the named defect while reproducing the same class one file/branch/enumeration-entry away -- severity 5 | CROSS-CUTTING | SYNTHESIS

## 2026-07-28 | Task: batch 12 -- eighth re-audit (8 agents: A, B, S, N, K, W, V, Red R) -- ran against Wave 21's committed state (commit b68e8fa)
- [error-handling] entitlementCrossTabSync's async rehydrate rejection is silently swallowed -- no logging on the reject path, unlike its sync-throw sibling; this wave's own new test for the exact scenario sets up a spy and never asserts on it -- severity 7, LIVE, direct Rule 8 violation | store/entitlementCrossTabSync.ts:77 | NEW
- [tests] fetchWithTimeout.test.ts's rewritten test (itself created because a prior version proved nothing) proves only one of the two timers its own finally block clears -- confirmed empirically by disabling the untested clearTimeout and watching all tests stay green -- severity 6, 2-way convergence (A, W) | tests/fetchWithTimeout.test.ts:76 | NEW
- [tests] 2 of validatePack.test.ts's 6 new regression tests use string fixtures that are non-discriminating (JS strings are iterable, never throw) -- confirmed by direct mutation testing, same defect class as cycle 7's F06 recurring inside the very suite that fixed it -- severity 5, 3-way convergence (K mutation-tested, V, Red R) | tests/validatePack.test.ts:163 | NEW
- [data-loss] parseBackup's #467 fix gives a truthy non-number _version a worse, generic error message than the scenario its own rationale calls out -- the new test locks the regression in as intended behavior -- severity 5, execution-verified | lib/importBackup.ts:94 | NEW
- [code-quality] validatePack's dedup loop uses an unchecked card id cast, producing garbled "Duplicate card IDs: " output for malformed cards -- highest convergence this cycle (4/8), 2 reviewers confirmed via direct execution -- severity 4 | scripts/validatePack.ts:194 | NEW

- [process] The dominant defect shape this cycle: a fix or test handling one of two structurally-identical branches while leaving its twin unaddressed, recurring 3 times independently, each time INSIDE the very task meant to close that class of gap (Tasks #469, #472, #468) -- severity 6 | CROSS-CUTTING | SYNTHESIS
- [process] Convergence and severity were decoupled this cycle -- the highest-convergence finding (4/8 reviewers) was the lowest-severity one (cosmetic), while the highest-severity finding (a live silent error-swallow) had only 1 reviewer -- a reminder not to let reviewer-count alone drive triage -- severity 4 | CROSS-CUTTING | SYNTHESIS
- [process] Security posture was genuinely clean this cycle -- both the dedicated security checklist and the adversarial ATTACKER lens independently found zero exploitable bypasses; the batch's live problems this cycle are entirely test-rigor and error-surfacing discipline, not security -- severity 3 | CROSS-CUTTING | SYNTHESIS

## 2026-07-28 | Task: batch 12 -- ninth re-audit (8 agents: A, B, S, N, K, W, V, Red R) -- ran against Wave 22's committed state (commit 26d242e)
- [code-quality] lib/importBackup.ts's new string-_version branch uses !isNaN instead of isFinite, accepting "Infinity"/hex/fractional strings as plausible versions -- the same defect ALSO exists in the untouched sibling numeric branch shipped 2 waves ago -- severity 6, LIVE, strongest convergence recorded yet in this batch (6/8 reviewers) | lib/importBackup.ts:108 | NEW
- [error-handling] validatePack's dedup-loop id guard (Task #478) closes the non-string case but not the empty-string case -- validateCard's own check treats both as equally invalid, the new guard only replicated half -- severity 5, 5-way convergence (N, B, K, W, Red R) | scripts/validatePack.ts:201 | NEW
- [requirements] parseBackup's new string-version branch has no acceptance path at all -- a numeric string equal to the current version is rejected while its numeric equivalent is accepted -- severity 5, 3-way convergence with differing severity framings (K/Red R: regression; W: accepted tradeoff) | lib/importBackup.ts:106 | NEW
- [error-handling] entitlementCrossTabSync's Task #474 fix logs a rejection branch that Zustand's real persist.rehydrate() can never trigger under this store's actual configuration -- traced against the real library source, not just the test mock -- severity 6, single-reviewer but exceptionally verified (Security Agent S) | store/entitlementCrossTabSync.ts:84 | NEW
- [code-quality] parseBackup's generic error-message string is now triplicated -- a direct Poka-Yoke stop-the-line violation -- severity 4 | lib/importBackup.ts:104 | NEW

- [process] Every one of this wave's three substantive fixes patched only the single symptom instance the prior cycle named, while a structurally identical sibling instance of the same defect class survived in the same function/module -- confirmed 3 separate times in one small diff -- severity 7 | CROSS-CUTTING | SYNTHESIS
- [process] lib/importBackup.ts's incremental per-shape _version branching (built across 2 waves without ever being unified into one validated parse step) is accumulating its own independent boundary bugs each time a new branch is added, rather than converging toward a single normalization function -- severity 6 | CROSS-CUTTING | SYNTHESIS

## 2026-07-28 | Task: batch 12 -- tenth re-audit (8-agent: A, B, S, N, K, W, V, Red R) -- ran against Wave 23's committed state (commit 8106ae3)
- [edge-case] Task #481's symmetric-acceptance fix is asymmetric: _version:"0" (string) is accepted while _version:0 (number) is rejected by the falsy check; conversely _version:"-1" (string) is rejected by the digit regex while _version:-1 (number) is accepted with no lower-bound check -- execution-verified by Agent W via npx tsx, 6-way independent convergence (A, B, N, W, Red R via reasoning/execution, S assessed present but non-security), the strongest convergence signal in this batch's 10-cycle history -- severity 7, LIVE, Rule 23 violation recreated inside the very task (#479/#481) meant to close a prior instance of this class | lib/importBackup.ts:parseBackup:103-142 | NEW
- [edge-case] Task #479's numeric _version branch only ported the isFinite check from the string branch's fix, not the accompanying no-negative/no-fractional constraint -- execution-verified: parseBackup({_version:-1}), ({_version:1.5}), ({_version:-0.0001}) all return ok:true -- severity 7, Rule 23a violation (fix did not generalize to every member of the class) | lib/importBackup.ts:parseBackup:103-142 | NEW
- [tests] The two new Task #481 tests assert only r.ok toBe(true) and never diff against the numeric equivalent despite the test names claiming symmetry with it -- a stub returning ok:true unconditionally passes both (Rule 18 Deletion Test fails); confirmed independently by Auditors A, B, K, V, W -- severity 6 | tests/importBackup.test.ts:437-450 | NEW
- [error-handling] Task #482's fix proves the reject-branch logging is unreachable for every real production failure (zustand's hydrate() catch never rethrows), but the fix's own test only covers the getItem-throws case, never the migrate()-throws case -- which is NOT hypothetical, it's this app's own designed behavior (store/migrations.ts throws on missing migrations, all 3 stores register migrate) -- no failsafe timer exists on this path unlike the mount-time hydration path -- severity 7, Rule 8 + Rule 23c violation, resolves a genuine K-vs-B/W disagreement in favor of B/W | store/entitlementCrossTabSync.ts:createCrossTabSync:48-69,94-117 | NEW
- [code-quality] entitlementCrossTabSync's module header states this module is used by entitlementStore.ts ONLY, but the Task #482 comment justifies keeping dead code by citing future reuse with a non-Zustand rehydrate function -- the file's own header two paragraphs above disclaims that generality -- severity 5, Rule 20 violation | store/entitlementCrossTabSync.ts:header | NEW
- [code-quality] Task #482's doc comment claims the dead-branch finding is "confirmed with a live regression test," but the cited test exercises only one failure injection (getItem rejects) of several zustand's catch-all actually covers (migrate/merge/setItem-reject) -- severity 4 | store/entitlementCrossTabSync.ts:94-117 | NEW
- [async] triggerRehydrate's `instanceof Promise` check misclassifies any non-native thenable as synchronous, resetting the in-flight flag while real async work continues -- contradicts the doc comment's own claim of Zustand-independent generality -- latent, no current caller triggers it -- severity 4 | store/entitlementCrossTabSync.ts:triggerRehydrate:99 | NEW
- [data-loss] validatePack's new id.trim()==="" dedup guard excludes blank-id cards from duplicate detection entirely -- any number of cards sharing id:"" now produce zero duplicate-id errors from this loop, where two such cards previously would have been flagged -- severity 5 | scripts/validatePack.ts:dedup-loop | NEW
- [process] Recurring twin-branch-left-open pattern: symmetric-looking dual branches (string vs numeric _version checks) get fixed one at a time across cycles, each fix closing the branch under scrutiny while leaving its structural sibling open on a different guard set -- severity 7 | CROSS-CUTTING | SYNTHESIS
- [code-quality] Doc comments this wave repeatedly assert claims stronger than the cited test or the file's own other statements support (symmetry claim, confirmed-with-a-test claim, reuse-justification contradicting the same file's USED BY header) -- Rule 20 violated at the documentation layer even where code-level fixes land correctly -- severity 6 | CROSS-CUTTING | SYNTHESIS

## 2026-07-28 | Task: batch 12 -- eleventh re-audit (8-agent: A, B, S, N, K, W, V, Red R) -- ran against Wave 25 + Task #487's committed state (commit a5760ef)
- [data-loss] scripts/validatePack.ts hardened duplicate-CARD-id detection across 4 tasks (#468/#478/#480/#492) but has ZERO duplicate-UNIT-id detection anywhere -- hooks/useLangPack.ts:291 collapses units by id via Object.fromEntries, so two units sharing an id silently delete an entire unit's cards from the live app with zero CI signal -- severity 8, LIVE, Rule 23 recurring one abstraction level up from every prior instance in this batch | scripts/validatePack.ts:187-229 | NEW
- [error-handling] Task #488's own NEW "Accepted trade-off" paragraph (written specifically to fix cycle-10's overclaim) contains a fresh, empirically-false claim: a "newer app version writing from another tab" is claimed rare-but-covered, but live script execution against the real zustand+migrations.ts proves migrate() does NOT throw in that scenario -- it silently accepts unmigrated future-shaped state with zero signal, untested and undocumented as its own gap -- severity 9, Rule 23(b) violation recreated inside the very fix meant to close a prior instance, ESCALATE | store/entitlementCrossTabSync.ts:121-124 | NEW
- [error-handling] The migrate-throw diagnosability gap was logged as accepted debt twice (Tasks #488, #490) without applying either of two concrete fixes that exist: entitlementStore.ts's persist config could register onRehydrateStorage (empirically confirmed to receive the raw error), or migrations.ts's throw sites could console.error before throwing (mirroring the same file's own other migration guards) -- severity 8, Rule 8 violation, ESCALATE | store/migrations.ts:191,280,329 + store/entitlementStore.ts | NEW
- [tests] The Task #485 sweep test added in the SAME wave as Task #487 recreates the exact .ok-only comparison pattern #487 was created to eliminate two blocks earlier in the same file -- a bug making the string branch return a different error message than the numeric branch would not be caught -- severity 6, Rule 23 instance, recreated within the same wave | tests/importBackup.test.ts:541-550 | NEW
- [async] isThenable only verifies a callable .then exists, not that it will ever settle -- no timeout in triggerRehydrate means a never-settling thenable permanently disables cross-tab sync with zero log, unlike the sync-throw/async-reject paths which both log -- severity 7, latent (no current caller triggers it) | store/entitlementCrossTabSync.ts:triggerRehydrate | NEW
- [tests] F016's BigInt precision-loss assertion never calls parseBackup -- it's computed entirely from language primitives on a test-local literal string and is true unconditionally regardless of production code, a vacuous Rule 18 Deletion Test failure -- severity 6 | tests/importBackup.test.ts:F016-test | NEW
- [code-quality] entitlementCrossTabSync.ts's doc comment has grown to ~90-113 lines across 8-9 tasks by paragraph accretion rather than consolidation -- one paragraph exists solely to explain why an earlier version of the SAME comment was wrong, and this cycle's own fresh overclaim was introduced into the same growing block -- severity 7, 3-way convergence (Auditor A, Agent W, Red Agent R) | store/entitlementCrossTabSync.ts:header-comment | NEW
- [error-handling] The merge()/setItem() re-persist gap (Task #490/F006 residual) remains untested and was deferred a second time at low severity, understating a real (not rare) storage-quota-exceeded trigger against AGENTS.md's zero-tolerance framing for silently swallowed errors | store/entitlementStore.ts | ESCALATE
- [edge-case] isValidBackupVersionNumber's Number.isInteger(v) is dead code for the string branch -- the /^\d+$/ regex already fully determines integer-ness before the predicate is ever called, so the "both branches agree by construction, in one place" claim is architecturally false even though the outcome is correct -- severity 4 | lib/importBackup.ts:isValidBackupVersionNumber | NEW
- [edge-case] isValidBackupVersionNumber accepts huge finite doubles like 1e21 without the same overflow guard the string branch effectively gets -- untested/undocumented residual gap in the "shared predicate" symmetry claim, mirrors the F016/F017 category already investigated for the string branch | lib/importBackup.ts:isValidBackupVersionNumber | NEW
- [code-quality] scripts/validatePack.ts and lib/packTypes.ts's hasValidUnitsArray have been manually reconciled across 4 waves with no shared validation module -- kept in sync only by comment cross-references and mirrored tests | scripts/validatePack.ts, lib/packTypes.ts | NEW
- [process] This batch's dominant defect pattern across cycles 6-11: a fix applied exhaustively to one instance of a defect class while an identical instance one abstraction level away is left untouched -- card-ID dedup hardened 4x while unit-ID dedup was never added; #487's .ok-only fix eliminated in one test while a sweep test recreated it in the same wave; the migrate-throw gap narrowed by documentation precision twice without applying either identified fix -- severity 8 | CROSS-CUTTING | SYNTHESIS

## 2026-07-28 | Task: batch 19 -- first-ever audit (8-agent: A, B, S, N, K, W, V, Red R) -- ran against the batch's shipped state (commit def221e, "73/73 COMPLETE") -- verdict FAIL, 1 finding at severity 7
- [error-handling] StudyDoneScreen.tsx's exit-mandatory-mode "Done" button has no try/catch around exitMandatoryMode, unlike two sibling call sites in app/study/page.tsx that both catch and unconditionally navigate -- an ordinary transient OS window-call failure strands the user in a permanently locked (non-closable, non-minimizable, always-on-top) window with no retry path -- severity 7, LIVE, reachable via the ordinary post-session flow, 4-way convergence (A, B, S, RedR) | components/StudyDoneScreen.tsx:37 | NEW -- promoted to Task #506
- [correctness-regression] InterruptHandler's interrupt:fire listener re-subscribes on nearly every render (unmemoized computeDue + unselected full settings-store subscription + pathname/router in deps), creating a real unlisten/relisten gap where a genuine OS-fired interrupt can be dropped -- a regression introduced by this batch's own hook-facade extraction -- severity 6, bounded impact (independent 30s poll thread still fires the next scheduled interrupt), 2-way convergence (B, W) | components/InterruptHandler.tsx, hooks/useInterruptConfig.ts | NEW
- [concurrency] Cross-thread TOCTOU on last_triggered_secs between interrupt.rs's 30s poll and os_events.rs's independent 5s poll can double-fire interrupt:fire under default (all-triggers-enabled) settings -- outcome is an idempotent duplicate notification/navigation, not data loss -- severity 5, 3-way convergence (S, W, RedR) | src-tauri/src/interrupt.rs, src-tauri/src/os_events.rs | NEW
- [input-validation] No IPC-side range validation on interval_hours/idle_threshold_minutes/snooze minutes; interval_hours also has no JS-layer clamp unlike its idle-threshold sibling -- all sub-cases degrade gracefully (self-disable via an existing zero-guard) rather than crash or corrupt -- severity 5, 4-way convergence (S, W, N, RedR) | src-tauri/src/interrupt.rs | NEW
- [test-quality] The 11 new Rust unit tests in os_events.rs test hand-copied mirror helper functions, never the real production closure -- a corrupted real condition would not fail any of them; 2 of the 11 don't reference production code at all -- falsifies the "verified end-to-end" claim added to app/settings/page.test.tsx in this same batch -- severity 4, strongest convergence of the cycle (6/8: A, B, K, V, W, RedR) | src-tauri/src/os_events.rs | NEW
- [error-handling] Poisoned-mutex paths across 4 call sites in interrupt.rs/os_events.rs are handled inconsistently -- exit_mandatory_mode returns Ok(()) (false success) on poison, both background poll threads go permanently dark with zero log -- not reachable today since no fallible operation exists inside any lock() in this batch's code -- severity 4, 5-way convergence (A, B, N, S, RedR) | src-tauri/src/interrupt.rs, src-tauri/src/os_events.rs | NEW
- [process] First-ever audit of a batch that had already been marked 73/73 COMPLETE across two waves (2026-07-05, 2026-07-06) and one direct task (#225, 2026-07-06) with no batch-level gate ever run -- confirms this project's Batch Completion Gate rule (AGENTS.md) is necessary in practice, not just in principle: task-level completion did not surface the one live, severity-7 bug (F1), which required a full 8-agent adversarial pass to find -- severity n/a (process observation) | CROSS-CUTTING | SYNTHESIS

## 2026-07-28 | Task: batch 19 re-audit cycle 2 (3-agent: A, K, Red R) -- verifying Task #506's fix against F1 -- ran against commit 78541ea, verdict PASS
- [error-handling] F1 confirmed genuinely closed at the root cause by all 3 agents independently, 2 of whom performed a LIVE Deletion Test (checked out the pre-fix file, reran the new tests, confirmed both fail on the reverted code, restored and re-verified byte-identical) rather than reasoning about it statically -- exhaustive grep confirmed no other unprotected call site of onExitInterrupt/exitMandatoryMode/enterMandatoryMode exists in the codebase -- severity n/a, CLOSED | components/StudyDoneScreen.tsx:37-40, lib/tauriInterrupt.ts:74-84 | RESOLVED
- [error-handling] Red Agent R found one residual, pre-existing (not introduced by this fix) architectural gap: exit_mandatory_mode's `?`-chained Rust command can partially fail (one window-chrome call succeeds, a later one fails), and the JS-side catch+unconditional-navigate pattern (shared by 3 other call sites, not unique to this fix) masks that partial failure with no user-visible signal -- severity capped at debt-level per calibration since it strictly improves on the prior fully-stuck state this fix replaced, and is a pre-existing pattern shared by app/study/page.tsx:75,126 and InterruptHandler.tsx, not new | src-tauri/src/interrupt.rs:183-203 | NEW, low-severity, logged as debt only

## 2026-08-15 | Task: batch 23 -- first-ever audit (8-agent: A, B, S, K, W, V, Red R + naive-reader lane N) -- ran against commit f5f1305, verdict FAIL, severity 7
- [requirements] Desktop passive notification still announces the raw, un-floored computeDue() count while the session that opens now always holds >=6 cards -- the exact defect this batch fixed on the server but never touched on desktop; sibling test file actively pins the stale "1 card ready" text -- severity 6, 4-way independent convergence (A, B, W, Red R) | components/InterruptHandler.tsx:179 | NEW
- [code-quality] Two independent INTERRUPT_SESSION_FLOOR=6 literals (client + server) synced only by a comment instruction, no test asserts equality -- matches this project's own named "parallel value" stop-the-line anti-pattern -- severity 4 | lib/queue.ts:21 | NEW
- [tests] No seam test wires the real Batch 23 fill pipeline (page.tsx -> useStudySession -> srsStore) end-to-end; every layer unit-tested in isolation only -- severity 4 | app/study/page.tsx:73 | NEW
- [requirements] Task #533's never-completely-empty backstop bypasses strandedAcrossDays entirely -- contradicts BRAND.md's wrong-answer-rules table; pre-existing but newly more reachable given Batch 23's wider fill surface -- severity 6, 3-way convergence (A, V, Red R) | hooks/useStudySession.ts:159 | NEW
- [requirements] computeDue's flex-fallback can promise a new card during a stranded pause that the real session fill mechanism would refuse to honor -- severity 5, 2-way convergence (V, Red R) | hooks/useInterruptConfig.ts:60 | NEW
- [tests] Four compounding seam-test gaps around the interrupt fill pipeline, one masked only incidentally (a getStats stub that always returns non-zero due) rather than by design -- severity 4 | components/InterruptHandler.test.tsx:0 | NEW
- [requirements] Server push floor has no matching ceiling -- on any backlog day above INTERRUPT_SESSION_CAP(8), the push announces more cards than the client can ever deliver; empirically demonstrated by the batch's own shipped test -- severity 7 | supabase/functions/send-interrupt-notifications/dueEstimate.ts:89 | NEW
- [requirements] Server push overstates card count on a brand-new user's first interrupt -- "6 cards ready" announced while the real session (empty near-due pool, flex capped at 3) delivers at most 3 -- reachable on 100% of new Pro users' first interrupt -- severity 7 | supabase/functions/send-interrupt-notifications/dueEstimate.ts:89 | NEW
- [requirements] No daily ceiling on flex-introduced new cards across multiple same-day interrupts -- Number.MAX_SAFE_INTEGER disables the daily aggregate cap for the rest of the day, not just the current session; contradicts BRAND.md's "one new card per day at steady state" for exactly the new-user population this feature targets first -- severity 6, 2-way convergence (W, Red R) | hooks/useStudySession.ts:135 | NEW
- [requirements] "How many cards are ready" is now computed by 3+ independently-maintained code paths (desktop InterruptHandler.tsx, mobile dueEstimate.ts, actual client session size) with no shared source of truth or reconciling test -- Rule 22 whole-operation-consistency gap -- severity 7 | CROSS-CUTTING | SYNTHESIS
- [requirements] BRAND.md's stranded-pause rule is enforced in the normal-cap path and the interrupt fill while-loop but not in the #533 backstop or computeDue's flex-fallback -- same guard applied inconsistently across sibling call sites doing the same job -- severity 5 | CROSS-CUTTING | SYNTHESIS
- [tests] Deletion-test failures cluster in this batch's new test coverage -- four separate tests pass unchanged when the production logic they claim to prove is deleted or reverted -- severity 4 | CROSS-CUTTING | SYNTHESIS
- [code-quality] Multiple numeric constants central to this batch's contract (FLOOR=6 in two files, CAP=8 mirrored as a hardcoded test mock) kept in sync only by comment instructions, no test asserts cross-file or mock-vs-real equality -- severity 3 | CROSS-CUTTING | SYNTHESIS

## 2026-08-15 | Task: batch 23 -- re-audit round 2 (8-agent, post Wave 1+2 remediation) -- verdict FAIL, severity 8
- [edge-case] Daily flex-introduction ceiling (INTERRUPT_FLEX_DAILY_MAX=9) is checked once per session mount, not once per introduction -- the while loop can introduce 3 more cards against a single stale check, overshooting the ceiling by up to 2 in a day -- severity 6, 3-way independent convergence (W, V, N) | hooks/useStudySession.ts:mount-fill effect:142 | NEW
- [tests] No test can detect the daily-cap overshoot -- the canIntroduceNewCard mock is a pure function of its own arguments and never models a count that changes across loop iterations; INTERRUPT_FLEX_DAILY_MAX's actual value is never asserted anywhere -- severity 5, 3-way convergence (W, K, B) | hooks/useStudySession.test.ts:capUsedNotStranded mock:0 | NEW
- [requirements] Desktop passive notification floors but never caps its announced count -- the exact defect class fixed on the server in the same remediation wave was never propagated to this sibling client path -- severity 7, 4-way convergence (B, K, R, and implicitly A) | components/InterruptHandler.tsx:passive-notification body construction:183 | NEW
- [edge-case] getNewCards never checks introductions[card.id] unlike the real fill-path selector selectQualifyingNewCard, letting computeDue fire an interrupt promising content the real session will refuse to introduce -- severity 5, 2-way convergence (W, N with more precise root cause) | store/srsStore.ts:getNewCards:180 | NEW
- [async] markInterruptFired and recordInterruptGateEvent are both called unconditionally in the passive-notification branch, before the permission check that decides whether a notification is even shown -- a user who has denied permission gets the cross-device fired gate written and the cooldown clock advanced for a fire they never saw, silently suppressing future interrupts -- severity 8 | components/InterruptHandler.tsx:passive interrupt-fire branch:134 | NEW
- [async] app/study/page.tsx's mount-time useState(initialQueue) only consumes its initializer on true first mount and the mount-fill effect has an empty dependency array, so any component mounting while a pack is still loading (es sessions, specialty packs, cold push-tap launches) permanently freezes the queue empty -- the exact defect Task #552 was meant to close, left open by its own fix -- severity 7 | hooks/useStudySession.ts:mount-time introduce effect:83 | NEW
- [edge-case] app/study/page.tsx's Study more handler is gated only on !isGlobal, which is also true for interrupt sessions, letting a post-interrupt tap build an uncapped 15-card session with no INTERRUPT_SESSION_CAP slice applied -- severity 7 | app/study/page.tsx:onStudyMore handler:116 | NEW
- [code-quality] The #533/#538 never-empty backstop is dead code -- the while loop above it always already tried and failed with bit-identical arguments whenever the backstop's own guard is true, but the surrounding comment and docs section 10.4 both describe it as an active, distinct safeguard -- severity 3, 4-way convergence (B, K, W, N) | hooks/useStudySession.ts:never-empty backstop:180 | NEW
- [code-quality] Rule 23: this remediation round repeatedly fixed the reported symptom without re-deriving whether the surrounding code and its own comments still made sense afterward -- dead backstop code still described as active, a daily-ceiling comment claiming per-introduction enforcement it doesn't deliver, an ambiguous boolean never renamed after it started covering two causes -- severity 4 | CROSS-CUTTING | SYNTHESIS
- [requirements] The interrupt session-size ceiling was hardened at some call sites but never swept across every sibling surface that shows or builds a session -- the desktop notification, the Study more handler, and the daily flex check's cross-tab coordination were each found independently by a different auditor -- severity 5 | CROSS-CUTTING | SYNTHESIS
- [code-quality] Multiple project-level and inline doc claims about this batch's guarantees were never checked against the code they describe, and were only caught by an adversarial reviewer re-deriving the claim from source -- severity 3 | CROSS-CUTTING | SYNTHESIS
- [async] Two call sites advance shared, effectively-irreversible state before the check that determines whether real content will actually reach the user -- both defects are invisible in normal manual testing since the unhappy path they expose is rare in a developer's own environment but common for real users -- severity 6 | CROSS-CUTTING | SYNTHESIS

## 2026-08-15 | Task: batch 23 -- re-audit round 3 (8-agent, post Wave 3+4 remediation) -- verdict FAIL, severity 8
- [data-loss] SRS-store hydration race: the mount-fill effect gates readiness only on allCardMap (pack loading), ignoring SRS-store hydration -- a card introduced during the race window gets its IntroductionRecord silently discarded by zustand persist's default merge when hydration completes, permanently skipping the 22-day intensive cadence with no error anywhere -- severity 8, 3-way independent convergence via three different traces (effect-level, hook-level, zustand-source-level) | hooks/useStudySession.ts:mount-time introduce/fill effect:144 | NEW
- [error-handling] mountFillDoneRef is set to true before any fill logic executes, with no try/catch around the effect body -- a mid-fill exception leaves cards recorded as introduced today (consuming the daily cap) but never added to the visible queue, and the session never retries -- severity 6 | hooks/useStudySession.ts:mount-fill effect body:144 | NEW
- [requirements] the Study more handler's buildQueue call omits the getIntroductionDueCardIds parameter that initialQueue's construction passes -- a card mid-intensive-introduction-phase due today is silently excluded from a rebuilt Study more queue that the same unit's initial load would have included -- severity 6 | app/study/page.tsx:Study more handler vs initialQueue construction:129 | NEW
- [error-handling] an uncaught exception from the mount-fill effect body propagates out of the useEffect with no error boundary around the /study route, crashing to Next.js's default error UI instead of degrading gracefully -- severity 5 | hooks/useStudySession.ts:mount-fill effect body; /study route:144 | NEW
- [error-handling] a resolved sendNativeNotification promise only proves the IPC call succeeded, not that the OS displayed it -- markFired still advances the shared cross-device gate for the full interval even on a silent OS-level drop -- severity 5 | components/InterruptHandler.tsx:passive notification branch:0 | NEW
- [error-handling] the interrupt:fire listener has no top-level try/catch -- an exception in computeDue or readInterruptGateState (the latter's own doc comment claims it never throws) silently drops the interrupt fire with no trace -- severity 5 | components/InterruptHandler.tsx:interrupt:fire listener callback:102 | NEW
- [code-quality] getResumableSession silently mutates store state as a side effect of what its name presents as a pure getter, called during React's render phase from a useState lazy initializer and two useMemo bodies -- severity 5 | store/srsStore.ts:getResumableSession():0 | NEW
- [requirements] canIntroduceNewCard's true/false guarantee is not delivered equally to all three real call sites -- two sites get an atomic same-tick check-then-act guarantee, computeDue's estimate is consumed only much later when the user taps a notification, an unbounded gap during which the real state can change entirely -- severity 4 | hooks/useInterruptConfig.ts:computeDue vs useStudySession.ts's canIntroduceNewCard call sites:0 | NEW
- [async] within the mount-fill effect, some store reads are live (canIntroduceNewCard, introduceCard, getNearDueCards via get()) while selectQualifyingNewCard reads render-closure-captured parameters -- the same readiness-signal-inconsistency class as the hydration race, narrower blast radius -- severity 4 | hooks/useStudySession.ts:mount-fill effect (live get() reads vs closure-captured params):144 | NEW
- [tests] getNewCards' introductions-filter fix (Task #567) has zero direct or regression test coverage anywhere in the repo, confirmed by two independent auditors via exhaustive grep -- severity 4 | store/srsStore.ts:getNewCards (introductions filter, Task #567):0 | NEW
- [async] Rule 22/23: the mount-fill effect gates readiness on a single signal (allCardMap) while multiple independent async readiness signals actually govern correctness (SRS-store hydration, sync-triggered background patches, pack-load error states) -- Task #573 fixed this class for pack-loading alone and left the sibling signals unaddressed -- severity 8 | CROSS-CUTTING | SYNTHESIS
- [error-handling] side-effecting operations in the interrupt/study pipeline run with no try/catch and no error boundary -- when they throw or reject, already-committed state (daily cap, cross-device gate) diverges permanently from visible output with no log or signal -- severity 6 | CROSS-CUTTING | SYNTHESIS
- [requirements] sibling code paths that should behave identically diverge without documentation or tests catching it -- a queue-rebuild parameter omission and an unacknowledged per-caller guarantee asymmetry, both found only by manual cross-file reading -- severity 5 | CROSS-CUTTING | SYNTHESIS
- [tests] verification claims in test comments and completion reports overstate actual coverage -- a false sibling-test citation and a completion report's blanket Deletion Test claim contradicting its own weaker per-fix claim -- severity 4 | CROSS-CUTTING | SYNTHESIS

## 2026-08-15 | Task: batch 23 -- re-audit round 4 (after Wave 5 remediation) -- verdict FAIL, severity 9
- [data-loss] lib/storage.ts's useIsHydrated failsafe-plus-late-merge reconciliation combined with hooks/useStudySession.ts's Task #587 hydration gate: when the 3s hydration failsafe fires before real Tauri file-store hydration completes, a card introduced during that window gets a fresh record written against an empty pre-hydration map; when real hydration later merges and the late-merge reconciliation tries to restore the just-written record, Zustand's shallow setState() merge replaces the ENTIRE introductions map with the single-record pre-hydration snapshot, silently discarding every other real introduction record the user has ever accumulated -- severity 9, resolved from a direct contradiction between two independent auditors (A/V vs W) via line-by-line re-trace of Zustand's subscribe/onFinishHydration/setState semantics | lib/storage.ts:useIsHydrated (failsafe + late-merge reconciliation):170 | NEW
- [requirements] hooks/useStudySession.ts's normal daily-cap introduction path (`if (canIntroduceNewCard(today)) introduceNext();`) runs unconditionally for every session type including isInterrupt, with no INTERRUPT_SESSION_CAP awareness -- an interrupt session already at the 8-card cap can grow to 9 via this path, contradicting both the client and server notification clamps that announce "at most 8" -- severity 7 | hooks/useStudySession.ts:mount-fill effect (normal daily-cap path):231 | NEW
- [async] resumeDecision's useState lazy initializer and resumedQueue/resumedPos useMemos read getResumableSession() with no hydrated guard, unlike the mount-fill effect's own guard -- a useState lazy initializer runs once only, so a genuine mid-mandatory-interrupt resumable session can be silently missed on a slow Tauri cold start, restarting the session from scratch instead of prompting to resume -- severity 7, 3-way convergence (A, K, V) | hooks/useStudySession.ts:resumeDecision (useState lazy initializer) / resumedQueue / resumedPos:58 | NEW
- [requirements] store/srsStore.ts's peekResumableSession/clearExpiredResumableSession (Task #597's stated fix for getResumableSession's render-phase mutation) have zero production callers -- the unsafe original remains live at all 4 real call sites including one (app/study/page.tsx:103-104) the fix's own diagnostic comment doesn't mention -- Task #597 marked COMPLETE with its own acceptance checkbox unchecked -- severity 6 | store/srsStore.ts:peekResumableSession / clearExpiredResumableSession (orphaned):88 | NEW
- [code-quality] hooks/useInterruptConfig.ts's computeDue and hooks/useStudySession.ts's mount-fill effect hand-duplicate the identical 3-tier content-availability decision across two files with no shared function -- the code's own comments admit this exact duplication already caused two prior divergence bugs (Task #523, #539); this batch added a third parallel tier to both copies independently, and is the direct root cause of the daily-cap CAP-overshoot finding above -- severity 5 | hooks/useInterruptConfig.ts:computeDue / mount-fill effect (duplicated decision logic):52 | NEW
- [async] up to 3 sequential introduceCard() calls within one mount-fill effect pass each trigger an independent async Tauri persist write; if they resolve out of order a stale earlier snapshot could silently overwrite a newer one on disk -- distinct from the hydration-failsafe finding, unconfirmed occurrence -- severity 5 | hooks/useStudySession.ts:mount-fill effect (multiple introduceCard persist writes):173 | NEW
- [edge-case] components/InterruptHandler.tsx's mandatory-path comment claims the mandatory branch "always shows content" so markFired() is called unconditionally -- contradicted by the project's own docs (session floor is a target not a guarantee, can leave a session empty); a real sequence can advance the shared cross-device gate clock for a fire the user never actually saw content from -- severity 5 | components/InterruptHandler.tsx:mandatory-mode branch:167 | NEW
- [requirements] removing the server's zero-card skip means a genuinely fully-caught-up Pro user now receives a recurring, contentless "Cards ready" push every interval indefinitely -- contradicts BRAND.md's stress-free/no-pressure principle -- severity 5 | supabase/functions/send-interrupt-notifications:dispatch (zero-card skip removed, server side):1 | NEW
- [tests] tests/seam_studyLoop.test.ts's two "page.tsx wiring" tests hand-copy the real page.tsx slice expression inline rather than exercising the real source -- Rule 18 pseudocode, despite docstrings claiming to reconstruct the exact page-level sequence -- severity 4 | tests/seam_studyLoop.test.ts:page.tsx wiring tests:211 | NEW
- [code-quality] FLOOR/CAP constants duplicated Node/Deno, guarded only by a test-suite assertion, not a deploy-time guard -- silent-drift risk if the edge function deploys independently of a full test run -- severity 4 | supabase/functions/send-interrupt-notifications/dueEstimate.ts:FLOOR/CAP constants (Node/Deno duplication):120 | NEW
- [async] computeDue reads live SRS-store state with no hydration gate, called from InterruptHandler.tsx's interrupt:fire listener which has no useIsHydrated check anywhere -- can decide fire/no-fire against pre-hydration empty data for one cycle, bounded impact since the next 90-minute cycle recomputes against real data -- severity 4 | hooks/useInterruptConfig.ts:computeDue:52 | NEW
- [code-quality] Task #587's own new comment overclaims what it fixed ("never runs against pre-hydration defaults... would later silently overwrite") -- Rule 23b: the code path does run against pre-hydration defaults and the reconciliation does silently overwrite, just not the specific key the comment focuses on | hooks/useStudySession.ts:mount-fill effect (Task #587 comment):170 | NEW
- [code-quality] app/study/page.tsx is 181 lines against CLAUDE.md's documented 150-line cap for this route -- Rule 1, no compensating extraction applied this batch | app/study/page.tsx:module-level (whole file):1 | NEW
- [code-quality] store/srsStore.ts is 405 lines against the 400-line services cap -- Rule 1, 4-way auditor convergence (B, K, W, and Agent C's own count) | store/srsStore.ts:module-level (whole file):1 | NEW
- [error-handling] mountFillStartedRef.current=true, setQueue(initialQueue), and the sessionIds/added/introducedIds construction all execute BEFORE the try block Tasks #592/#593 added specifically to contain a fill-pass throw -- a throw from any of these three statements would still escape uncaught, the exact failure mode #592/#593 claims to prevent | hooks/useStudySession.ts:mount-fill effect:174 | NEW
- [performance] getNearDueCards(Number.MAX_SAFE_INTEGER) forces an O(n log n) filter+sort over the entire ~30K-card catalog synchronously inside a mount effect on every interrupt-session open -- self-documented as "not yet measured" | hooks/useStudySession.ts:mount-fill effect (getNearDueCards call):200 | NEW
- [performance] removing dispatch.ts's zero-estimate skip means every gated-eligible token proceeds through claimToken+send with no new throttle -- self-documented as "not yet measured against higher real volume" | supabase/functions/send-interrupt-notifications/dispatch.ts:dispatch (zero-estimate skip removed):1 | NEW
- [edge-case] if getNearDueCards throws after the flex loop already introduced 1-3 new cards, those introductions are permanently recorded (consuming the daily flex ceiling) even though the visible queue may end up smaller than the flex effort spent | hooks/useStudySession.ts:mount-fill effect (flex loop + getNearDueCards throw):200 | NEW
- [code-quality] docs/INTERRUPT_ARCHITECTURE.md SS10.2 still documents the mount-fill guard as mountFillDoneRef / deps [allCardMap] -- the same diff's own Wave 5 shipped code renamed it to mountFillStartedRef / deps [allCardMap, hydrated] with no doc update | docs/INTERRUPT_ARCHITECTURE.md:SS10.2 (mount-fill guard documentation):357 | NEW
- [async] Rule 22a: hydration-timing races recur across three independent code paths in the same batch area (the mount-fill effect's own reconciliation defect, the resumeDecision sibling gap, and computeDue's ungated read) -- Task #587 hardened exactly one call site and its own reconciliation mechanism itself contains a shallow-merge data-loss defect; the structurally identical resumeDecision sibling was never brought along -- severity 8 | CROSS-CUTTING | SYNTHESIS
- [requirements] Rule 20: multiple COMPLETE-marked remediation tasks this batch do not match wired reality -- Task #587's own comment overclaims what it fixed, Task #597's replacement functions were never wired to any real caller despite being marked COMPLETE with an unchecked acceptance box, and the interrupt-cap invariant is asserted as fact in docs/tests without being true on the real path -- severity 6 | CROSS-CUTTING | SYNTHESIS
- [code-quality] the duplicated content-availability decision logic between computeDue and the mount-fill effect is the direct root cause of the interrupt-session cap-overshoot finding -- the daily-cap path in one copy never inherited the CAP-awareness present in the other copy's flex/near-due tiers -- severity 6 | CROSS-CUTTING | SYNTHESIS

## 2026-08-16 | Task: batch 23 -- re-audit round 5 (after Waves 6+7 remediation) -- verdict FAIL, severity 8
- [data-loss] lib/storage.ts's map-aware reconciliation (round-4's fix for the severity-9 bug) only generalizes to NEW sub-keys added during the failsafe window, not COLLIDING sub-keys (same card id present in both the live write and real persisted data, differing content) -- the live value unconditionally wins, silently discarding real persisted history for that one entry. Rule 23a: the fix didn't generalize to every member of the defect class. Resolved from a direct contradiction between two auditors (K vs W) via the orchestrator's own line-by-line re-trace of the subDiff computation -- severity 8 | lib/storage.ts:createPlatformStorage (late-hydration reconciliation, onFinishHydration handler):248 | NEW
- [async] app/study/page.tsx still gates its entire interactive page -- including handleRate/commitSession and recordIntroductionResult, both real persisted writes -- on the LENIENT useIsHydrated rather than the strict signal Task #606 introduced specifically for write-performing consumers; this is the concrete reachability path that puts a card rating inside the exact failsafe window the reconciliation bug above requires -- severity 7 | app/study/page.tsx:StudyPage (component body, hydration gate):60 | NEW
- [requirements] the mount-fill effect has no resumeDecision awareness -- it performs real introduceCard writes and queue changes before the resume-prompt UI can intercept, and those writes are never undone when the fill's queue gets overwritten by the apply-resume effect -- silently burns real daily/flex budget on introductions the user never saw, reachable via any incomplete prior interrupt session (unitId is always "" for interrupt mode, so any prior incomplete one matches) -- severity 7 | hooks/useStudySession.ts:mount-fill effect:200 | NEW
- [async] InterruptHandler.tsx's configSeqRef staleness guard only protects the catch/reject path, not a stale call resolving SUCCESSFULLY after a newer one -- a real rapid-toggle race the comment claims to guard against but the code doesn't fully cover -- severity 6 | components/InterruptHandler.tsx:updateInterruptConfig effect / configSeqRef guard:73 | NEW
- [edge-case] the apply-resume effect has no branch for resumeDecision==="accepted" with a null resumedQueue (a narrow but real race where the resumable session expires between the pending and accepted reads) -- silent no-op resume, sessionStartedAtRef stuck at epoch 0 -- severity 6 | hooks/useStudySession.ts:apply-resume effect:482 | NEW
- [async] the interrupt:fire listener has no re-entrancy guard; Rust's emit_interrupt is documented fire-and-forget with no queueing -- two rapid-succession fires could both pass the early-return guards and both proceed, single-agent finding -- severity 5 | components/InterruptHandler.tsx:interrupt:fire listener:102 | NEW
- [tests] useIsHydratedStrict -- the load-bearing fix for round 4's severity-9 bug -- has zero test coverage proving its one differentiating behavior (never resolving via the failsafe); every existing hydration-gate test manipulates persist.hasHydrated() directly and would not catch a silent revert to the lenient signal -- severity 4 | lib/storage.ts:useIsHydratedStrict:175 | NEW
- [tests] two of tests/storage.test.ts's three new map-aware-reconciliation regression tests never actually exercise the map-shaped field during the failsafe window and would still pass with the whole new branch deleted -- notably none of the three tests exercises the sub-key collision scenario, so this suite would not have caught the finding above either -- severity 4 | tests/storage.test.ts:map-aware reconciliation regression tests:474 | NEW
- [code-quality] hooks/useStudySession.ts is 546 lines against the 400-line services cap -- unlike its two siblings in this same batch (app/study/page.tsx, store/srsStore.ts), both already extracted under the same cap this batch itself established as precedent -- severity 3 | hooks/useStudySession.ts:module-level (whole file):1 | NEW
- [code-quality] docs/INTERRUPT_ARCHITECTURE.md section 10 is stale for Wave 7's CAP guard and Wave 6's shared-predicate extraction -- a repeat instance of a named recurring failure class in this exact file, one round after a prior staleness issue was fixed -- severity 3 | docs/INTERRUPT_ARCHITECTURE.md:section 10 (file-summary table and cross-file documentation):10 | NEW
- [tests] two tests in hooks/useInterruptConfig.test.ts pass the Deletion Test negatively -- both would produce the identical asserted result even if the guard clause each test's name claims to prove were deleted -- severity 3 | hooks/useInterruptConfig.test.ts:computeDue tests:217 | NEW
- [tests] two of the three Task #617 CAP-guard regression tests pass under realistic narrower mutations of the guard clause -- the underlying guard itself is independently confirmed correct by four agents, so this is a pure test-quality gap with no live defect behind it | hooks/useStudySession.test.ts:CAP-guard regression tests:632 | NEW
- [async] dispatch.ts reports a zero-estimate send as successful even when the compensating 24h-gate write fails, silently degrading the "at most once per day" guarantee -- single-agent finding, requires a write failure to trigger, already a consciously accepted trade-off pattern in this file | supabase/functions/send-interrupt-notifications/dispatch.ts:dispatch send handler:101 | NEW
- [requirements] a negative estimate.cardCount in dueEstimate.ts still gets clamped and announced as "6 cards ready" rather than surfaced as an error state -- documented not reachable today | supabase/functions/send-interrupt-notifications/dueEstimate.ts:buildNotificationPayload:166 | NEW
- [code-quality] Task #619's accepted-debt async write-ordering risk lives only in an inline comment with no entry in a durable debt ledger | hooks/useStudySession.ts:mount-fill effect (Task #619 comment):291 | NEW
- [code-quality] Task #619's own comment overstates the maximum possible introduceCard call count in one pass (says 4, actual max is 3 since introducedIds is shared) -- Rule 23b, a fix/comment must not itself overclaim | hooks/useStudySession.ts:mount-fill effect (Task #619 comment):291 | NEW
- [data-loss] Rule 23a/22: the round-4 fix for the severity-9 hydration-race bug is incomplete on two independent, compounding axes -- the reconciliation logic doesn't generalize to colliding sub-keys, and a real interactive write path (page.tsx) was never migrated to the strict signal the fix's own doc comment says all writers must use -- together these mean a user rating an already-loaded card during a slow cold start can still lose that card's real persisted history -- severity 8 | CROSS-CUTTING | SYNTHESIS
- [tests] a recurring pattern across this round's own test additions: multiple new regression tests written specifically to prove a remediation (strict-hydration, CAP guard, map-aware reconciliation, flex/near-due fallback) fail the Deletion Test -- spans four unrelated subsystems, indicating a procedural gap (tests written to document intent rather than falsify a wrong implementation) rather than one file's issue -- severity 4 | CROSS-CUTTING | SYNTHESIS

## 2026-08-17 | Task: batch 23 -- re-audit round 6 (after Wave 8 remediation) -- verdict FAIL, severity 8
- [requirements] Wave 8's own Task #629 fix silently defeats the interrupt-session floor when a user declines a stale resume prompt -- the mount-fill effect's dependency array [allCardMap, hydrated] excludes resumeDecision, so once the user declines (or falls into the accepted-but-expired branch) nothing ever re-triggers the fill pass the code's own comment promises will retry -- reachable via the ordinary snooze-then-decline flow since interrupt sessions always key on unitId="". 5-way independent convergence (A, B, K, Red R, W); the fix's own new comment and the just-rewritten architecture doc both repeat the same false "regardless of accept/decline" claim -- severity 8, Rule 23(b) violation | hooks/useStudySession.ts:mount-fill useEffect (lines 155-307) / apply-resume useEffect (lines 312-339):176 | NEW
- [edge-case] app/study/page.tsx's new hydratedStrict gate (Task #628) has no escape hatch if real hydration genuinely never completes -- the route now shows "Loading..." forever with no timeout, retry, or user-visible error on a genuine storage failure, reversing HYDRATION_FAILSAFE_MS's explicit "never leave the user stuck" design goal for this one route; the trade-off isn't recorded as an open risk anywhere for Max to knowingly accept -- 3-way convergence (W, A, K) -- severity 6 | app/study/page.tsx:module-level render gate:69 | NEW
- [tests] Task #634's own new regression test has 4 of its 5 assertions matching regardless of whether the fix branch executes -- only the clearActiveSession assertion actually falsifies; the test's own comment claims to prove sessionStartedAtRef was reset, which this construction doesn't prove | hooks/useStudySession.test.ts:Task #634 fallback-from-null-resumedQueue test:0 | NEW
- [code-quality] the collision-detection log in lib/storage.ts's reconciliation fires on reference inequality, not necessarily real content difference -- the correct data-safety branch still runs either way, but the log fires far more often than its message implies | lib/storage.ts:collision-detection log:267 | NEW
- [data-loss] the collision fix's triple isPlainObject guard falls through to the old blanket-replace if any of snapshotAtExpiry/preMerge/postMerge is transiently not a plain object -- reintroduces the whole-field-replace defect class for that scenario, not reachable today (introductions always defaults to {} synchronously) | lib/storage.ts:per-subkey merge guard:248 | NEW
- [requirements] docs/INTERRUPT_ARCHITECTURE.md section 10.1 misattributes the INTERRUPT_SESSION_CAP slice to app/study/page.tsx when it actually lives in hooks/useStudyQueueSetup.ts (extracted by the pre-existing Task #612) -- a fresh recurrence of the same doc-drift class this file has already been fixed for twice (Tasks #625, #631) | docs/INTERRUPT_ARCHITECTURE.md:section 10.1:0 | NEW
- [async] dispatch.ts's bounded retry for recordGateFired can append duplicate rows to the append-only interrupt_gate_events table on a lost-response (not lost-write) failure -- confirmed functionally harmless (reader takes most recent effective_until), unbounded silent accumulation is a storage-growth/audit-cleanliness concern | supabase/functions/send-interrupt-notifications/dispatch.ts:recordGateFired retry:0 | NEW
- [error-handling] interruptFireInFlightRef is a per-component-instance ref, not module-scoped -- a theoretical unmount-mid-processing race, single-agent finding, not a live reachable path today | components/InterruptHandler.tsx:InterruptHandlerCore interrupt:fire callback:0 | NEW
- [tests] Task #633's own config-race test still passes with the new success-path check deleted, since updateInterruptConfig's success branch has no further observable action today -- the production code's own comment already discloses this honestly, so no hidden overclaim, just a test that can't currently prove its target | components/InterruptHandler.test.tsx:Task #633 config-race test:0 | NEW
- [requirements] Rule 23(b): two findings this round (the resume-decline floor-starvation gap and the hydration-gate escape-hatch gap) share a root pattern recurring across this batch's remediation history -- a fix scoped to the specific failure the previous audit named, without re-verifying it holds across the full state-transition space it claims to cover -- severity 8 | CROSS-CUTTING | SYNTHESIS

## 2026-08-17 | Task: batch 23 -- re-audit round 7 (after round-6 remediation) -- verdict FAIL (verified, then fixed live during the same round), severity 8
- [edge-case] app/study/page.tsx checked queue.length===0 (-> StudyEmptyQueue) BEFORE resumeDecision==="pending" (-> StudyResumePrompt) -- the mount-fill effect deliberately skips filling while a resumable session is pending (Task #629), so a caught-up day with a pending resumable session left queue empty AND resumeDecision pending at once, permanently hiding the resume prompt behind "Nothing ready"; StudyEmptyQueue's Home button never clears the stale session, so the trap persisted up to SESSION_EXPIRY_MS (24h) -- found by Agent W, independently verified and FIXED same round (checks reordered, regression test added) -- severity 8 | app/study/page.tsx:StudyInner:97 | NEW -- FIXED
- [requirements] hooks/useHydrationStuck.ts only monitored hydratedStrict, but its real caller gates on a 3-way OR (!hydrated || packLoading || !hydratedStrict) -- a stuck packLoading with resolved hydratedStrict either hung forever with no retry, or showed a stale "stuck" retry screen after hydratedStrict resolved -- 2-way convergence (Agent W, Red Agent R) -- FIXED same round (hook generalized to a `blocked` composite parameter, call site passes the real gate condition) -- severity 7 | hooks/useHydrationStuck.ts:useHydrationStuck:39 | NEW -- FIXED
- [tests] hooks/useStudySession.test.ts's rewritten Task #634/#645 test still had one non-falsifying assertion (sessionCorrect) -- the mutation step (handleRate("again")) never increments sessionCorrect, so the reset assertion passed whether or not the fix branch ran -- 2-way convergence (Agent K, Agent V), same defect class the test's own #645 fix was supposed to have closed for all four fields -- FIXED same round (added a leading handleRate("good") to genuinely mutate sessionCorrect) -- severity 6 | hooks/useStudySession.test.ts:resets session state test:284 | NEW -- FIXED
- [tests] app/study/page.test.tsx had zero coverage of the resume-prompt render path -- no test ever set resumeDecision="pending", the exact gap that let the queue/resumeDecision render-order bug survive through Task #629 and round-6's own remediation -- FIXED same round (new test added) -- severity 6 | app/study/page.test.tsx:module-level test suite:301 | NEW -- FIXED
- [requirements] docs/INTERRUPT_ARCHITECTURE.md section 10.2's Task #629 narrative went stale the moment Task #643 shipped in this same round -- still described queue changes as "discarded... on decline," no longer true -- FIXED same round | docs/INTERRUPT_ARCHITECTURE.md:section 10.2:351 | NEW -- FIXED
- [requirements] CLAUDE.md line 32 still claimed app/study/page.tsx caps the interrupt queue -- fourth recurrence of the doc-drift class Task #648/F006 was supposed to have closed (after #625, #631, and F006 itself), missed because F006 never checked CLAUDE.md -- FIXED same round -- severity 4 | CLAUDE.md:hooks/useStudySession.ts architecture bullet:32 | NEW -- FIXED
- [tests] components/StudyHydrationStuck.tsx (new this round) had no co-located test, breaking this codebase's 100%-consistent convention and Rule 14 -- its one interactive element (window.location.reload()) had zero coverage anywhere -- 2-way convergence (Agent A, Agent B) -- FIXED same round (new test file added) -- severity 6 | components/StudyHydrationStuck.tsx:StudyHydrationStuck:9 | NEW -- FIXED
- [code-quality] hooks/useStudySession.ts crossed the 400-line Rule 1 cap (416 lines) as a side effect of this round's own runFillPass extraction -- third recurrence of the file-size live_pattern -- FIXED same round (comments condensed, 416->399/400) -- severity 5 | hooks/useStudySession.ts:module-level:1 | NEW -- FIXED
- [requirements] docs/INTERRUPT_ARCHITECTURE.md's "loading screen now stays up indefinitely" claim (Task #628) went stale the moment this same round's own Task #644 fix shipped a bounded retry screen -- third stale-reference instance in the same file within the same commit that fixed two other instances via F006 -- FIXED same round -- severity 5 | docs/INTERRUPT_ARCHITECTURE.md:section 10.2:441 | NEW -- FIXED
- [edge-case] hooks/useStudySession.ts's "accepted"+resumedQueue branch never claims mountFillStartedRef, asymmetric with the decline branch's ref claim added by Task #643 -- could theoretically let a stray runFillPass() run against an already-finished resumed session -- 2-way convergence (Agent W, Agent V) -- not reachable today (requires a specialty pack to ship ready:true; none are), capped and logged to debt.md | hooks/useStudySession.ts:apply-resume effect accepted branch:327 | NEW
- [async] supabase/functions/send-interrupt-notifications/dispatch.ts's Task #649 retry has no backoff and was only reasoned about for a single-token blip -- under a sustained outage every token pays 3x cost -- 2-way convergence (Agent W, Red Agent R) -- not reachable under normal conditions, capped and logged to debt.md | supabase/functions/send-interrupt-notifications/dispatch.ts:sendAndRecord:135 | NEW
- [requirements] Rule 23a synthesis: five findings this round independently show the same systemic gap -- a fix closes the single instance a prior finding named, but leaves an identical sibling instance of the same defect class untouched or self-contradicted within the very same commit (CLAUDE.md missed by F006's doc-drift fix; docs/INTERRUPT_ARCHITECTURE.md's own narrative left stale by the fix it describes, twice; the decline-branch ref-claim fixed but not its accept-branch sibling; a new defensive-code addition contradicting the round's own "don't defend unreachable paths" principle elsewhere in the same commit) -- severity 5 | CROSS-CUTTING | SYNTHESIS

## 2026-08-17 | Task: batch 23 -- re-audit round 8 (8-agent, post round-7 remediation) -- verdict FAIL (verified, then fixed live during the same round), severity 7
- [requirements] hooks/useHydrationStuck.ts's HYDRATION_STUCK_TIMEOUT_MS (15000ms, tuned for local storage hydration stalls) was widened in scope by round-7's own F012 fix to also cover packLoading, a network fetch of an 8.6MB pack file -- an ordinary (not even "slow") 3G connection can legitimately exceed 15s downloading that much data, so the round-7 fix's own retry screen would false-positive (and its Retry button's reload() would restart the identical slow fetch) for real slow-connection users -- Red Agent R CHAOS finding, reachable today with normal (if unlucky) network conditions, not hypothetical -- FIXED same round (widened to 45000ms with documented reasoning) -- severity 7 | hooks/useHydrationStuck.ts:HYDRATION_STUCK_TIMEOUT_MS:37 | NEW -- FIXED
- [tests] round-7's own F012 fix (useHydrationStuck generalized to a composite blocked parameter) had a hook-level unit test proving the HOOK tracks whatever boolean it's given, but nothing proved the PAGE-LEVEL WIRING -- Agent B confirmed concretely by reverting app/study/page.tsx's wiring to the old, narrower useHydrationStuck(!hydratedStrict) call and finding the full 2024-test suite stayed green -- exactly the "fix passes its own new test but leaves the original guarantee provable-broken via an untouched adjacent surface" pattern this team has been flagged for repeatedly -- FIXED same round (new page-level regression test added, Deletion-Tested) -- severity 6 | app/study/page.test.tsx:hydration gating describe block:560 | NEW -- FIXED
- [documentation] tests/pushDueEstimate.test.ts:115's comment still attributed the INTERRUPT_SESSION_CAP slice to app/study/page.tsx -- a FIFTH recurrence of the doc-drift class first flagged at Task #648/F006, missed by round-7's own F002 fix (which only checked CLAUDE.md) -- Agent B finding -- FIXED same round -- severity 3 | tests/pushDueEstimate.test.ts:115 | NEW -- FIXED
- [documentation] docs/INTERRUPT_ARCHITECTURE.md's detailed §10.2 narrative (which self-mandates staying current with every mount-fill-effect-touching change) never received a matching update for round-7's own F009 fix, even though the doc's terser CLAUDE.md sibling did -- a fourth instance of the exact "terse doc fixed, detailed doc missed" pattern round-7's own SYNTHESIS_PATTERNS (SP001) named -- Agent W finding -- FIXED same round (new paragraph added) -- severity 4 | docs/INTERRUPT_ARCHITECTURE.md:section 10.2:378 | NEW -- FIXED
- [requirements] .autocode/debt.md's F011 entry (logged round 7) described an internally impossible state-transition chain -- required resumeDecision to be simultaneously "accepted" and later still "pending" for its own named consequence to occur, which the actual one-shot state machine cannot produce -- Agent V finding, caught via direct state-machine tracing rather than trusting the entry's own narrative -- the underlying asymmetry (accepted branch never claims mountFillStartedRef) IS real; only the reachability chain justifying its debt disposition was wrong -- FIXED same round (debt.md entry corrected with the coherent consequence) -- severity 4 | .autocode/debt.md:F011 entry | NEW -- FIXED
- [documentation] CLAUDE.md's Notable Modules list had no bullet for hooks/useHydrationStuck.ts or components/StudyHydrationStuck.tsx despite comparably-important same-era extractions (e.g. useSnoozeAndExit.ts) receiving one -- useHydrationStuck.ts was itself the subject of a severity-7 round-7 finding -- Agent W finding -- FIXED same round -- severity 2 | CLAUDE.md:Notable modules list:37 | NEW -- FIXED
- [async] Red Agent R DECAY: an unchecked coupling now exists between production code comments (hooks/useStudySession.ts) and specific prose entries in .autocode/debt.md -- nothing mechanically verifies the referenced entry still exists or still says what the comment implies -- accepted as a known limitation of the existing debt.md convention, logged to debt.md rather than tooled around | hooks/useStudySession.ts:apply-resume effect else-branch:347 | NEW
- [performance] Red Agent R DECAY: HYDRATION_STUCK_TIMEOUT_MS is now a single hardcoded constant implicitly load-bearing for two diverging-growth-curve latency domains (local storage vs. network pack-fetch proportional to a still-growing curriculum) -- the round-8 widening fix will itself need revisiting as the pack grows further -- logged to debt.md, not urgent today | hooks/useHydrationStuck.ts:HYDRATION_STUCK_TIMEOUT_MS:37 | NEW

## 2026-08-17 | Task: batch 23 -- re-audit round 9 (8-agent, post round-8 remediation) -- verdict FAIL (verified, then fixed live during the same round), severity 7
- [requirements] round-8's own justification for widening HYDRATION_STUCK_TIMEOUT_MS (15000->45000ms) was factually wrong on two independent, verified counts: (1) `it`, the only ready:true language, is bundled statically at build time and never fetched over the network at runtime at all; (2) every real pack fetch is hard-bounded to FETCH_TIMEOUT_MS (20000ms) via an AbortController + independent backstop, so packLoading can never legitimately stay true past ~20s regardless of connection speed -- the widening was structurally incapable of ever engaging for the scenario it claimed to fix, while tripling the wait for the one failure mode still reachable through it (genuine storage-hydration stalls) -- 3-way convergence (Red Agent R CHAOS, Agent B structural fetch-bound trace, Agent A static-pack trace) -- REVERTED to 15000ms same round, with corrected reasoning documented -- severity 7 | hooks/useHydrationStuck.ts:HYDRATION_STUCK_TIMEOUT_MS:48 | NEW -- FIXED
- [tests] none of round-8's new/updated tests actually pin HYDRATION_STUCK_TIMEOUT_MS's specific value -- both page-level tests only assert the END STATE after advancing a hardcoded 45000ms, and the hook's own 5 unit tests all pass an explicit 15000ms override, never exercising the real default -- verified via live Deletion Test (reverting the constant to 15000ms left all 24 affected tests green) -- 4-way convergence (Agent A, Agent W, Agent K, Agent V) and explicitly the "SPECIFIC PATTERN TO CHECK" this round's own live_patterns.md brief named in advance -- FIXED same round (new tests added importing and asserting the real constant) -- severity 6 | hooks/useHydrationStuck.test.ts:module-level:1 | NEW -- FIXED
- [tests] the orchestrator's own first attempt at closing the above finding reproduced the identical defect class it was fixing: a test that derives its timer boundaries FROM the imported constant (advanceTimersByTime(TIMEOUT-1)/(TIMEOUT)) is tautological -- it moves with the constant and cannot catch a regression of the constant's own VALUE. Caught via the orchestrator's own live Deletion Test (simulating a regression left the "fixed" test green) before commit, not by a separate agent -- corrected by adding an explicit expect(HYDRATION_STUCK_TIMEOUT_MS).toBe(15000) assertion -- a real-time instance of Rule 23(b) self-correction, not a bug that shipped | hooks/useHydrationStuck.test.ts:uses HYDRATION_STUCK_TIMEOUT_MS as its default:97 | NEW -- FIXED
- [documentation] a 6th+ recurrence of the "app/study/page.tsx caps/slices the queue" doc-drift class, this time found as 4 MORE live sibling instances (components/InterruptHandler.tsx, InterruptHandler.test.tsx, hooks/useStudySession.test.ts x2) that round-8's own "swept the corpus, found none" claim missed entirely -- the round-8 verification claim was itself a fresh, empirically-false statement, matching this team's own historically-worst pattern (error-handling: a fix's new disposition comment contains a false claim) -- Agent A finding -- FIXED same round (all 4 corrected) | components/InterruptHandler.tsx:256 | NEW -- FIXED
- [edge-case] hooks/useStudySession.ts's resumedQueue (filters out any queueIds entry no longer in allCardMap) can desync from resumedPos (the raw, unfiltered saved index) if a card was deprecated between session-save and resume (within the 24h SESSION_EXPIRY_MS window) -- can silently show the wrong card or end a resumed session early with mismatched displayed stats, with StudyResumePrompt's own "Card X of Y" text built from the unfiltered count -- previously undiscovered defect, found by the naive-reader audit lane (Agent N) -- reachable but rare (requires a content deploy timed within an active resume window), logged to debt.md not fixed this round | hooks/useStudySession.ts:resumedQueue/resumedPos:80,88 | NEW
- [requirements] components/InterruptHandler.tsx's interrupt engine has no isProEnabled check anywhere in its activation path -- unlike every other Pro-gated feature in the codebase (specialty packs, analytics) -- any Free user can enable the full proactive interrupt engine today including desktop mandatory-lock mode, contradicting BRAND.md's pricing table -- pre-existing gap discovered during this batch's security re-audit (Agent S), out of Batch 23's scope, a genuine product/business decision requiring Max's call rather than a unilateral fix -- flagged to Max, logged to debt.md, not fixed this round | components/InterruptHandler.tsx:InterruptHandler:22 | NEW
- [requirements] Rule 23(b)/(a) synthesis: this round's dominant finding (the timeout revert) is itself the clearest instance yet of this batch's recurring meta-pattern -- a fix's own new justifying comment, written specifically to explain and defend a prior round's remediation, contained a fresh, empirically-false claim that took 3 independent agents converging from different angles (CHAOS reasoning, structural code tracing, static-bundling tracing) to actually falsify, even though the false claim was checkable by reading two files (hooks/useLangPack.ts, lib/fetchWithTimeout.ts) directly -- severity 7 | CROSS-CUTTING | SYNTHESIS

## 2026-08-17 | Task: batch 23 -- re-audit round 10 (8-agent, post round-9 remediation) -- verdict FAIL (verified, then fixed live during the same round), severity 4 (capped -- not reachable today)
- [error-handling] round-9's own revert comment for HYDRATION_STUCK_TIMEOUT_MS made a SECOND factual overclaim beyond the one it was correcting: "packLoading is GUARANTEED to resolve within 20s regardless of connection speed" -- false, since hooks/useLangPack.ts's dynamic-load effect chains fetchManifest()+loadPack() (a third loadPack() for specialty) SEQUENTIALLY via .then(), each independently 20s-bounded (not raced against one shared ceiling), plus lib/basePackLoader.ts's cache-read step runs before any timed fetch with no bound of its own -- real worst case ~40-60s+, or structurally unbounded for the cache-read portion. This is a textbook instance of this project's own worst historical pattern (a fix's new disposition comment contains a fresh, empirically-false claim, 13x prior occurrences max severity 9) recurring for a FOURTH consecutive round (7, 8, 9, now 10) -- 5-way independent convergence (Agent A, Agent B, Agent K, Agent V, Red Agent R) -- capped at severity 4 per AGENTS.md since `it` (the only ready:true language) never reaches this code path at all, so no live number is currently mistuned -- FIXED same round (comment rewritten across all 3 files it had propagated to: hooks/useHydrationStuck.ts, CLAUDE.md, .autocode/debt.md's Task #378 entry -- no numeric claim asserted this time, only a correctly-hedged explanation) | hooks/useHydrationStuck.ts:module-level comment:31 | NEW -- FIXED
- [tests] a second, independent instance of the exact tautological-assertion defect this same round's own cycle_history already documents fixing once (hooks/useHydrationStuck.test.ts's HYDRATION_STUCK_TIMEOUT_MS pin) -- hooks/useStudySession.test.ts's "passes the real INTERRUPT_FLEX_DAILY_MAX constant" test compared flexCallArgs only against the SAME imported constant the production code also imports, with no hardcoded literal anchoring the constant's actual value (9) -- found by the naive-reader audit lane (Agent N), independent of the timeout-focused investigation the other 7 agents ran -- FIXED same round (added `expect(INTERRUPT_FLEX_DAILY_MAX).toBe(9)`), verified via live Deletion Test (simulating the constant narrowed from 9 to 6 made the new assertion fail) -- severity 4 | hooks/useStudySession.test.ts:passes the real INTERRUPT_FLEX_DAILY_MAX test:1010 | NEW -- FIXED
- [documentation] app/study/page.test.tsx's round-9 comment claimed its own boundary-rewrite test alone would catch "a future accidental change to the exported value" -- imprecise: that test's two boundaries are both derived from the same imported constant, so they move together with a regression and cannot independently catch one; the value-pin lives solely in the sibling hook-level test -- Agent V finding -- FIXED same round (comment corrected to accurately scope what this specific test does and doesn't prove) -- severity 2 | app/study/page.test.tsx:571 | NEW -- FIXED
- [async] Red Agent R DECAY: production comments now point at specific LINE NUMBERS inside .autocode/debt.md (a stricter, faster-to-go-stale form of the existing debt.md-comment-coupling debt already logged in round 8) -- not fixed, same accepted-limitation disposition as the round-8 entry it's a variant of, not logged as a new separate row | .autocode/debt.md:new-row-line-number-refs | NEW
- [requirements] Rule 23(b) synthesis, second occurrence in two rounds: an agent (Agent W) reported having applied its own corrective code edit to hooks/useHydrationStuck.ts during its audit pass -- audit agents are instructed to find, not fix, and this session's own prompts for round 10 did not explicitly restate the master audit skill's "FORBIDDEN from writing code" rule to each individual agent. Verified directly (git status, file content diff) that no unintended edit survived -- the orchestrator's own sequential edits were the only changes that landed -- but this is a real process gap worth carrying into future rounds' agent prompts, not just this round's specific near-miss | PROCESS | NEW

## 2026-08-17 | Task: batch 23 -- re-audit round 11 (8-agent, post round-10 remediation; agents explicitly forbidden from Edit/Write this round) -- verdict FAIL (verified, then fixed live during the same round), severity 7
- [requirements] Red Agent R (ATTACKER lens): hooks/useStudySession.ts's handleRate requeue-on-"again" splice was completely unscoped to isInterrupt and had no cap check -- every wrong answer in an interrupt session inserted a duplicate card 3 positions ahead with nothing removed, growing the queue by 1 unbounded, silently defeating INTERRUPT_SESSION_CAP (8) and with it the 45-90s / mandatory-mode-lock-duration promise this whole batch exists to keep. Reachable on the very first real interrupt session anyone has today just by getting a card wrong -- survived 10 prior rounds because no existing test exercised "again" and INTERRUPT_SESSION_CAP together. New, single-agent finding (not independently convergent, but independently verified by the orchestrating session by direct code read before fixing) -- FIXED same round (requeue now skipped once an interrupt session is already at INTERRUPT_SESSION_CAP; the wrong card is scored and returns via normal FSRS/introduction scheduling instead of immediate in-session retry), verified via live Deletion Test (temporarily removed the guard, confirmed the new regression test failed with queue length 9 instead of 8, restored, confirmed 46/46 tests green) -- severity 7 | hooks/useStudySession.ts:handleRate:361 | NEW -- FIXED
- [tests] Agent W: the round-10 fix pinned only the derived INTERRUPT_FLEX_DAILY_MAX constant's hardcoded value; its three base constants (INTERRUPT_SESSION_FLOOR=6, INTERRUPT_SESSION_CAP=8, INTERRUPT_SESSION_MAX_NEW=3 -- BRAND.md's literal "6-8 cards" promise) had zero hardcoded-value pins anywhere in the suite, every consuming test only ever comparing against the SAME imported constant the production code reads -- a silent edit to any of the three would leave every test in the suite green, exactly the sibling-instance-of-the-same-defect-class pattern this batch's own live_patterns.md names as its #1 recurring failure mode -- FIXED same round (3 new hardcoded-value pins added to tests/queue.test.ts, Deletion-Tested by mutating INTERRUPT_SESSION_FLOOR and confirming the new pin fails) -- severity 4 | tests/queue.test.ts:new describe block:216 | NEW -- FIXED
- [documentation] Agent A: round-10's own corrective comment (explaining why HYDRATION_STUCK_TIMEOUT_MS's real worst-case bound exceeds ~20s) named only lib/basePackLoader.ts's unbounded pre-fetch cache-read step and omitted lib/specialtyPackLoader.ts's byte-for-byte identical instance of the same pattern -- since a specialty-pack load is the documented "third loadPack()" in the chain, this unbounded stretch can occur twice in series, not once -- the THIRD consecutive round (8, 9, now 10) this specific hook's justification comment has shipped a fresh, independently-checkable inaccuracy, now appearing as an incomplete generalization rather than a false claim -- FIXED same round (comment rewritten in hooks/useHydrationStuck.ts and .autocode/debt.md's Task #378 entry to name both loader files) -- severity 4 (capped, not reachable today -- `it` never enters this path) | hooks/useHydrationStuck.ts:module-level comment:44 | NEW -- FIXED
- [documentation] Agent N: CLAUDE.md's hooks/useStudySession.ts bullet still claimed a "14-param contract" -- the real UseStudySessionParams type has 16 fields (getNearDueCards, peekResumableSession, clearExpiredResumableSession added since the count was last updated) -- FIXED same round -- severity 3 | CLAUDE.md:32 | NEW -- FIXED
- [documentation] Agent N: CLAUDE.md flatly asserted interrupt sessions stay within "45-90 seconds," contradicting lib/queue.ts's own comment on INTERRUPT_SESSION_CAP disclosing a real worst case of 64-120s (up to 30s past the 90s target), a deliberate owner-ratified tradeoff -- CLAUDE.md's summary silently dropped the disclosed exception -- FIXED same round (CLAUDE.md now states the target and the disclosed worst-case exception together) -- severity 3 | CLAUDE.md:32 | NEW -- FIXED
- [documentation] Agent V (informational, not scored as a cycle_history claim): CLAUDE.md described the flex loop as calling canIntroduceNewCard(today, INTERRUPT_FLEX_DAILY_MAX) directly -- since Task #618 (Wave 6) the real call goes through canFlexIntroduceToday(canIntroduceNewCard, today), a thin wrapper -- functionally identical, textually stale -- FIXED same round | CLAUDE.md:32 | NEW -- FIXED
- [requirements] Agent B / Agent S: components/InterruptHandler.tsx still has no isProEnabled Pro-gate anywhere (open since round 9). New evidence this round: hooks/usePushRegistration.ts, mounted from inside the identical InterruptHandlerCore component, DOES gate the same interruptEngine flag on isProEnabled before uploading a push token -- a sibling in the same component tree applies opposite entitlement policy to the same feature, evidence the omission is a bug not a deliberate carve-out. Agent S separately confirmed no additional cross-user/RLS-bypass dimension (interrupt_gate_events' RLS holds regardless of entitlement state). Not fixed -- remains a product/business decision requiring Max's explicit sign-off, now flagged a third consecutive round (9, 10, 11) -- severity 5 (Agent B argues this meets the severity-7 bar; not unilaterally escalated, noted for Max's judgment) | components/InterruptHandler.tsx:InterruptHandler:3137 | NEW
- [edge-case] Multiple agents (A, B, W) re-confirmed unchanged: hooks/useStudySession.ts's resumedQueue/resumedPos desync on a deprecated queueIds entry within the 24h resume window, first found round 9, still zero test coverage, now open across 3 consecutive rounds -- not fixed this round given scope already spent on the higher-severity handleRate finding -- severity 5 | hooks/useStudySession.ts:874 | NEW
- [async] Red Agent R (new): INTERRUPT_FLEX_DAILY_MAX's "cross-session daily ceiling" is only a same-tab ceiling on the web build -- srsStore has no cross-tab sync, so two open tabs can each independently flex up to INTERRUPT_SESSION_MAX_NEW new cards, doubling the daily ceiling. Logged as debt, not fixed -- severity 4 (narrow reachability window, web-build-only) | store/srsStore.ts:introductions (no cross-tab sync):0 | NEW
- [code-quality] Red Agent R (new): the client (lib/queue.ts) and server (dueEstimate.ts) INTERRUPT_SESSION_FLOOR=6 values are coupled only by a prose "keep in sync" comment, no shared source or test enforcing the literal (only cross-file equality is tested). Logged as debt, not fixed -- severity 2 | lib/queue.ts:INTERRUPT_SESSION_FLOOR comment:24 | NEW
- [requirements] Rule 23(b)/22 synthesis: this round's headline finding (the unbounded "again" requeue) is the first NEW, previously-undiscovered live-user-impact bug found in 4 rounds (7-10 were all doc-drift/test-tautology corrections of each other) -- a reminder that even after a batch's remediation cycle settles into diminishing-severity doc/test fixes, a genuinely fresh adversarial lens (Red Agent R, unprimed except the raw code) can still surface a real, reachable, previously-missed defect in code that's already been read by dozens of agent-passes across 10 rounds -- worth keeping the adversarial/unprimed lenses in the rotation even as convergent findings shrink, rather than trimming the audit team once severity trends down | CROSS-CUTTING | SYNTHESIS

## 2026-08-18 | Task: batch 23 -- re-audit round 12 (8-agent, post round-11 remediation, agents forbidden from Edit/Write) -- verdict FAIL (verified, then fixed live during the same round), severity 6
- [edge-case] 3-way convergence (Agent A, Agent K, Agent V): round-11's atInterruptCap guard only stops an interrupt queue from GROWING past INTERRUPT_SESSION_CAP going forward via handleRate's splice -- it never clamps the SEPARATE resume-accept writer (setQueue(resumedQueue)), which had no cap of its own, unlike hooks/useStudyQueueSetup.ts's explicit initial-mount .slice(0, INTERRUPT_SESSION_CAP). A session persisted oversized by the pre-round-11 code (or any future bug) and resumed within its 24h SESSION_EXPIRY_MS window would load unclamped, directly defeating the session-length promise round 11's own fix exists to protect -- the exact "fix applied to one writer of shared state, sibling writer left untouched" meta-pattern this batch's live_patterns.md already names as its #1 recurring failure mode. FIXED same round (resumedQueue now sliced to INTERRUPT_SESSION_CAP for isInterrupt sessions, mirroring useStudyQueueSetup.ts's pattern exactly), verified via live Deletion Test (removed the slice, confirmed the new regression test failed at length 10 instead of 8, restored, confirmed 48/48 tests green) -- severity 6 | hooks/useStudySession.ts:resumedQueue:80 | NEW -- FIXED
- [async] Agent W (new): hooks/usePushRegistration.ts's onPushToken registration (a real async Tauri IPC round-trip) had no `cancelled` check immediately after resolving, unlike the getPushToken call two lines below -- a dep change (sign-out, license downgrade, interruptEnabled toggle) mid-await left the returned unlisten unassigned to the outer var when cleanup ran (a no-op), then the async IIFE resumed and registered a live listener AFTER teardown -- permanently leaked, closing over the stale userId and silently uploading every future token event under the wrong/signed-out user. FIXED same round (checks cancelled immediately after the await, calls the returned unlisten and returns if torn down, mirroring the existing !cancelled pattern), verified via live Deletion Test (new test's unlistenSpy assertion failed at 0 calls instead of 1, confirmed the leak, restored, confirmed 10/10 tests green) -- severity 6 | hooks/usePushRegistration.ts:registration effect:87 | NEW -- FIXED
- [edge-case] Round-12 direct fix (closing debt tracked since round 9, Agent N): hooks/useStudySession.ts's resumedPos read the RAW saved.position, indexing into the unfiltered queueIds rather than the filtered resumedQueue (which drops ids no longer in allCardMap, e.g. a deprecated card within the 24h resume window) -- could resolve to the wrong card or trigger isDone prematurely. Agent W (round 12) explicitly recommended fixing this now rather than re-logging a 4th round, since the fix shape was already fully specified in debt.md since round 9. FIXED same round (resumedPos recomputed by counting surviving queueIds entries before the saved position), Deletion-Tested (new dropped-id regression test failed at pos 2/wrong-card "c3" instead of pos 1/"c2" against the raw-position revert) -- severity 5->3 (downgraded per the debt.md entry, since the reachable wrong-card/premature-done risk is now closed; only StudyResumePrompt's pending-state display count remains, cosmetic-only) | hooks/useStudySession.ts:resumedPos:89 | NEW -- FIXED
- [documentation] Round-12 companion fix: app/study/page.tsx's StudyResumePrompt resumeTotal was the raw, unclamped saved.queueIds.length -- after this round's resumedQueue CAP-clamp, an oversized saved session would show a prompt promising a higher count than the accepted session actually delivers (a fresh, directly visible mismatch this round's own fix would otherwise have introduced). FIXED same round (resumeTotal now clamped to INTERRUPT_SESSION_CAP for isInterrupt), Deletion-Tested (new test's dataset.resumeTotal assertion failed at "10" instead of "8" against the unclamped revert) -- severity 4 | app/study/page.tsx:resume-prompt render:83 | NEW -- FIXED
- [documentation] Agent W (new): CLAUDE.md's lib/featureFlags.ts bullet described a stale 2-arg isProEnabled(flagValue, licenseType) signature with no expiry check -- the real 3-arg signature (Task #420) checks validUntil against SUBSCRIPTION_GRACE_PERIOD_MS, a real behavior the summary omitted entirely, at the exact function central to the still-open Pro-gate finding -- FIXED same round -- severity 3 | CLAUDE.md:30 | NEW -- FIXED
- [tests] Agent V (new): round-11's two handleRate cap tests both use isInterrupt: true, so neither isolates the `isInterrupt &&` conjunct of the guard -- a regression simplifying `isInterrupt && queue.length >= CAP` down to just `queue.length >= CAP` would silently break requeue-on-wrong for long non-interrupt sessions with no test catching it. FIXED same round (new control test: non-interrupt session at CAP length still re-inserts on "again"), Deletion-Tested (failed at length 8 instead of 9 against the simplified-guard mutation) -- severity 3 | hooks/useStudySession.test.ts:handleRate describe block:483 | NEW -- FIXED
- [code-quality] Agent A (new): hooks/useStudySession.ts crossed the ~400-line Rule 1 cap (410 lines) as a side effect of round 11's own comment additions -- the fourth recurrence of this specific file-size live_pattern across this batch's rounds. FIXED same round (multiple long historical comment blocks condensed without losing their "why" content, file landed back at exactly 400 lines after also absorbing this round's own new comments) -- severity 2 | hooks/useStudySession.ts:module-level:1 | NEW -- FIXED
- [requirements] Red Agent R (new): system clock manipulation defeats INTERRUPT_FLEX_DAILY_MAX's cross-session daily ceiling -- today is derived purely from the device's local clock (localDateStr()), no server trust anchor; setting the clock back a day resets canIntroduceNewCard's per-day count, reopening the flex loop, repeatable indefinitely. A single-device variant of the already-logged cross-tab race. Logged as debt, not fixed -- severity 3 (self-directed only, honor-system-consistent, no victim) | store/srsStore.ts:canIntroduceNewCard (today derivation):0 | NEW
- [requirements] Red Agent R (new, informational, reachability unconfirmed -- lib/srs.ts out of this round's scope): a card rated once during its intensive-introduction phase gets a real FSRS CardProgress entry independent of its IntroductionRecord -- once reps > 0 it's eligible for BOTH the cadence-driven due queue AND FSRS-driven due/near-due queues on any later day, and neither queue path excludes a card just because its cadence quota is already spent today. Batch 23's own near-due fill (getNearDueCards) widens the practical surface for this pre-existing scheduler seam to actually fire. Logged as debt (unscored -- needs a dedicated lib/srs.ts investigation to confirm or rule out) | lib/srs.ts:scheduleCard interaction with lib/introduction.ts:0 | NEW
- [requirements] Rule 23(a)/22 synthesis: this round's headline convergent finding (3 independent agents on the resume-path CAP gap) is a second consecutive round where the adversarial/naive lenses caught a real, previously-missed live bug in code many prior rounds had already reviewed -- round 11 found the first instance of this exact meta-pattern in handleRate itself; round 12 found its sibling in the resume-accept writer. Two consecutive "new real bug found" rounds after four rounds of pure doc/test correction is a strong signal the fix-then-verify-siblings discipline (per AGENTS.md Rule 23) is earning its cost here, not diminishing returns -- worth continuing the identical 8-agent process rather than trimming it | CROSS-CUTTING | SYNTHESIS

## 2026-08-18 | Task: batch 23 -- re-audit round 13 (8-agent, post round-12 remediation, agents forbidden from Edit/Write) -- verdict FAIL (verified, then fixed live during the same round), severity 7
- [edge-case] 6-way convergence (Agent A, B, K, N, V, and independently cited by S/W) -- round 12's own two fixes in the same function never composed: resumedQueue was clamped to INTERRUPT_SESSION_CAP, but its sibling resumedPos was not -- for an oversized legacy session (queueIds.length > CAP, no dropped ids) resumed at a position >= CAP, pos ended up exceeding the now-shorter queue.length, triggering isDone immediately on the very next render and silently discarding every unreviewed card with no recovery path (clearActiveSession then deletes the record). A fresh regression WITHIN round 12's own remediation, not across rounds -- the third occurrence of this batch's #1 recurring meta-pattern ("fix applied to one writer/instance of shared state, sibling left untouched"), this time between two derived values in the same commit rather than two separate writers. FIXED same round (resumedPos now clamped via the same Math.min(rawPos, INTERRUPT_SESSION_CAP) shape as resumedQueue), verified via live Deletion Test (new regression test's pos assertion failed at 9 instead of 8 against the unclamped revert) -- severity 7 | hooks/useStudySession.ts:resumedPos:88 | NEW -- FIXED
- [documentation] Agent V (new): round 12 clamped resumeTotal (app/study/page.tsx's pending-state resume prompt) but left its sibling prop resumePos on the SAME component call fully raw -- for the identical oversized-session scenario, the prompt rendered a self-contradictory "Card 10 of 8" (numerator exceeding denominator), strictly worse than pre-round-12 behavior (which was at least internally consistent, just inflated). Same meta-pattern recurring a level higher, in the display layer. FIXED same round (resumePos clamped to CAP-1, mirroring resumeTotal's own clamp), Deletion-Tested (new test's resumePos assertion failed at 9 instead of 7 against the unclamped revert) -- severity 4 | app/study/page.tsx:resume-prompt render:82 | NEW -- FIXED
- [documentation] Agent N (new): .autocode/debt.md's round-12 entry contained a dangling internal cross-reference ("see the round-12 atInterruptCap-adjacent entry below") pointing at a row that didn't exist, and separately claimed failure mode (b) (premature isDone) was "both closed" when it wasn't (see the resumedPos finding above) -- FIXED same round (entry corrected to describe the round-13 fix and remove the dangling reference) -- severity 2 | .autocode/debt.md:round-9/12/13 resumedQueue/resumedPos row:268 | NEW -- FIXED
- [requirements] Red Agent R (single-agent finding, every underlying fact independently re-verified via direct Read/grep before logging) -- SEVERITY 7, NOT FIXED THIS ROUND: a push-notification tap or plyglt://interrupt deep link received while mid-session on any other /study route navigates into a stale, mislabeled interrupt session instead of a real floor-filled one. Root cause: app/study/page.tsx mounts StudyInner with no key, so a same-pathname query-only navigation doesn't remount; hooks/useStudySession.ts's mount-fill effect is gated by a one-shot-per-hook-instance mountFillStartedRef that's already true from the prior session, so queue/pos/sessionCorrect/sessionTotal never resync while the header relabels to "Quick review." hooks/usePushInterruptTap.ts and hooks/useInterruptDeepLink.ts both router.push unconditionally with no pathname guard, unlike components/InterruptHandler.tsx's own interrupt:fire listener which explicitly checks pathname.startsWith("/study"). Push is project-memory-confirmed live-verified on a real iPhone; the plyglt:// scheme is registered in production -- reachable today, not hypothetical. Deliberately NOT fixed live this round -- both candidate fix shapes touch this file's most heavily-audited, most fragile resume/mount-fill interaction (7+ rounds of careful tuning), and a rushed fix here risks a new class of regression worse than the one being closed. Logged to debt.md as a Full-complexity item explicitly recommended for its own dedicated task/round rather than a same-round patch -- flagged prominently to Max, not buried | app/study/page.tsx + hooks/useStudySession.ts:StudyInner remount / mount-fill effect:0 | NEW
- [async] 2-way convergence (Agent A, Agent W) -- the exact "no cancelled check after an async Tauri listen()/subscribe call" defect round 12 fixed for usePushRegistration.ts's onPushToken is present, unfixed, in 5 sibling subscription sites across components/InterruptHandler.tsx (interrupt:fire, tray:study), hooks/useInterruptDeepLink.ts, hooks/usePushInterruptTap.ts, and components/AppMenuListener.tsx. Agent W's honest reachability assessment caps this at severity 4 (mostly [] deps or router-dependent effects with normally-stable references in production, though React 18 StrictMode dev-only double-invoke does trigger it today). Logged as debt with an explicit recommendation to fix all 5 in one pass rather than one-at-a-time discovery (exactly how round 12 fixed one instance and missed the other four in the same subsystem) -- not fixed this round, lower priority than the round's headline finding | hooks/useInterruptDeepLink.ts + hooks/usePushInterruptTap.ts + components/InterruptHandler.tsx + components/AppMenuListener.tsx:async subscribe calls:0 | NEW
- [code-quality] Agent W (new): store/srsStore.ts's saveActiveSession/rateCardAndSaveSession write activeSession.queueIds directly with zero INTERRUPT_SESSION_CAP awareness -- confirmed zero production callers (test-only), an unguarded landmine if ever wired to a real call site in a future refactor. Logged as low-severity debt, not fixed -- severity 2 | store/srsStore.ts:saveActiveSession/rateCardAndSaveSession:134 | NEW
- [requirements] Pro-gate escalation (Agent W, asked explicitly for an honest assessment after round 12's inconclusive owner exchange): "this has crossed into avoidance-of-decision territory... force the actual binary in front of Max directly and unavoidably... via AskUserQuestion with the two concrete options spelled out, not repeated as free text." Now open across 5 consecutive rounds (9-13) with zero technical content left to find -- Agent S (round 13) restated it as a "HEADLINE" item with nothing new, confirming further audit rounds cannot resolve a product decision. The orchestrating session is following Agent W's recommendation and raising this via a direct structured question at the end of round 13's report, not as another debt-table line item | components/InterruptHandler.tsx:InterruptHandler:20 | NEW
- [requirements] Rule 23(a)/22 synthesis, third consecutive round: rounds 11, 12, and now 13 have each found a genuinely new, previously-undiscovered live bug in code that many prior rounds had already reviewed -- round 11 in handleRate's splice, round 12 in the resume-accept writer, round 13 in round 12's OWN pair of fixes not composing together. This is now a clear pattern, not a fluke: verifying a fix in isolation is insufficient when a sibling value/writer touches the same invariant in the same commit -- the audit process should explicitly prompt future rounds to check "do this round's own multiple fixes compose correctly with EACH OTHER," not just "does each fix work in isolation" and "does this fix have an untouched sibling from a PRIOR round." Three-for-three real-bug rounds is strong evidence this 8-agent process is still earning its cost at round 13, not experiencing diminishing returns | CROSS-CUTTING | SYNTHESIS

## 2026-08-18 | Task: batch 23 -- Pro-gate decision implemented (owner-directed, post round-13 escalation)
- [requirements] Max answered the round-13 escalation directly: "Yes, gate it." components/InterruptHandler.tsx's InterruptHandler() now wires isProEnabled(flags.interruptEngine, licenseType, validUntil) into its early-return, closing a gap open across 5 consecutive audit rounds (9-13) where any Free user could enable the full proactive interrupt engine. Mirrors hooks/usePushRegistration.ts's existing pattern for the identical flag. components/InterruptHandler.test.tsx's beforeEach updated to default licenseType to "subscription" so all 27 pre-existing tests (written before this gate existed) continue to exercise InterruptHandlerCore; 3 new dedicated gate tests added and Deletion-Tested (Free blocked, expired grace period blocked, active subscription allowed) -- severity 5, RESOLVED | components/InterruptHandler.tsx:InterruptHandler:20 | RESOLVED
